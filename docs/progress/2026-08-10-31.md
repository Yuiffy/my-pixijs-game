# 进度归档：2026-08-10-31

[返回进度概览](../../progress.md)

本文件保留原 progress.md 的阶段记录。“进行中”、端口和未推送等描述仅反映当时状态；最新状态请看主索引与 Git 历史。

## 2026-08-10 · 狍子偶像羁绊调整

- 狍子偶像的羁绊由怕死 / 主持 / 跳舞改为主持 / 跳舞 / 偷袭，并同步更新偷袭成员池数据断言。
- 数据专项 `33/33`、完整自走棋回归 `369/369`、`pnpm run check` 通过；系统 Chrome 主流程无控制台错误，备战与战斗截图通过非黑屏检查并已人工检查。

## 2026-08-10 · 沐霂加入矮人羁绊

- 沐霂的羁绊由主持 / 跳舞扩展为主持 / 跳舞 / 矮人，并同步更新数据回归断言。
- 数据专项 `33/33`、完整自走棋回归 `369/369`、`pnpm run check` 通过；系统 Chrome 主流程无控制台错误，备战、战斗与移动端截图通过非黑屏检查并已人工检查。

## 2026-08-10 · 四时小路羁绊调整

- 四时小路Komichi的羁绊由流量 / 攻击性改为流量 / 杂谈 / 偷袭，并同步更新数据回归断言。
- 数据专项 `33/33`、完整自走棋回归 `369/369`、`pnpm run check` 通过；系统 Chrome 主流程与羁绊图鉴专项无控制台错误、失败请求，截图通过非黑屏检查并已人工检查。

## 2026-08-10 · 四时小路双形象与五费阵容调整

- 新增五费近战输出“四时小路Komichi”，羁绊为流量 / 进攻；基础生命 360、攻击 48、护甲 24。技能“路牌开道”持续 `5.5 / 6.25 / 7` 秒，攻击距离增加 `105 / 120 / 140`，普攻击退 `28 / 36 / 44`，并格挡 `28% / 35% / 42%` 的远程来源伤害，不附带眩晕。
- 四时小路成为首个双形象棋子：普通状态空手，技能期间切换为持组合路牌形象；施法瞬间另有独立路牌弹出特效。路牌按用户 B 站参考图制作，同一金属杆上方为黄底红圈红斜杠禁令牌，下方为蓝底白色左箭头牌并保留 `TO THE KOMICHI` 字样。
- 三档素材已接入：棋子 / 经典档直接使用用户提供的普通与持牌原始立绘；极简档使用新的朝右 Q 版双形象；细节档按本轮约定暂时复用极简双形象。独立路牌特效使用单独的透明 PNG。
- 狍子偶像升为五费并改为先锋 / 主播 / 舞者的近战护阵控制：技能获得护盾，持续期间普攻短暂眩晕并真实拉近目标。星汐Seki 升为五费，并提高生命、攻击与护甲。
- 病院坂灵和露蒂丝降为四费；病院坂灵复活数量改为 `1 / 2 / 3`，露蒂丝群疗降为 15%、两名低血友军护盾降为 12%，基础数值同步下调。
- 数据、战斗与渲染专项回归共 `210/210` 通过；完整 `pnpm run autochess:test` 为 `366/366`，`pnpm run check` 与 `pnpm run build` 通过。
- 完整回归暴露并修正两处随棋子费用变化而失真的 AI 测试夹具；Go 精确复核候选按质量排序后限制为 Top-16，避免组合候选并集突破原定预算。AI 控制专项最终为 `101/101`。
- 仓库主 `verify-autochess.cjs` 系统 Chrome 流程通过：开局、布阵、两轮战斗、结算、强化、全屏与移动布局正常，控制台错误为空。
- 新增 `scripts/verify-komichi.cjs` 系统 Chrome 专项场景。它断言空手纹理 `rift-unit:minimal:komichi`、持牌纹理 `rift-unit-ability:minimal:komichi`、独立路牌对象可见、射程 `58 -> 163 -> 58`、技能计时 `0 -> 5.5 -> 0`，并检查三项资源请求、画布尺寸、控制台 / 页面错误与截图像素。
- 空手、施法路牌、持续持牌、结束复原四张 1440x900 截图均通过非黑屏检查并逐张人工目检；角色、路牌、血条、名称和相邻单位没有不可读的遮挡。

## 2026-08-10 · 露蒂丝继续降费

- 按用户后续要求，露蒂丝从四费继续降为三费；保留此前已削弱的 `380` 生命、`32` 攻击、`31` 护甲，以及 `15%` 群疗和最低血量两人 `12%` 护盾。

## 2026-08-10 · 蛙梓细节版紫发修复

- 找回 8 月 9 日 19:46 的蛙梓细节版洋红底原始生成图；原图头发本来是饱和紫色，旧成品变灰是洋红软遮罩与去溢色误伤紫色通道，并非生成形象错误。
- 重新使用硬色键去底：自动采样 `#fb04fa`、容差 `64`、边界内收 `2px`，关闭软遮罩与 `despill`，再规范化为 512x512 RGBA、90% 主体占用；四角 alpha 均为 0。
- 系统 Chrome 检查 42px 白底、56px 棋盘底和 72px 深色底，紫发与金瞳均清楚且无可见洋红外圈；实际“细节”档准备阶段强制部署蛙梓，资源请求为 200，`render_game_to_text`、画布尺寸、控制台和失败请求均正常。
- 精灵渲染专项 `41/41`、`pnpm run check`、`pnpm run build` 与 `git diff --check` 通过；构建仅保留既有浏览器兼容数据库过期提示。
- 三费 / 四费棋池数量相应变为 `11 / 10`。数据专项 `33/33`、完整自走棋回归 `366/366`、`pnpm run check` 与 `pnpm run build` 均通过；系统 Chrome 烟测和全部截图人工检查正常，控制台错误为空。

## 2026-08-10 · 米米脑控与露蒂丝随机针筒

- 米米“脑控”新增脑波核心、友军神经连线、同步标记和敌方失控标记；原有范围护盾、近身伤害与眩晕规则不变。
- 露蒂丝“咕咕诊所”不再全队瞬间治疗或固定给两人护盾。现在按星级锁定施法距离内生命比例最低的 `3 / 4 / 5` 名受伤队友，向每人发射一枚会追踪目标的 `💉`。
- 每枚针命中时独立以等概率结算治疗或护盾，数值为目标最大生命的 `24% / 30% / 36%`；击退距离在 `48~100 / 56~112 / 64~124` 内随机，另有 `12%` 概率触发 `2.6` 倍“大力针”。命中反馈会明确标出治疗、护盾和大力结果。
- 数据专项 `33/33`、战斗专项 `136/136`、渲染专项 `41/41` 与目标 ESLint 已通过。
- 新增 `scripts/verify-mimi-rutice-effects.cjs` 系统 Chrome 专项：交叉检查 `render_game_to_text()`、Phaser 视图、Canvas、控制台、失败请求和截图像素；脑控、针筒飞行、随机命中三张 1440x900 截图均通过非黑屏检查并逐张人工目检。

## 2026-08-10 · 星汐极简身份与满区尺寸修正

- 星汐极简图改以萌娘百科的经典 Project SP 半身 / 全身设定与 B 站本人头像为身份参考：保留蓝粉爆炸双马尾、紫瞳、熊形发夹、灰色超大外套、黑白短上衣、粉色短裤和不对称长袜，移除遮挡人物的大型山猪。
- 新图使用内置 ImageGen 生成绿色色键底，经项目统一色键工具去底并规范化为 `512x512` RGBA、90% 主体占用；42px 实战尺寸仍可通过发型和大色块服装辨认。
- 修复满区变形纹理沿用原角色纹理缩放值的问题；切换满区与恢复普通形态时会按当前纹理帧重新计算显示尺寸，虫形现在与正常角色处于同一视觉量级。
- 数据素材专项 `33/33` 与目标 ESLint 通过；系统 Chrome 实战强制并排显示星汐和满区，截图人工检查正常，`render_game_to_text()`、画布、资源请求和控制台均正常。现有满区技能专项在旧的剩余时长断言处得到 `1.18s`、要求 `>=1.20s`，属于既有时序容差，与本轮显示修复无关。

## 2026-08-10 · 米米与露蒂丝技能最终门禁

- 米米脑控与露蒂丝随机针筒的完整自走棋回归 `366/366` 通过；`pnpm run check` 与 `pnpm run build` 均通过，构建仅保留既有浏览器兼容数据库过期提示。
- 最终系统 Chrome 专项仍以脑控、针筒飞行和随机命中三张已人工检查的非黑屏截图为视觉证据；控制台错误和失败请求均为空。

## 2026-08-10 · 托管风格与 AI 等级分离

- 托管配置拆成风格 `稳健 / 平衡 / 搏上限` 与等级 `新手 / 老手 / 长考 / 看穿`，另保留独立 `Go测试` 研究入口；新用户默认 `平衡 + 老手`，旧设置按原能力迁移。
- 新手只用阵容启发式，老手只用 Go v4 当前局面模型；两者均使用普通信息、跳过未来商店/敌人、真实战斗 rollout、60Hz 临战审计和组合救援搜索。长考保留模型筛选与 20/60Hz 战斗模拟，看穿继续使用 oracle 规划。
- 浏览器共享 / 单实例 rollout 缓存上限降为 `7500 / 2500`，Go IndexedDB 持久化降为 5000 条；周期保存只读取 Go schema 前缀，不再先复制整张共享 Map。
- 8 战冷基准：老手 `0` 次模拟、最慢单次托管约 `20.15ms`；新手 `0` 次模拟、最慢约 `8.93ms`。改造前普通稳健为 `1548` 次模拟、最慢约 `2308ms`。
- 新配置、快速路径、缓存限量读取与旧 Go/看穿兼容专项已通过；完整自走棋回归 `369/369`、`pnpm run check` 与 `pnpm run build` 全部通过。
- 系统 Chrome 已验证桌面、390×844 移动端和默认配置：风格与等级可独立选择、默认值为 `平衡 + 老手`，老手实际开局保持普通信息且 rollout miss 为 `0`；截图均通过非黑屏检查并逐张人工目检，控制台错误和失败请求为空。

## 2026-08-10 · 棋子属性复核与后排保护技能调整

- 露蒂丝按三费近战辅助重新收敛为 `320` 生命、`27` 攻击、`26` 护甲；病院坂灵保留复活机制，但普攻调整为 `31` 攻击、`0.96` 秒攻击间隔，降低持续 DPS。
- 五费星汐Seki 的生命提高到 `450`；三费雪烛因四秒技能盾较强，基础生命降至 `175`，攻击降至 `23`，攻击间隔调整为 `1.08` 秒。
- 三理理的旧护盾嘲讽改为“脚臭试管”：投出可见的 `🧪`，落地形成持续恐惧区域；进入区域的敌人停止攻击和施法，并实际远离该区域。
- 大黑鼠的随机怪话重做为后排保护型拖延技能：迎客松种在敌人前进路线上并持续小范围轻度减速；`75mm 大吧唧` 作为可见投射物砸伤单个敌人并击退，不再提供旧护盾、嘲讽或范围眩晕。
- 数据、战斗与渲染专项共 `210/210` 通过；完整自走棋回归 `369/369`、`pnpm run check`、`pnpm run build` 与 `git diff --check` 全部通过。系统 Chrome 专项验证了大吧唧飞行与 `48` 击退、迎客松减速区、试管飞行与恐惧逃离；六张桌面/移动截图均通过像素检查和人工目检，控制台错误与失败请求为空。

## 2026-08-10 · 隐藏 Go 测试入口

- 正式设置不再显示“研究模式”与 `Go测试` 按钮，玩家只看到三种托管风格和 `新手 / 老手 / 长考 / 看穿` 四个等级。
- 内部 `go` 策略、旧 Go 存档迁移、训练脚本、模型和持久缓存全部保留，后续仍可用于离线研究与门禁。
- 设置专项和系统 Chrome 桌面/390×844 移动端验证通过：界面中均不存在“研究模式”或 `Go测试`，布局无溢出或遮挡，老手开局仍为普通信息且 rollout miss 为 `0`；三张截图均通过像素检查并逐张人工目检，控制台错误和失败请求为空。

## 2026-08-10 · 三理理恐惧区缩短

- “脚臭试管”三档区域持续时间由 `4.2 / 4.8 / 5.4` 秒统一缩短为 `1` 秒，星级仍提升半径；区域消失后只保留约 `0.16` 秒的状态刷新余量，随后恢复索敌，避免敌人在边缘反复进入、逃离形成近似永久硬控。
- 数据专项 `33/33`、战斗专项 `136/136` 与目标 TypeScript ESLint 通过；新增回归明确验证区域消失、恐惧清零、恢复锁敌并且继续推进不会重新恐惧。完整自走棋回归 `369/369`、`pnpm run check` 与 `pnpm run build` 均通过。
- system Chrome 专项确认技能文本状态为 `118` 半径、`1` 秒持续，恐惧目标实际远离；六张桌面/移动截图通过非黑屏检查并逐张人工目检，控制台错误和失败请求为空。
- 完整自走棋回归 `369/369`、`pnpm run check`、`pnpm run build` 与本轮源码/测试范围的 `git diff --check` 全部通过；构建仅保留已有的浏览器数据过期提示。

## 2026-08-10 · 中单光一极简形象修正

- 按用户指定的萌娘百科新版立绘重做中单光一极简图，改为最常见的灰棕短发、浅色宽檐帽、白色长外套、黑围巾、黑裤与长靴形象，并统一朝画面右侧。
- 采用普通站姿来保证 42px 棋子尺寸下的身份辨识；足球进球庆祝式滑跪继续由战斗位移动画表达，不把动作固定在静态角色图里。
- 新图经绿色色键去底并规范化为 `512x512` RGBA，主体最长边占画布 90%。
- 精灵专项 `41/41`、`pnpm run check` 与 `pnpm run build` 均通过；系统 Chrome 强制在棋盘、备战席和五个商店位同时显示光一，实际显示尺寸约为 `54px / 59px / 68px`，素材请求、`render_game_to_text()`、画布、控制台和失败请求均正常，截图通过非黑屏检查并人工确认朝右与小尺寸辨识度。

## 2026-08-10 · 四时小路都市传说重做

- 常驻被动改为【路牌格挡】：未持牌也能概率减免远程来源伤害，成功时一定弹出路牌素材、回复额外能量并获得 `1.1` 秒移速；持牌期间格挡率由一星 `42%` 提高到 `72%`，二三星同步成长。
- 主动技能更名【都市传说降临】，采用 `100` 能量持续消耗；自动、攻击、受击和格挡都能补能。首次发动和持牌期间再次回满都会触发【出道冲击】，高速贴近目标、撞开沿途敌人，并伤害和单体眩晕主目标；每次冲击消耗 `30` 能量，避免逐帧重复触发。
- 持牌普攻继续小幅击退，并为每次攻击加入路牌砸头动画；空手 / 持牌双形象、格挡路牌和砸击路牌均通过 `render_game_to_text()` 暴露变体以便验证。
- 数据、战斗与渲染专项 `210/210`，完整自走棋回归 `369/369`、目标 TypeScript ESLint、`pnpm run check` 与隔离目录生产构建均通过；构建只保留已有浏览器数据库过期提示。
- system Chrome 专项覆盖空手、常驻格挡、首次冲刺、沿途撞飞、目标眩晕、持牌砸牌、持牌格挡、回满再冲和能量耗尽退场。九张 `1440×900` 截图通过非黑屏检查并逐张人工目检，空手 / 持牌切换和路牌效果可见，控制台错误与失败请求为空。

## 2026-08-10 · 自走棋 v0.3.0 玩家更新日志

- 自走棋版本从 `0.2.3` 升级到 `0.3.0`，同步更新 `package.json`、游戏内版本弹窗和根目录 `CHANGELOG.md`。
- 更新日志改为面向玩家的描述，重点说明四时小路、羁绊/平衡、技能反馈、托管设置和棋子形象；移除训练次数、日志容量、模型指标和控制台接口等内部信息。
- 同步更新版本断言；发布一致性检查通过，完整自走棋回归 `369/369`、`pnpm run check` 和 `pnpm run build` 均通过。
- 系统 Chrome 桌面与 390×844 移动端均打开更新弹窗并人工检查；弹窗分别显示 `v0.3.0 · 都市传说降临`，无横向溢出，控制台错误为空。

## 2026-08-10 · 四时小路横扫与回能收紧

- 四时小路基础攻击由 `48` 降到 `44`；持牌期间停止自动回能和攻击回能，只保留受击回能与成功【路牌格挡】的额外回能，补满后仍可再次触发【出道冲击】。
- 持牌普攻改为朝主目标横扫 `110` 度扇形；一至三星范围为 `145 / 160 / 180`，副目标受到 `55% / 60% / 65%` 伤害且不附带控制，原小幅击退只作用于主目标。
- 战斗路牌新增扇形扫过轨迹，横扫末段、出道冲击命中和格挡均采用倾斜旋转弹开的节奏；单字占位由“路”改成 `+`，路牌旁同步出现 `+` 闪光。
- 数据、战斗与渲染专项 `210/210` 及目标 TypeScript ESLint 通过。system Chrome 专项覆盖空手、常驻格挡、首次冲刺、沿途撞飞、目标眩晕、扇形横扫、持牌格挡、受击回满再冲和能量耗尽退场；九张 `1440x900` 截图通过非黑屏检查并逐张人工目检，控制台错误与失败请求为空。
- 完整自走棋回归 `369/369`、发布一致性检查、`pnpm run check`、`pnpm run build` 与 `git diff --check` 全部通过；构建仅保留已有的浏览器数据过期提示，现有 `3100` 游戏页面继续正常响应。

## 2026-08-10 · 梨安极简韩舞形象修正

- 重做梨安极简图，保留紫灰长发、侧辫、心形发夹、紫粉眼睛和白紫主色，服装改为短外套、内搭、短裙裤与厚底舞台靴组成的韩舞 / 偶像女团舞台装；保持徒手、朝右和极简粗轮廓。
- 姿势改为双脚鞋底落在同一地面线、膝盖微屈、重心侧移并配合脸旁手势的女团编舞定格；移除单脚后抬、足尖鞋、绑腿、芭蕾手位、婚纱与头纱语义。
- 新图使用内置 ImageGen 结合本地身份与旧极简风格参考生成绿色色键底，经软遮罩、去溢色和 `1px` 边界内收后规范化为 `512x512` RGBA，主体最长边占画布 90%，四角完全透明。
- 精灵专项 `41/41`、`pnpm run check`、`pnpm run build` 与 `git diff --check` 全部通过；系统 Chrome 在棋盘、备战席和五个商店位同时显示梨安，实际尺寸约 `54px / 59px / 68px`，素材请求、纹理、`render_game_to_text()`、画布、控制台和失败请求均正常，截图通过非黑屏检查并逐张确认双脚站姿和小尺寸辨识度。构建仅保留已有浏览器数据过期提示。

## 2026-08-11 · 看穿战斗期后台预演

- 浏览器托管 Worker 协议新增分片式 `prewarm`：长考 / 看穿在真实战斗期间，从一次完整快照按 60Hz 精确推进到下一整备，并用独立投机托管器预先填充 Worker 内共享战斗评分缓存；预热响应不包含操作，正式托管器状态与真实桥接器均不被修改。
- 首片之后只发送无快照续片，每片最多推进一次托管 tick；每片返回主线程都会重新核对当前战斗、种子、回合、等级、商店随机游标、阵容、风格和思考等级，阶段变化或状态不符时立即停止续片并转入正式决策。
- 中途首次开启看穿时，扩大搜索预算现在归属于“下一次实际整备”；在整备期开启则仍作用于当前回合。思考浮层会区分“下一回合预演中”和整备期“看穿推演中 / 长考中”。
- 四档使用统一的 `153100--153103` 商店种子、`152104` 敌人种子、20Hz 粗筛 / 60Hz 实战跑到最多 20 战：新手平均终止战 `14.25`、老手 `17.25`、长考与看穿均为 `20`；长考与看穿均为平均 `19` 胜，看穿平均净资产 `290`、略高于长考的 `289.5`。看穿没有低于长考，并会在后段按未来敌人分叉。
- Worker 浏览器分包中移除对 `window` 的隐式依赖，状态与成长持久化改为检测 `globalThis.localStorage` 能力；正式 Worker 异常时会立即切换老手快速回退并重放待处理开局，不会卡在标题页。
- 系统 Chrome 从第 1 战中途由老手切到看穿通过：真实创建 3 次 Worker 生命周期，动画连续 4 帧约 `16.6ms`，战斗时钟继续推进，主线程 rollout miss 为 `0`，控制台错误和失败请求为空。移动端预演提示移到第二行右侧，不再遮挡核心生命或左侧控制。
- 最终门禁：AI 控制 `114/114`、Go / 小模型专项 `38/38`、完整自走棋 `381/381`、`pnpm run check`、`pnpm run build`、根主流程、Worker 预演专项与策略设置专项全部通过；所有最新桌面、移动、全屏和阶段截图均通过像素检查并逐张目检。干净开发服务运行于 `http://127.0.0.1:3300/game/autochess`。

## 2026-08-11 · 四时小路横扫视觉简化

- 移除持牌普攻横扫的扇形填充与两侧边缘描线，不再显示类似量角器的攻击范围；路牌仍按原 `110` 度轨迹横扫，并在末段倾斜旋转弹开。
- 扇形伤害判定、主副目标倍率、击退和回能规则均未改动。渲染专项 `41/41` 与目标 TypeScript ESLint 通过；system Chrome 专项确认 `sweep` 路牌可见、该特效的 `shape` 图层不可见、主副背后目标伤害仍为约 `50.6 / 27.83 / 0`，截图通过非黑屏检查并已人工确认无范围边线，控制台错误和失败请求为空。
- `pnpm run check`、`pnpm run build` 与 `git diff --check` 通过；构建仅保留已有的浏览器数据过期提示。生产构建覆盖共享 `.next` 后已重启 `3300` 开发服务，并在新进程上再次通过四时小路专项与截图目检。

## 2026-08-11 · 搏上限看穿经济、冗余操作与工坊诊断

- 固定商店种子 `153100`、敌人种子 `152104`、搏上限 + 看穿、20Hz 粗筛 / 60Hz 实战复现玩家观感：旧路径首战花到 `1` 金，第 12 战曾出现 `24` 买 / `20` 卖 / `23` 移动；满阵容后会连续空刷，且旧终局报告直到第 32 战才首次使用工坊。
- 首战补人口、升本和稳定支出现在至少保留 `5` 金；同回合卖出的棋种禁止回购（立即合成除外），新买 / 刚合成 / 工坊升级的 UID 本整备期禁止卖出；经济收尾后停止重新购物，首次落位后停止候补清理和利息出售。
- 第 13 战阵型 trace 从同期 `124` 次移动降到 `70` 次；最终落位为一次腾位加七个 UID 的单向移动，没有同 UID 往返。报告：`artifacts/autochess-highroll-oracle-seed153100-r13-trace-after-v2.json`。
- 工坊取消第 32 战隐藏门槛，额外现金余量从 `200` 降到 `20`。进一步修复：入选阵容的一星棋也可直升；最优二星太贵时回退到可负担的一星；经济收尾 / 锁定胜阵后不再为无法执行的商店购买跳过工坊；满棋盘满候补仍允许购买会立即合成的棋。
- 长考 / 看穿在第 18 战后连续 4 次付费空刷会提前请求 60Hz 精确阵容复核，避免 20Hz / 模型假负分把金币刷光。固定 trace 中第 19 战在阵容不再变化后曾从 `20` 金连续空刷到 `0`，最终精确落位却得到 `10535` 胜分；修复后该战刷新降到 `6` 次并保留 `20` 金。
- 同种子最新冷缓存终局：搏上限 + 长考和搏上限 + 看穿均在第 `26` 战死亡、`23` 胜；看穿在第 24 战解锁并直升、之后继续使用工坊，整局 `4` 次工坊动作。看穿 `707` ticks、总 CPU `80.60s`、真实均值 `114.01ms/tick`、最慢 `2921.97ms`；长考 `744` ticks、总 CPU `70.16s`、均值 `94.30ms/tick`、最慢 `1726.23ms`。
- benchmark 修正 `averageAutopilotTickMs` 聚合口径，并新增 `averageAutopilotRunMs` 与 `totalAutopilotTicks`；trace 额外记录升级剩余、利息、人口上限和工坊状态。
- 当前 AI 控制专项增至 `122/122` 通过，新增工坊 / 十本储蓄 / 连续空刷精确复核回归；目标 TypeScript ESLint 与 benchmark `node --check` 通过。
- 待完成：完整自走棋回归、`pnpm run check`、`pnpm run build`、系统 Chrome 主流程与截图目检；再跑平衡风格长考 / 看穿终局作为与既有第 31 / 32 战基准的同口径比较。
- 平衡 + 看穿第 41 战逐动作 trace 进一步定位到不可逆替换：已有唯一三星四时小路、雪烛、苔庭医者和花寄鹤，却因当前战略低于安全线而依次卖出，购买一星新项目再用工坊重练；并非工坊未触发，而是危险局面的 `replacementAction` 放开了全部高星牺牲。
- 长考 / 看穿现在保护唯一三星成品：未立即连锁合成三星的商店购买不得以它作为替换牺牲，候补清理、筹资和利息出售也不会把它当作闲棋；同棋种已有另一枚三星时仍允许清理重复成品。
- 曾实验“看穿自适应目标池解锁后整局锁定”，但固定种子第 21—28 战膨胀到单战 `10+` 次出售、`16+` 次购买，冷缓存单战接近一分钟，已撤回；金币门槛继续作为停止新项目投资的刹车。
- 新增唯一三星保护回归；AI 控制专项当前 `126/126` 通过，目标 ESLint 与新增目标测试通过。待重新冷跑第 41 战 trace 和终局生存，确认修复没有引入操作或思考时间退化。

## 2026-08-11 · 看穿终局工坊、空刷与落位收尾

- 平衡 + 看穿第 41 战唯一三星保护修复后只执行 `1` 次购买、`2` 次工坊、`0` 出售、`0` 刷新、`0` 移动；保留四时小路、雪烛、苔庭医者和花寄鹤等唯一三星核心，净资产由 `680` 提高到 `850`，预测分由 `10190` 提高到 `10685`。
- 未来商店刷新现在同时受剩余刷新预算和实际可执行购买约束；第 47--60 战每回合只使用 `1--2` 次赠送刷新，终局续跑 `paidRerolls = 0`，不再满阵容后付费空刷。
- 工坊解锁动作不再清空已经精确验证的胜阵；锁定的精确主力在落位前可把工坊支出降到正常利息储备线，不再额外保留无用途的 20 金。第 44 战实际完成二星塔神后才一次落位，动作收敛为 `2` 买、`2` 工坊、`0` 卖、`0` 刷、`5` 移动并获胜。
- 固定商店种子 `153100`、敌人种子 `152104`、20Hz 粗筛 / 60Hz 实战，从第 41 战快照冷缓存续跑到第 `60` 战死亡，完整局共 `57` 胜；旧版在第 `46` 战死亡，上一轮修复在第 `56` 战死亡。本次第 42--58 战全胜，第 59 战首次掉血。
- 第 42--60 战共 `325` 个托管 tick、CPU `172.15s`，冷缓存平均 `529.70ms/tick`、最慢 `7.85s`；最慢点是第 60 战的 60Hz 精确落位搜索。浏览器正式路径在 Worker 中运行且战斗期会后台预演，因此长考不会阻塞 Phaser 渲染线程。
- 修复交互运行环境在模块加载时过早固化的问题；浏览器同款终局规划重新稳定使用最多 6 战短滚动前缀，不再误回退到整条路线重放。AI 控制专项 `131/131`、目标 TypeScript ESLint与脚本语法检查通过。
- 诊断报告：`artifacts/autochess-balanced-oracle-seed153100-r41-after-v9.json`、`artifacts/autochess-balanced-oracle-seed153100-r70-after-v11.json`；第 44 战逐动作 trace 为 `.tmp/autochess-balanced-oracle-r44-trace-v13.json`。
- 最终门禁：完整自走棋回归 `398/398`、`pnpm run check`、`pnpm run build` 与 `git diff --check` 通过；构建仅保留既有浏览器数据库过期提示。
- 仓库 `verify-autochess.cjs` 系统 Chrome 主流程通过；Worker 预演专项实际创建 `3` 个 Worker，四帧动画跨度 `19.2ms`、主线程 rollout miss 为 `0`，控制台错误与失败请求均为空。19 张桌面、移动、全屏、战斗和预演截图通过像素检查并逐张人工目检。

## 2026-08-11 · 搏上限看穿满阵容工坊与终局复测

- 满级、预测失败的濒死棋盘现在让工坊沿用稳定化刷新的储备线，并可忽略当前商店项目，优先升级精确阵容中的场上主力；工坊解锁与升级不再被无关商店候选长期压后。
- 20Hz / 模型粗筛触发 60Hz 精确复核后，整回合最多再允许 `4` 次付费刷新；买牌或空刷计数重置不会重新开放额度。新买、刚合成或刚经工坊升级的棋子也不会在同一整备期立刻进入出售候选。
- 固定商店种子 `153100`、敌人种子 `152104` 的第 25 战，旧路径为 `26` 刷新、`14` 购买、`5` 出售、`0` 工坊；正式无 trace 路径收敛到 `2` 刷新、`1` 购买、`1` 出售、`1` 工坊。该种子仍在第 25 战死亡，但异常操作已经消失，目标诊断 CPU 约由 `38.6s` 降到 `20.4s`。
- 四个商店种子 `153100--153103` 在同一敌人种子下的终止战为 `25 / 19 / 40+ / 17`，平均 `25.25`；搏上限依然保留高方差，而不是把四局强行收敛成相同阵容。
- 强牌种子 `153102` 续跑到第 `59` 战死亡，共 `56` 胜；第 43 战已拥有场上与候补合计 `18` 个三星。第 40--59 战付费刷新均为 `0`，每回合显示的 `1--2` 次刷新都是免费刷新。
- 同一 59 战长局冷缓存共 `1215` 个托管 tick，平均 `229ms/tick`、最慢 `7.06s`；热缓存命中 `1070`、miss `0`，平均 `186ms/tick`、最慢 `3.83s`。后期主要成本已是残血阵容与站位组合搜索，真实战斗模拟缓存不再是唯一或主要瓶颈。
- 报告：`artifacts/autochess-highroll-oracle-seed153100-153103-r40-after-v13.json`、`artifacts/autochess-highroll-oracle-seed153102-r70-after-v13.json`、`artifacts/autochess-highroll-oracle-seed153102-r70-warm-v13.json`。
- 最终门禁：AI 控制专项 `133/133`、完整自走棋 `400/400`、`pnpm run check` 与 `pnpm run build` 全部通过；构建仅保留既有浏览器数据库过期提示。干净 `3500` 服务上的系统 Chrome 主流程再次通过，Worker 预演创建 `3` 个 Worker、四帧跨度约 `19.6ms`、主线程 rollout miss 为 `0`，控制台错误和失败请求均为空；19 张本轮截图已逐张目检且无黑屏、遮挡或布局回归。

## 2026-08-11 · 平衡看穿中期完整阵容救场

- 固定敌人种子 `152104` 复现“平衡 + 看穿”约第 17 战猝死：第 18 战前的现代看穿通常只精算长考级别 Top-2，两个候选全败后也不会扩大到看穿的 Top-4；更关键的是局部候选生成漏掉了完整换阵胜解。商店种子 `153102` 第 17 战有 `6,226` 套不同合法阵容，离线 60Hz 精算找到 8 套胜解，但旧搜索一套都没有生成。
- 普通托管 tick 继续使用小预算；Top-2 真实精算全败时才扩大到看穿 Top-4。最终 60Hz 精算仍全败时，Worker 内触发一次紧急救场：枚举并去重完整合法阵容，模型取前 64 套，20Hz 粗模拟筛选，再最多用 4 套 60Hz 真实战斗验证。不会固定实际战斗种子，也不会阻塞 Phaser 渲染线程。
- `153102` 同一第 17 战随机状态由旧版预测 `-121` 并死亡，修复后真实战斗获胜且剩余 `5` HP，优势约 `+3.88`。16 个未见商店种子 `153100--153115` 的同口径矩阵中，第 12 战胜率由 `31.25%` 升至 `56.25%`，第 16 战胜率由 `68.75%` 升至 `81.25%`，第 20 战存活率由 `56.25%` 升至 `81.25%`，20 战前死亡由 `6/16` 降至 `3/16`。
- 16 种子矩阵平均托管 tick 为 `36.21ms`；最慢一次紧急长考 `3.49s`，运行在 Worker 内。报告为 `.tmp/autochess-balanced-oracle-r20-16-exhaustive-rescue-v2.json`，单局翻盘报告为 `.tmp/autochess-balanced-oracle-seed153102-r17-exhaustive-rescue.json`。
- 已知边界：`153103` 第 17 战的唯一胜解模型只排第 94，且 20/30/40/50Hz 都误判，只有 60Hz 判胜；实时穷举需十几秒以上，未为这一例无限扩大在线预算。`153108` 在模型/启发式覆盖的 193 套 60Hz 阵容中仍无胜解，更像前期来牌路线已无解；后续应补新版战斗数据重训模型。
- 最终门禁：AI 控制专项 `135/135`、完整自走棋 `402/402`、`pnpm run check`、`pnpm run build` 与 `git diff --check` 全部通过；构建仅保留既有浏览器数据库过期提示。仓库系统 Chrome 主流程和 Worker 预演专项通过，实际创建 `3` 个 Worker、主线程 rollout miss 为 `0`、控制台错误为 `0`，桌面 / 移动截图近黑比例为 `0.0008 / 0.0005`；19 张截图均已逐张人工目检。

## 2026-08-11 · 搏上限看穿败后恢复、候补经济与终局性能

- 玩家截图中的 `6/7` 为托管购买、合成和换位过程中的中间态；回归统一在“开战前最终状态”统计是否上满。固定种子第 `1--40` 战全胜且最终少上人口回合为 `0`，因此不把操作中的 `6/7` 判为故障。
- 搏上限在本局出现任何败局后会立即进入恢复压力：血量尚可时按平衡目标评估未来阵容，濒死时按稳健目标排序，并对粗筛判负的开战决策重新执行 60Hz 精确审计，不再因“搏上限就应该接受掉血”而过早开战。
- 候补经济从“某棋种全部保留”收紧为每个财务追星项目只保留最强一枚；与当前主力无关的早期一星库存会在候补压力或利息节点出售。一旦最终落位开始，阵容会冻结到开战，不再重复买卖或来回换阵；同棋种同星级的站位也按 UID 稳定化。
- 交互模式的完整阵容池超过 `8,000` 套时，看穿沿用小模型筛选与有限 60Hz 复核，不再二次全量穷举；第 17 战约 `6,226` 套的中期翻盘搜索仍保留。
- 固定商店种子 `153100`、敌人种子 `152104` 的最终压测：第 `1--40` 战全胜，第 `57` 战死亡，第 `47 / 55 / 57` 战失败；第 40--57 战全部刷新都是免费刷新，付费刷新为 `0`。第 47 战满候补的棋子均为三星成品，不是没有追星价值的一星库存。
- 终局性能优化后，第 47 战的慢点从 `54.09s` 降到 `3.26s`；第 40--57 战总 CPU 从 `215.4s` 降到 `151.5s`，平均从 `580.7ms/tick` 降到 `408.5ms/tick`，最慢从 `54.09s` 降到 `8.12s`。新的最慢点是第 57 战仅剩 `4 HP` 时的最终生存复核，运行于 Worker 中，不阻塞 Phaser 渲染线程。
- 报告：`.tmp/autochess-highroll-oracle-seed153100-r40-terminal-v8.json`、`.tmp/autochess-highroll-oracle-seed153100-r57-profile-v8.json`、`.tmp/autochess-highroll-oracle-seed153100-r57-profile-bounded-v9.json`。终局续跑使用第 40 战开战前快照，会重放第 40 战且可能多执行一次工坊，因此它用于终局压力测试，不作为无缝续档证据。
- 最终验证：AI 控制专项 `143/143`、完整自走棋 `410/410`、`pnpm run check`、`pnpm run build`、系统 Chrome 主流程、Worker 预演、策略设置和 autoplay 浏览器验证全部通过。Worker 预演实际创建 `3` 个 Worker，主线程 rollout miss 为 `0`；29 张标题、备战、战斗、结算、全屏、移动、设置与预演截图均已逐张目检，无黑屏、透明画面或明显遮挡。

## 2026-08-12 · 稳健与搏上限风格校准

- 搏上限继续在首次实际败局后立即止损：血量尚可时按平衡目标恢复，临界血量按稳健目标恢复。曾实测“允许一场败局后继续冒险”，第 20 战存活从 `81.3%` 降到 `50%`，风险过量，未保留该实验。
- 搏上限四理财银行期改为分层兑现：普通购买最多牺牲 `1` 档利息，优质购买 `3` 档，合成 `5` 档，升人口 `3` 档；凑理财仍可牺牲 `2` 档。它会先建立理财和利息优势，但遇到真正好牌、合成或人口战力时不会死守现金。
- 固定商店种子 `153100--153115`、敌人种子 `152104`、20Hz 粗筛 / 60Hz 实战、最多 20 战：搏上限第 20 战存活保持 `13/16`；四理财覆盖由 `12/16` 提高到 `15/16`，首次四理财由平均第 `12.58` 战提前到 `11.73`，平均利息由 `1.84` 提高到 `2.77`，平均净值由 `284.6` 提高到 `296`。净值标准差由 `28.1` 提高到 `38.4`，范围由 `237--329` 扩大为 `204--368`，形成更高上限与方差。
- 稳健的后期牌购买窗口由第 `12` 战提前到第 `10` 战；安全十本的终局投入门槛调整为 `124` 金激活 / `44` 金保留，已有六份项目可在 `104` 金激活 / `24` 金保留。健康、安全且现金明显溢出时不再强制要求四理财，`16 HP` 受伤线及以下仍停止非必要终局投资。
- 稳健 + 看穿固定种子 `153100` 在没有凑成四理财时仍打满 32 战并取得 31 胜：第 22 战解锁工坊，之后持续升级十人阵容，第 31 战已有 `4` 个场上三星；没有出现稳住前期后停止转型。报告为 `.tmp/autochess-style-oracle-survival-seed153100-r32-tuning-v1.json`。
- 最终门禁：AI 控制专项 `144/144`、完整自走棋 `411/411`、目标 ESLint、脚本语法检查、`pnpm run check`、`pnpm run build` 与 `git diff --check` 全部通过。干净 `3700` 服务上的系统 Chrome 主流程、风格设置、Worker 预演和 autoplay 专项通过；Worker 实际创建 `3` 次、主线程 rollout miss 为 `0`，控制台错误和失败请求均为空。29 张标题、备战、战斗、结算、全屏、移动、设置与预演截图通过像素检查并逐张目检。

## 2026-08-12 · 搏上限动态卖血与 30 战资产评估

- 移除“历史上输过一次就整局永久止损”的开关。搏上限现在只按当前血线降档：`HP > 10` 保持搏上限，`9--10 HP` 改用平衡恢复目标，`HP <= 8` 才进入稳健保命；当前阵容预测失败时仍保留独立的局部稳定化流程。
- 健康搏上限的当前战止血预算限制为最多 `8` 次空刷新、最多牺牲 `3` 档利息；进入受伤线后才放开原有完整预算。看穿在健康但 20Hz 粗筛判负时，以 60Hz 精确模拟复核当前阵容，再决定投入小额预算或有意识地卖血，避免粗筛假阴性白花钱。
- 新增的两个健康止血参数进入离线策略训练搜索空间，并受完整稳定化上限约束。AI 控制回归新增历史败局不永久降档、动态预算和 60Hz 假阴性复核覆盖。
- benchmark 每战新增场上卖价、板凳卖价、战后存款与三者总资产；目标战报告同时输出到达率、存活率、死亡局数量、到达者与幸存者分布，不再用包含升本投入的 `netWorth` 代替现金资产。
- 固定商店种子 `153100--153115`、敌人种子 `152104`、20Hz 粗筛 / 60Hz 实战、最多 30 战：30 战存活由 `9/16` 提升到 `10/16`，平均终止战由 `26.44` 提升到 `27.06`；四理财保持 `16/16`，平均首次达成由第 `12.44` 战提前到第 `12.25` 战，平均利息由 `4.04` 提升到 `4.12/战`。
- 6 个新旧都活到第 30 战的配对种子中，平均剩余 HP `12.5 -> 12.83`、场上价值 `206.33 -> 217.67`、存款 `107.67 -> 111.17`、总资产 `360.83 -> 373.83`。全体第 30 战场上价值上限由 `230` 提高到 `263`、总资产上限由 `441` 提高到 `446`，同时场上价值标准差由 `14.10` 扩大到 `36.95`，保留更高上限与方差。
- 托管平均单局 CPU 由 `53.56s` 降到 `43.98s`，平均 tick 由 `86.14ms` 降到 `72.02ms`；刷新 / 购买 / 出售动作分别由 `620 / 1627 / 982` 降到 `572 / 1525 / 890`。报告为 `.tmp/autochess-highroll-oracle-r30-baseline.json` 与 `.tmp/autochess-highroll-oracle-r30-dynamic-v2-16.json`。
- 最终门禁：完整自走棋回归 `413/413`、训练 Worker 冒烟、`pnpm run check` 与 `pnpm run build` 全部通过；构建仅保留既有浏览器数据库过期提示。
- 干净 `3800` 服务上的系统 Chrome 主流程、Worker 战斗期预演和 autoplay 专项通过。战斗中由老手切到看穿实际创建 `3` 个 Worker，四帧跨度约 `16.7ms`，主线程 rollout miss 为 `0`，控制台错误与失败请求均为空；27 张标题、备战、战斗、结算、强化、全屏、移动、设置和预演截图通过像素检查并逐张目检。

## 2026-08-27 · 自走棋 v0.4.0 战术暂停与验收修复

- 久置复查先以完整自走棋回归 `413/413` 和仓库 system Chrome 主流程建立基线。玩法本身没有发现现成回归，但根验收脚本已经脱离新版 DOM HUD：旧坐标没有真正锁店、购买或升本，羁绊截图也没有打开说明层，导致脚本看似通过却没有覆盖声明的操作。
- 战斗阶段新增暂停 / 继续按钮与 `P` 快捷键；暂停会同时冻结 Phaser 前台固定步长和隐藏页后台补时，离开战斗后自动清理。暂停状态会明确显示，羁绊与战斗统计仍可查看。
- `window.autoChessAI` 新增 `pause()` / `resume()`，`render_game_to_text().interface.battlePaused` 与可用操作同步公开状态；输入框、选择框和可编辑区域继续屏蔽全局快捷键，战斗按钮聚焦时仍允许 `P` 正常切换。
- 移动端战斗工具改为稳定的紧凑图标尺寸，暂停、快速结算、统计与镜头控制在 `390x844` 下保持 `0px` 横向溢出；桌面继续显示图标、名称和键位。
- 根 Chrome 验收改用 DOM 可访问名称执行锁定 / 解锁、两次购买、升本、暂停 / 继续和 `P` 键，等待三张协议头像加载，并检查移动端溢出、控制台错误与失败请求。PNG 非黑屏检查抽到公共工具，AI 验收同步复用；AI 脚本改从 `package.json` 读取版本，移除长期滞留的 `v0.2.3` 弹窗断言和重复 PNG 解析器。
- 自走棋版本由 `0.3.0` 升到 `0.4.0`，同步更新 `package.json`、游戏内版本弹窗和 `CHANGELOG.md`；发布一致性检查与 AI 浏览器契约验收通过。
- 最终验证：暂停专项 `3/3`、完整自走棋回归 `414/414`、目标 ESLint、脚本语法检查、`pnpm run check`、`pnpm run build` 与 `git diff --check` 全部通过。system Chrome 根主流程和 AI 契约流程通过，控制台错误与失败请求均为空；19 张主流程截图与 5 张 `v0.4.0` AI 截图全部通过纯色 / 透明 / 近黑检查并逐张人工目检。构建仅保留既有 Browserslist / Baseline 数据过期提示。
- 已知边界：移动端战斗继续把 `1120x720` 固定世界完整缩进竖屏，因此战场比桌面小且上下留白明显；要进一步改善需设计移动端镜头跟随或分区观察模式，不能只靠放大 CSS，否则会裁掉单位和战斗统计。

## 2026-08-28 · 游戏内更新日志展示完整历史

- 游戏内更新日志从只渲染 `AUTOCHESS_RELEASE` 最新对象改为渲染 `AUTOCHESS_RELEASE_HISTORY`，同步收录 `v0.1.0` 至当前 `v0.4.1` 的 8 个玩家可见版本。
- 弹窗改为按版本倒序的可滚动时间线，当前版本保留强调样式；桌面和移动端均能滚动查看最旧版本，移动端无横向溢出。
- 新增更新日志回归测试与 `pnpm autochess:verify:release-notes` 专项脚本，校验游戏内版本集合与根目录 `CHANGELOG.md` 的版本集合一致，并检查顶部 / 底部截图。
- 验证：更新日志专项 `1/1`、完整自走棋回归 `415/415`、`pnpm autochess:release:check`、`pnpm run check`、`pnpm run build`、system Chrome 根主流程和更新日志专项均通过；专项桌面内容高度 `2755px`、移动端内容高度 `3477px`，两端横向溢出均为 `0px`，控制台错误与失败请求为空。

## 2026-08-30 · 武侠小说生成器重构

- 当前任务原始目标：`/goal /game/wuxia 的武侠小说生成器，还非常简陋、有缺陷。你来找到当前的问题，并且调研 构思如何做一个更完善、更好玩、更有扩展性、更合理的武侠小说生成器，并将其实现。`
- 旧版自动滚动片段改为 12 回合、4 章节的互动小说：开局选择姓名、出身、执念和种子，每回合三选一并公开风险、资源变化和检定概率，结局综合执念、线索、属性、风声与关系。
- 新增纯数据 `novelEngine`、可复现 RNG、关键事件/通用事件/失败推进、两条同行者事件、地点发现、结局评分与 `localStorage` 自动存档；事件入场统一同步正文地点、当前地点和地图。
- 门派、游侠和镖局现在有一致的归属、出生地、盘问场景与人物形象；修复镖局误挂丐帮、终章 `13/12`、移动端顶栏图标被隐藏、设置文案与实际清档行为不符等问题。
- 旧世界/片段适配层补齐真实门派 ID、回合/标志/同伴字段、经验/最大气血写入、死亡与结束结果合并，保留历史内容兼容性。
- 新界面采用墨色、朱砂、玉色与金色信息层级，桌面三栏、移动端正文优先；江湖志、行路图、同行者和设置使用可访问抽屉，图片改用稳定尺寸的 Next.js 图片组件。
- `pnpm run wuxia:verify` 覆盖开局、键盘选择、刷新续卷、抽屉、12 回合、同行者加入、资源边界、结局、390px 溢出、控制台/请求错误与六张截图；同设置、同种子、同 12 个选择的完整重放结果必须逐项一致。
- 设计与扩展协议记录在 `docs/WUXIA_GENERATOR_DESIGN.md`。后续可继续增加出身专属开场、同伴个人线、阵营声望和事件 schema/可达性单元测试。
- 最终验证：武侠目标 ESLint、`node --check scripts/verify-wuxia.cjs`、system Chrome 完整验收、六张截图人工检查与 `git diff --check` 通过。`pnpm run check` 的全仓 ESLint 无警告/错误，TypeScript 与 `pnpm run build` 均只被并行自走棋改动中的既有 `RiftLineScene.ts:437 GameObject.setScale` 类型错误阻断；生产构建在类型阶段前已编译成功，本轮未改动该自走棋文件。

## 2026-08-30 · 自走棋 v0.4.2 竖屏战场重排

- 当前任务：复查久未更新的自走棋，修复、优化并整理需要更新或重构的部分。
- 手机备战继续使用 `480×1000` 逻辑画布，战斗改用 `480×900` 专属竖屏画布和 `448×680` 战场框；竖屏平板也使用同一套比例，不再把 `1120×720` 横向战场缩成窄条。
- 战斗缩放与平移从 Phaser 主相机下沉到战场、棋子和特效三层；计时器、横幅、统计、Toast 与 DOM 工具保持固定，放大和拖动不会再把 HUD 移出可视区域。
- 棋子和点状特效反向抵消非等比拉伸；多端点技能特效与投射物方向按展示比例投影，光束、牵引线、尾迹和目标保持一致。
- React、CSS 与 Phaser 统一使用最大宽度 `1200px`、最小高宽比 `1.12` 的竖屏阈值；移动统计面板改为宽屏触控布局，标签按真实宽度累计排布。
- 快速结算与统计按钮补齐稳定的辅助功能名称、快捷键和展开状态；system Chrome 主流程增加移动统计、`1.18x` 缩放、拖动、复位及 `820×1180` 平板验证，并使用临时 Chrome profile 与截图重试清理。
- 最终门禁通过：目标 ESLint、渲染专项 `41/41`、完整自走棋回归 `415/415`、验证脚本语法、`pnpm run check`、`pnpm run build` 与 `pnpm autochess:release:check` 均成功；构建仅保留浏览器兼容数据过期提示。
- system Chrome 最终主流程确认移动战斗 `480×900`、备战 `480×1000`、缩放 `1.00 → 1.18 → 1.00`、拖动中心 `240.0,450.0 → 203.4,450.0` 后复位；`820×1180` 平板横向溢出为 `0`。AI 接口与更新日志专项也通过，控制台错误和失败请求均为 `0`。
- 本轮最终生成的主流程 `22` 张、更新日志 `4` 张和 AI 验收 `5` 张截图已逐张打开检查，未发现黑屏、透明画布、HUD 漂移、文字重叠或响应式回归。
- 后续真机验收可补充双指缩放和刘海安全区；自动化当前已覆盖缩放按钮、鼠标/单指拖动、复位以及桌面/手机/平板/全屏布局。

## 2026-08-30 · 自走棋 v0.5.0 战术复盘与平衡审计

- 结算新增基于真实战斗统计的战术复盘：胜局聚合相同棋种并标出关键输出或支援；败局按优先级识别少上人口、战斗超时、零激活羁绊、全面压制，以及敌方前后排最高输出威胁。
- 桌面 Phaser 结算和移动 React 结算共用同一纯数据生成器；`render_game_to_text()` 在 `battle.debrief` 与 `result.debrief` 暴露一致的结构化结果。桌面排行改为每侧五行并保留独立滚动，移动长阵容可滚动到最后一名。
- 波次基准新增每个角色的阵容覆盖、包含 / 排除胜率、边际胜率和可用阵容率。第 2--5 战中贪吃岁、老弥和兔子射手持续正边际，雅吨、浣熊店员和绒绒的狗持续负边际；但固定一星简化阵容在第 6 战后几乎全灭，无法控制辅助定位、升星、经济与完整羁绊，因此本版不直接修改角色数值。
- 现代托管基线仍保持正常：平衡风格 16 种子平均终止于第 `19.94` 战，搏上限平均终止于第 `27.06` 战。旧 `simulate-autochess-runs.mjs` 只到第 8--9 战，确认属于过时简化评测器，不作为调平衡依据。
- 最终门禁通过：战术复盘单测 `5/5`、渲染专项 `41/41`、完整自走棋回归 `420/420`、目标 ESLint、验证脚本语法、`pnpm autochess:release:check`、`pnpm run check` 与 `pnpm run build` 均成功；构建仅保留浏览器兼容数据过期提示。
- system Chrome 主流程、结算滚动、更新日志与 AI 接口专项全部通过，控制台错误和失败请求均为 `0`。主流程同时覆盖胜局 `standout` 和败局 `population`，移动复盘横向溢出为 `0px`；十人移动排行可滚动至 `264/264`，第 10 名完整显示且不与继续按钮重叠；更新日志按倒序展示 `0.5.0` 至 `0.1.0` 共 `10` 个版本。
- 本轮最终生成的主流程 `22` 张、结算滚动 `6` 张、更新日志 `4` 张和 AI 验收 `5` 张截图均通过非黑屏检查并逐张打开目检，未发现透明画面、HUD 漂移、文字重叠或响应式回归。
- 后续平衡工作应基于完整托管对局记录做同费用、同星级和同定位的配对替换实验，优先复核雅吨、大黑鼠、恬豆、又一等低频角色；真机仍可补充双指缩放和刘海安全区验收。

## 2026-08-30 · 自走棋 v0.5.1 又一升星修复

- 又一的「叛逆转场」补齐技能升星成长：一星保持每脚 `0.78` 倍伤害与 `0.45` 秒眩晕，二星提高到每脚 `0.96` 倍与 `0.70` 秒，三星提高到每脚 `1.16` 倍与 `1.00` 秒；三档继续使用相同的 `0.52` 秒跃击过程，不改变突进手感与落点时机。
- 新增同费用、同星级、同攻击类型、同定位、同施法时机和同羁绊层级的配对替换基准；背景阵容、敌方波次和战斗种子保持一致，避免把来牌、站位或随机性差异误判为单个棋子的强弱。
- 固定第 `6 / 8` 战、每对 `40` 个匹配阵容、每个阵容 `7` 个战斗种子的 `4228` 次观测中，一星结果保持完全一致；二星平均余量差由 `-0.29` 改善到 `-0.24`，三星由 `-0.43` 改善到 `-0.35`。总体余量差由 `-0.2002` 改善到 `-0.1574`，总体配对胜率差约为 `-0.09%`，没有出现总体胜率膨胀。
- 保留当前保守数值：二星胜率差仍约 `-3%`，三星在所选后期阵容中双方都全胜，单一替换基准不足以支持继续大幅加伤；后续应结合真实托管选取率、升星率与完整对局胜率继续观察。
- 新增 `pnpm autochess:benchmark:replacements` 与 `pnpm autochess:verify:youyi`。又一专项 `1/1`、战斗专项 `136/136`、AI 控制 `147/147`、目标 ESLint、脚本语法、发布一致性、根 system Chrome 主流程、AI 接口、更新日志和又一浏览器专项均已通过；浏览器实测三档总伤害为 `55.61 / 114.99 / 233.24`，落地后的剩余眩晕为 `0.433 / 0.683 / 0.983` 秒，控制台错误和失败请求均为空。
- 本轮又一专项 `6` 张、根主流程 `22` 张、AI 验收 `5` 张和更新日志 `4` 张截图全部通过非黑屏检查并逐张打开目检，未发现透明画面、HUD 漂移、文字重叠或响应式回归；诊断用白屏截图不作为测试证据。
- 基准报告：`artifacts/autochess-v050-youyi-replacement-baseline.json`、`artifacts/autochess-v051-youyi-replacement-after.json`。
- 最终门禁：完整自走棋回归 `420/420`、`pnpm run check`、`pnpm run build`、`pnpm autochess:release:check` 与 `git diff --check` 全部通过；Next.js 构建保留的输出仅为 Browserslist / Baseline 数据过期提示。

## 2026-08-30 · 自走棋 v0.5.2 恬豆糖果拾取修复

- 同费用、同星级、同攻击类型、同定位、同施法时机和同羁绊层级的配对替换审计中，恬豆基线 `2320` 次观测的胜率差为 `-3.84%`、平均余量差为 `-0.2361`；场均治疗仅 `49.13`，每场结束平均仍残留 `2.11` 颗棒棒糖。
- 根因不是技能数值不足，而是落地糖果只能精确碰撞拾取；多个单位同时靠近时还会按内部单位 ID 而非距离选择触发者。地面糖果现增加 `28px` 拾取容差，并改为距离最近者优先，范围外单位仍不能隔空触发。
- 调整后同口径胜率差收窄到 `-2.37%`、平均余量差收窄到 `-0.1413`；场均治疗提高到 `83.75`，残留糖果降到 `0.54`。二星约持平，第 8 战没有出现明显超模。曾实验 `25` 点起始能量，但胜率差退化到 `-3.40%`，因此未保留。
- 配对替换基准新增候选棋子的伤害、治疗、护盾、承伤和未触发地面效果统计；战斗回归新增拾取容差内成功与容差外不触发覆盖。基准报告为 `artifacts/autochess-v051-tiandou-replacement-audit.json` 与 `artifacts/autochess-v052-tiandou-pickup-after.json`。
- 新增 `pnpm autochess:verify:tiandou` system Chrome 专项，公开文本状态确认五颗糖从空中到落地，友军与糖果实体边缘相隔 `20px` 时实际治疗 `34.16`、获得 `+16` 移速，地面糖果由 `5` 降至 `4`；控制台错误和失败请求均为空。
- 目标 ESLint、AI 控制 `147/147`、发布一致性、根 system Chrome 主流程、AI 接口和更新日志专项均已通过。恬豆专项 `3` 张、根主流程 `22` 张、AI 验收 `5` 张和更新日志 `4` 张截图全部通过非黑屏检查并逐张打开目检，未发现透明画布、HUD 漂移、文字重叠或响应式回归。
- 最终门禁：完整自走棋回归 `420/420`、`pnpm run check`、`pnpm run build`、`pnpm autochess:release:check` 与 `git diff --check` 全部通过；生产构建仅保留既有浏览器兼容数据过期提示。

## 2026-08-30 · 自走棋 v0.6.0 推荐站位

- 雅吨 / 大黑鼠完成 `2800` 次同定位配对替换审计：雅吨胜率差 `+0.43%`、平均余量差 `+0.0471`，两者跨回合互有胜负；相同施法时机口径没有可比较同费样本，因此不做缺乏证据的数值调整。
- 站位体验审计确认后期最多十人仍主要依赖逐个拖动，而 AI 已有经过完整回归的稳定规范阵型；本轮将其下沉到 core 并提供一次性玩家操作。
- 推荐站位现只重排 `state.board`，保持 UID、星级、场上人数、备战席和选中棋子；桌面、移动端、`A` 快捷键及文字状态已经接入。
- 新增 3 条站位专项，覆盖混合近远程、重复单位、乱序棋盘、重复调用、备战席隔离、选择态迁移和战斗期禁用；站位 + Go 策略回归 `41/41`、目标 ESLint 与脚本语法检查通过。
- 修复 `.rift-action` 及移动结算、分页、抽屉和会话控件中无效的字体 shorthand，避免按钮回退到浏览器默认字号；同时收紧桌面商店操作区，`1440x900` 下商店面板底部为 `885.9px`、说明区底部为 `872.4px`，不再超出视口。浏览器计算样式确认移动分页与抽屉关闭按钮均为预期 `11px / 700`，抽屉横向溢出为 `0`。
- system Chrome 推荐站位专项确认八人乱序阵容归一到格位 `[4,9,10,11,15,16,17,23]`，选中 UID 在操作前后保持 `60604`；`390x844` 下五个底部操作键均约 `68px`，横向溢出和重叠均为 `0`。根主流程、AI 接口和更新日志专项同样通过，控制台错误与失败请求均为 `0`。
- 自走棋版本升级到 `0.6.0`，同步更新 `package.json`、游戏内 13 个版本的更新日志和根目录 `CHANGELOG.md`；发布一致性与桌面 / 手机滚动验收通过。
- 最终门禁：完整自走棋回归 `423/423`、AI 控制专项 `147/147`、目标 ESLint、脚本语法、发布一致性、`pnpm run check`、`pnpm run build` 与 `git diff --check` 全部通过；生产构建仅保留既有浏览器兼容数据过期提示。
- 推荐站位 `4` 张、更新日志 `4` 张、根主流程 `22` 张和移动商店抽屉 `1` 张截图全部通过纯色 / 透明 / 近黑检查并逐张打开目检，未发现黑屏、遮挡、截断、HUD 漂移或响应式回归。

## 2026-08-30 · 自走棋 v0.6.1 掌上简报

- 新一轮角色、羁绊与阵容覆盖审计确认当前商店池为 `42` 个单位、`18` 个羁绊，费用分布 `7 / 8 / 11 / 10 / 6`，随机六人阵容平均激活 `4.56` 组羁绊；现有证据不足以支持直接调整羁绊或单卡数值。
- 最高价值交互问题是移动商店无法可靠检查棋子：详情只由桌面 hover / focus 打开，触屏点击卡片会直接购买，而买不起的 disabled 卡片无法聚焦；横屏触控样式还会强制隐藏详情。
- 修复前 system Chrome 在 `390x844`、`0` 金币下复现：5 张卡全部 disabled，信息按钮 `0`、可见详情 `0`，触摸卡片后金币和商店均未变化但仍无法查看；横向溢出、控制台错误和失败请求均为 `0`。证据图 `.tmp/autochess/shop-detail-v0.6.0-baseline/mobile-shop-uninspectable.png` 已通过像素检查并人工打开确认。
- `4323` 旧服务虽返回页面 `200`，但 Next 静态资源已 `404/500`；已按仓库约束识别为陈旧监听进程并改用干净的 `4324` 开发服务，不把它误判为游戏运行故障。
- 移动商店现为卡片本体一次点击购买、旁侧独立信息图标查看详情；信息按钮不受金币或储存空间限制。详情在抽屉内就地展开，同一时间只保留一张，切换和关闭不会改变金币、商店、棋盘或备战席。
- 手机详情继续展示生命、攻击、护甲、射程、技能、被动、升星成长与回能规则；桌面继续使用 hover / focus 浮层。横屏触控规则不再隐藏详情，信息按钮和购买卡保持独立焦点与无障碍名称。
- 新增 `pnpm autochess:verify:shop-details`。system Chrome 专项在 `390x844` 手机与 `820x1180` 竖屏平板确认详情可见、抽屉可滚动、横向溢出与控件重叠均为 `0`；0 金币查看“又一”前后状态完全一致，正常购买仍一次完成，金币 `100 -> 97`、持有数 `1 -> 2`。
- 自走棋版本升级到 `0.6.1`，同步更新 `package.json`、游戏内 14 个版本的历史、根 `CHANGELOG.md` 和发布一致性检查。更新日志桌面内容高度 `4602px`、移动端 `6013px`，两端都能滚到 `v0.1.0` 且无横向溢出。
- 最终门禁通过：完整自走棋回归 `424/424`、新增商店源码专项 `42/42`、目标 ESLint、脚本语法、发布一致性、`git diff --check`、`pnpm run check` 与 `pnpm run build` 全部成功；构建只保留既有 Browserslist / Baseline 数据过期提示。
- 根 system Chrome 主流程、AI 契约、更新日志和商店详情专项均通过，控制台错误与失败请求全部为 `0`。最终商店 `4` 张、根主流程 `22` 张、AI `5` 张和更新日志 `4` 张截图全部通过像素检查并逐张人工目检，未发现黑屏、透明画布、遮挡、文字截断、HUD 漂移或响应式回归。

## 2026-08-30 · 自走棋 v0.7.0 远征收官

- 流程审计确认游戏对外承诺 16 战短局，但第 16 战胜利原本只能继续到第 17 战；胜利总报告的 `endGame(true)` 没有玩家可达路径，正常游玩只能在失败后结束。
- 第 16 战胜利结算新增“完成远征”与“继续无限”两条明确路径。`Enter` 默认完成远征并进入既有胜利档案；继续无限保留旧 `resultContinue` 语义和托管长局行为，在进入前明确提示第 17 战。
- 引擎新增受阶段保护的 `canFinishCampaign` / `finishCampaign`，桥接和 AI 控制新增 `finishCampaign()` / `continueEndless()`；错误阶段调用会拒绝执行。文字状态新增 `campaignVictoryPending`，完成远征或进入无限后 `campaignCleared` 均为真。
- Phaser 桌面结算与移动 DOM 结算共用同一状态条件：桌面双按钮均为 `220x42`，手机双按钮均为 `177x44`，没有重叠或横向溢出；完成远征后的桌面和手机胜利档案均完整显示最终阵容、羁绊与三类场均排行。
- 新增 `pnpm autochess:verify:campaign-choice` system Chrome 专项，覆盖桌面 `Enter`、移动点击、胜利档案、继续无限、按钮尺寸、横向溢出、文本状态、控制台与失败请求。验收时识别并避开无响应的陈旧 `4324` 监听，最终在干净 `4325` 服务完成。
- 更新日志浏览器专项移除手工维护的版本数组，改为直接读取 `AUTOCHESS_RELEASE_HISTORY`；`v0.7.0` 发布后不再因遗漏同步版本数量而误报失败。
- 最终门禁通过：完整自走棋回归 `426/426`、更新日志专项 `1/1`、脚本语法、发布一致性、`pnpm run check` 与 `pnpm run build` 均成功；构建只保留既有 Browserslist / Baseline 数据过期提示。
- 根 system Chrome 主流程、AI 契约、更新日志与远征收官专项全部通过，控制台错误和失败请求均为 `0`。最终远征 `4` 张、根主流程 `22` 张、AI `5` 张和更新日志 `4` 张截图全部通过纯色、透明与近黑检查并逐张人工目检，未发现黑屏、遮挡、文字截断、HUD 漂移或响应式回归。

## 2026-08-30 · 自走棋 v0.7.1 同局重演

- 可复现性复查确认普通无参数重开原本就会生成新种子，但通过 `?seed=` 进入的固定对局在重开时也会被 `resetToTitle()` 换成随机种子，违背了版本历史中稳定重开与复盘的承诺。
- `resetToTitle(seed?)` 现支持显式覆盖；`EngineBridge` 只在页面明确提供 `restartSeed` 时复用它。测试和托管仅用数字种子构造桥接器时仍保持原来的普通重开语义，不会被误判为固定浏览器会话。
- 页面会把合法查询种子同时用于首次创建和后续重开。最终 system Chrome 专项在受控熵下确认固定模式 `907101 -> 907101` 且开局协议完全一致，无参数模式 `2788712957 -> 2788712958`；画布为 `1440x853`，文字状态、DOM、控制台和请求记录均正常。
- 站位基准的 `--compare-only` 现在保留四种候选阵型的完整逐战结果，不再只序列化赢家。同一十人二星阵容在第 `16 / 20 / 24 / 32` 战各四个种子中，真人记录阵为 `11/16`、分翼阵为 `10/16`、中锋与中线阵均为 `9/16`；第 32 战只有中锋阵取得 `1/4`，没有单一阵型跨波次稳定占优，因此本版不把可预测的一键推荐改成敌方感知自动换阵。
- 基准报告为 `artifacts/autochess-v071-formation-audit.json`；新增 `pnpm autochess:verify:seed-restart`，并将版本升级到 `0.7.1`，同步更新游戏内 `16` 个版本、根 `CHANGELOG.md` 与发布一致性元数据。
- 最终门禁通过：AI 控制专项 `149/149`、完整自走棋回归 `427/427`、目标 ESLint、脚本语法、发布一致性、`pnpm run check` 与 `pnpm run build` 全部成功；构建只保留既有 Browserslist / Baseline 数据过期提示。
- 生产构建后识别到旧 `4325` 监听仍返回 HTML 但无法挂载游戏画布，按仓库约束将其判定为陈旧服务；最终在干净的 `4326` 开发服务复核。种子专项 `2` 张和更新日志 `4` 张截图全部通过纯色、透明与近黑检查并逐张人工目检；更新日志桌面内容高度 `5203px`、手机内容高度 `6915px`，两端都能从 `v0.7.1` 滚到 `v0.1.0`，横向溢出、控制台错误与失败请求均为 `0`。

## 2026-08-30 · 武侠 2K 可读性与回合结算

- 用户反馈 27 寸 2K 屏上的开局说明、标签和选项文字过小；统一建立 `11--17px` 的 UI 字号层级，2K 实测字段标签 `13px`、选项标题 `16px`、说明 `14px`、功能与页脚说明 `13px`，并加宽开局设定栏与桌面游戏侧栏。移动端去除原有 `8px` 文字，同时保持正文优先和无横向溢出。
- 选择后不再立即把结果压进上文并展示下一事件。引擎新增可存档的 `pendingOutcome` 阶段，记录选择、公开检定点数、成败、结果段落和实际资源变化；玩家点击“继续下一回”或按回车/空格后，才由 `continueNovelAction` 抽取下一事件，最终回也会先结算再进入结局。
- `window.render_game_to_text()` 新增 `outcome` 屏幕与结构化结算数据。系统 Chrome 验收覆盖 2K 字号门槛、结算刷新恢复、下一选项隔离、键盘继续、12 回合确定性重放、最终回结算和 390px 移动结算；九张截图通过像素检查并逐张目检，控制台错误与失败请求为空。
- 为避免同仓库另一个 `4326` 开发任务争用 `.next`，当前 `3806` 服务使用独立的 `.next-wuxia-3806` 目录。通用 web-game 动作客户端也通过，文本状态与截图正常且没有错误日志。
- 最终门禁通过：目标文件 ESLint、验证脚本语法、`pnpm run wuxia:verify`、`pnpm run check`、隔离目录的 `pnpm run build` 与 `git diff --check` 均成功；生产构建仅保留既有 Browserslist / Baseline 数据过期提示。

## 2026-08-30 · 武侠世界模拟、关系图与逐招战斗

- 参考《太吾绘卷》《鬼谷八荒》《了不起的修仙模拟器》《大侠立志传》和《漫野奇谭》的官方说明，将方向收敛为“世界先模拟、导演挑选合理戏剧、结果再转写成小说”，没有把 12 回短局堆成经营沙盒。
- 存档升级为 `NovelState.version = 3` 与 `wuxia-novel-save-v3`；新增 10 个地点、16 条双向道路、人物住所/日常路线/目标/停留/记忆。主角和 NPC 每日最多沿一条相邻路径移动，关键人物距离过远时事件会合理等待。
- 新增事件导演候选评分和相遇记录；基础同地概率之上，关系、共同目标、未揭秘密和伏笔会提高戏剧概率，但不会改变人物实际位置。`render_game_to_text()` 会公开候选理由、人物行程和现场角色。
- 新增有方向和强度的关系图，覆盖亲属、养亲、舅甥、师徒、同门、宿敌、仇敌、欠命、护持与同行；关系按证据逐步揭示，并参与后续相遇、正文和结局。
- 新增门派武学、招式性质、威力、迅捷、命中、距离、耗气、冷却、难度、标签、克制、熟练度和习得来源；支持参悟/出售/归还残卷、观摩、偷学与实战成长。
- 战斗复用自走棋的确定性随机源和状态演算原则，输出距离、内力、冷却、命中、闪避、格挡、破招、状态和伤害的逐招战报。完整战报用于复盘，正文只选择双方关键攻防，并按 `actorId + techniqueId + result` 去重。
- 四章手稿严格各收三回；每回保存上一回连续性、事件、玩家动作、真实战斗片段、关系/势力/伏笔后果与转场，结局阅读器可按章节阅读和复制整卷。
- 地图、江湖动态、人物行踪、关系网、势力志、武学谱和逐招战斗已经接入桌面/2K/390px 界面。抽屉验证会等待 260ms 动画结束，断言最终背景完全不透明，并使用元素截图避免 `fullPage` 对固定层的合成假象。
- `pnpm run wuxia:verify` 已覆盖 12 回完整流程、同种子重放、合法移动、戏剧概率、关系揭示、秘籍/偷学/熟练度、战斗正文去重和移动端战斗/地图/人物抽屉，并在最终截图方式下连续通过。
- 进入正文时会主动回到页首；配角姓名改为排除主角与核心人物后无放回抽取，测试新增 10 名世界人物全局唯一断言。通用动作客户端在 `1280x720` 真实点击开卷后从首回页首开始，文本状态为第 1 日且无控制台错误。
- 截图目检发现相遇概率曾显示 JavaScript 浮点尾数；世界模拟现将概率统一保留最多两位小数，界面与解释句均显示为 `17.12% -> 32.32%` 一类可读格式，并有回归断言。

### 收尾 TODO

- [x] 修复逐招转写中的同招同结果重复。
- [x] 确认抽屉最终背景不透明，并改用抽屉元素截图。
- [x] 增加 390px 战斗、地图和人物抽屉专项。
- [x] 更新 v3 设计与调研文档。
- [x] 重跑武侠完整流程并逐张检查最终截图。
- [x] 运行全仓 `pnpm run check`、隔离 `pnpm run build` 和 `git diff --check`（通用动作客户端与目标 ESLint 已通过）。

- 最终门禁通过：目标 ESLint、验证脚本语法、两次最终 `pnpm run wuxia:verify`、通用 web-game 动作客户端、`pnpm run check`、隔离目录生产构建和 `git diff --check` 全部成功。生产构建仅保留既有 Browserslist / Baseline 数据过期提示；`3806` 服务复查仍返回 `200`。

## 2026-08-30 · 武侠 v4 无主线人物沙盘

- 用户反馈固定“沉星渡”线路即使拥有世界模拟，仍像换皮主线；本轮将叙事模式改为 `emergent_sandbox`，新局、存档和回归均禁止旧主线事件进入运行状态。
- 新增 `wuxiaRoster.ts`：从自走棋 24 名角色原型复用头像、关系羁绊、技能名与说明，并武侠化为幻真楼（VirtuaReal）、星游社（PSPLive）、四禧庄（四禧丸子）和散游盟。每个种子稳定抽取 8 名活跃人物，同时保证四方各至少一人。
- 八名人物各自拥有起始地点、住所、日常路线、目标、停留、记忆、独门招式和本局隐秘。旧有关系羁绊生成公开旧识，同门生成门派关系，种子另生成手足、仇敌、师徒、欠情、护持、结义和对手等秘密边。
- 地图扩为 12 个地点、22 条双向道路，加入幻真楼、星游别院和四禧庄；修复四禧庄到悬灯诊棚的反向连接，所有人物移动继续逐日沿相邻路段发生。
- 事件导演不再读取主线前置链：每名人物按目标、距离、同地角色、关系压力、近期出场和随机扰动形成候选；秘籍与低气血休整也参与竞争。`selectedCandidateEventId` 让地图抽屉准确解释本回来源。
- 人物事件提供援手/结伴、逐招演武、观察/偷学三类选择；每局三册角色抄本拥有真实地点与来源，首册发现按种子落在第 2--5 回。选择会改变人物关系、位置、记忆、势力态度、招式来源和熟练度，并继续参与后续事件。
- 结局不再揭晓预写案件，而是总结本局实际遇见的人、形成的关系、掌握的招式、门派影响与未完目标。四章仍严格各三回，保留短篇小说的阅读节奏和独立回合结算。
- `NovelState.version` 升为 `4`，存档键改为 `wuxia-novel-save-v4`；设计文档已同步到 v4、12 地点、22 条道路、八人沙盘和四个原型势力。

### 本轮收尾

- [x] 移除运行时固定主线依赖，五个种子验证阵容与首遇均有差异。
- [x] 接入自走棋角色、关系羁绊、头像和技能数据的武侠适配层。
- [x] 覆盖人物移动、复杂关系、援手同行、演武、秘籍、观摩、偷学和熟练度。
- [x] 保持回合结果独立展示、2K 可读字号和桌面/移动响应式布局。
- [x] 更新 v4 设计文档；浏览器动作客户端、全流程回归、静态检查和生产构建结果已记录如下。

- 通用 `develop-web-game` 动作客户端实际完成三轮“开卷 -> A 选择 -> 结果 -> Space 继续”，状态从人物相遇推进到地点抄本，三张截图逐张检查且错误文件为 `0`。
- `pnpm run wuxia:verify` 最终通过：五个种子得到五套阵容和五种首遇；完整 12 回覆盖援手、同行、演武、观察、偷学、秘籍与熟练度，四章为 `3 / 3 / 3 / 3`，同种子重放逐项一致。
- 最终 13 张专项截图已全部打开目检；2560x1440 字段标签 `14px`、选项标题 `18px`、说明 `15px`、动态文字 `14px`，桌面、2K 和 390px 均无横向溢出或遮挡，控制台错误与失败请求为空。
- 最终门禁通过：目标 TS/TSX ESLint、验证脚本语法、`pnpm run check`、隔离 `.next-wuxia-build-final` 的 `pnpm run build` 与 `git diff --check`。构建只保留既有 Browserslist / Baseline 数据过期提示，`http://127.0.0.1:3806/game/wuxia` 复查返回 `200`。

## 2026-08-30 · 武侠 v5 多原型事件导演

- 用户试玩发现 v4 虽然会随机人物和地点，但事件结构始终是“到一处遇一人，再从援手、演武、偷学里选一个”，随机性停留在名词替换，没有形成不同的江湖处境。
- 存档升级为 `NovelState.version = 5` 与 `wuxia-novel-save-v5`，旧固定相遇模板不会从 v4 存档继续进入新流程。
- 人物目标现生成追踪、保护、挑战、联络四类事件；两名有关联的人同场时生成恩仇、欠情、师承、亲友四类关系事件。每类都有独立的开场事实、选择语法和后果写回。
- 新增市井假令、荒路踪迹、诊棚求药、门派争议四类纯地点事件，以及前回余波、秘籍流转和休整候选；地点事件不要求先遇到 NPC。
- 最近四回相同原型强降权；连续两回人物事件后，地点或余波显著升权。首次约战、秘籍和余波拥有逐回增长的兜底压力，既能看到核心系统，也不固定成预写主线。
- 支援、调查、应战、求教、调停、传信、地点处置和余波应对会分别改变关系、势力、同行、传闻、人物目标与武学来源。小说正文不显示内部概率、点数、秒数或技能规则文本。

### 收尾 TODO

- [x] v5 事件导演、关系/地点事件与后果写回落盘。
- [x] 24 名角色补齐手写武侠招式描写，小说战斗改为文学化转写。
- [x] 验证脚本覆盖多原型、多行为、地点、余波、关系、秘籍和逐招战斗。
- [x] 重跑 system Chrome 完整流程并逐张检查最终截图。
- [x] 运行通用动作客户端、全仓检查、隔离生产构建和服务复查。

- `pnpm run wuxia:verify` 最终十二回覆盖 `kinship / liaison / manual / tutelage / challenge / place-trail / fallout / track / place-market`；选择覆盖支援、调查、秘籍、习武、逐招实战、地点处置与余波，没有生成旧 `sandbox-aid / sandbox-duel / sandbox-observe` 三选一。
- 五个固定种子的首回覆盖欠情、亲友和联络三种结构；完整流程经过至少 3 名角色、4 个地点，揭开亲属/仇敌/师徒关系，四章严格为 `3 / 3 / 3 / 3`，同种子同选择完整重放一致。
- 通用 `develop-web-game` 动作客户端连续完成三轮选择与继续，实际场景依次为欠情旧事、市井假令和地点秘籍；三张截图逐张检查，文本状态逐回推进且错误文件为 `0`。
- system Chrome 的 13 张最终截图已逐张打开检查；秘籍和战斗结算在等待入场动画后亮度正常，1440px、2560px 与 390px 未见溢出、遮挡或文字截断，控制台错误与失败请求为空。
- 最终门禁通过：目标 TS/TSX ESLint、验证脚本语法、`pnpm run wuxia:verify`、`pnpm run check`、隔离 `.next-wuxia-build-v5` 的 `pnpm run build` 与 `git diff --check`。生产构建只保留既有 Browserslist / Baseline 数据过期提示，`http://127.0.0.1:3806/game/wuxia` 返回 `200`。

## 2026-08-30 · 武侠姓名随机与命数解耦

- 根因是姓名按钮把剧情种子直接传给 `generateName(seed)`，同一种子必然只得到同一个姓名；命数随机按钮又同时改写姓名，使两个本应独立的设置互相绑死。
- 开局挂载后会生成一次独立随机姓名；姓名按钮改用即时随机熵，并显式排除当前姓名，因此连续点击不会停在同一个结果。
- 命数随机按钮现在只重抽种子与活跃江湖人物，不再修改玩家已经选定或输入的姓名；固定剧情种子也不会限制姓名随机。
- system Chrome 专项连续换名得到 `叶长歌 -> 陆惊鸿 -> 楚问棠 -> 陆惊鸿 -> 叶问棠 -> 沈问棠 -> 裴照野`，相邻结果均不同且剧情种子始终不变；固定种子后仍换到 `苏临川`，随后随机剧情种子，姓名继续保持 `苏临川`。
- 通用 `develop-web-game` 动作客户端真实点击姓名按钮，截图显示随机姓名正常排版，`render_game_to_text()` 保持 `setup`，错误文件为 `0`。
- 最终门禁通过：目标 ESLint、验证脚本语法、完整 `pnpm run wuxia:verify`、通用动作客户端、`pnpm run check`、隔离 `.next-wuxia-build-name` 的生产构建与 `git diff --check`；`http://127.0.0.1:3806/game/wuxia` 返回 `200`。

## 2026-08-30 · 武侠 v6 玩家主动章节循环接线

- 姓名专项再次在 `3806` 的 system Chrome 上验证：剧情种子保持不变时连续七次随机的相邻姓名均不同；固定种子后仍能继续换名，单独随机剧情种子也不会改写姓名。`WUXIA_NAME_ONLY=1` 聚焦模式已加入 `scripts/verify-wuxia.cjs`。
- 完成 v6 现有界面接线：`choose_agenda -> planning -> scene -> outcome -> chapter_break` 分别展示路线、主动活动、场景选择、独立新正文和章末小结；三幕后进入章末，开启下一章后回到主动规划，不再引用固定总回合数。
- 顶栏改为本章幕数；设置抽屉把“暂结此卷，生成收束”和“舍弃存档，重新开局”拆开。键盘数字键可选择路线、活动或场景，回车/空格可继续结果及章末。
- `render_game_to_text()` 已公开 campaign phase、路线、活动、人物意图、机会、门派辨识、传承、章末里程碑与内容包注册信息，并移除 `maxTurns`。
- 新路线页、计划页、人物意图、活动列表和章末页补齐桌面与 390px 样式。system Chrome 短流程从第一章走到第二章，八张桌面/移动截图通过非黑与横向溢出检查并逐张目检；控制台错误和失败请求均为空。
- 通用 `develop-web-game` 客户端真实点击姓名随机按钮，`render_game_to_text()` 保持 setup，截图正常且错误文件为 `0`。目标 ESLint、TypeScript、全仓 `pnpm run check`、隔离 `.next-wuxia-build-current` 的生产构建与 `git diff --check` 已通过；Next 自动写入的临时类型目录已从 `tsconfig.json` 恢复。

### 后续 TODO

- [x] 将 `scripts/verify-wuxia.cjs` 余下的 v5 固定十二回长流程全面改写为 v6 主动活动流程，覆盖限时大会、追人、战斗门派辨识、自创武学、创派与第十三回以后继续推进。
- [x] 更新 v6 设计文档最终状态并完成最终长流程截图验收。

## 2026-08-30 · 武侠 v6 世界状态守恒修复

- 限时机会活动区分“提前抵达准备”与“正式参加”：早到时可按真实脚程先到当地、查规矩、结识早到者或帮组织者做事；正式参加后标记 `attended`，不再重复进入活动列表。
- 修正机会线索结算顺序：提前到场只推进并激活线索，正式参加才将同一机会线索结清；不会再被通用 `leadId` 分支提前截断。
- 外派战斗辨招新增可序列化交手账，记录对手、招式、切磋 / 公开比试 / 敌对冲突、胜负及实际门派往来与戒心变化；敌对清算不再错误提高对方门派好感。
- 传艺馆新增学徒会从真实已相识人物中选取并写入 `followerActorIds`，后续重新统计不会丢失；结算新增追随者与势力态度变化。
- 行程页新增武学领悟、名望、追随者和自创武学概览；势力志新增辨招证据与最近交手后果，武学谱新增自创传承和真实追随者名册。
- 引擎/浏览器长流程、示例内容包、v6 设计文档和最终门禁均已补齐，具体结果见下节。

## 2026-08-30 · 武侠 v6 长流程与沉浸式收尾

- 修正完整验收的旧假设：满血时不再强求不存在的养伤活动；积累公开名望会依次选择休整听风、盛事筹备 / 会面或沿路替当地人办事，仍硬性要求八幕内解锁创招。
- system Chrome 完整主动生涯走到第 22 幕、第 8 章、第 27 日：提前抵达并正式参加比武招亲，形成异派辨招与交手账，自创“截流一式”，让四名真实人物写入追随者名册，并在听雨渡创立“听雨行止门”。第十三幕后仍可无限开启新章。
- 目检发现并修正自动断言未覆盖的沉浸问题：交手实录保留底层确定性演算，但页面不再显示气力分数、伤害点数、冷却等调试式数值；结果变化、关系牵系、辨招把握与势力态度改为定性表达。
- 武学谱不再显示 `huanzhen`、`home`、`player_sect_*` 等内部势力 ID，统一映射为幻真楼、青云门和玩家自立门派等世界内名称；专门的武学谱仍保留玩家主动查看的招式属性与修习数据。
- 完整 `pnpm run wuxia:verify` 通过，武侠引擎测试 `10/10`；13 张 2560px / 桌面 / 390px 长流程截图通过像素检查并逐张目检，控制台错误与失败请求为空。
- 通用 `develop-web-game` 客户端真实走完两幕“选路线 -> 排活动 -> 作选择 -> 看结果 -> 继续”，最终为第 2 幕后的规划页，`render_game_to_text()` 与画面一致且没有错误日志。

### 最终待办

- [x] 对照完整用户目标逐项审计实现与直接证据。
- [x] 顺序运行全仓 `pnpm run check`、隔离目录生产构建与 `git diff --check`，复查 3806 服务。

- 最终审计确认路线与目标、人物意图、追人、限时盛事、真实移动、复杂关系、秘籍与成长、异派辨招、无限章节、自创、传艺、创派和内容包均有源码状态模型与直接测试证据，不存在只声明接口而未接入的项目。
- 最终全量门禁再次通过：`pnpm run check`、`pnpm run wuxia:verify`（`10/10` 单测与第 22 幕系统 Chrome 长流程）、隔离 `.next-wuxia-build-v6-final` 的 `pnpm run build` 和 `git diff --check`；13 张新截图逐张目检，`http://127.0.0.1:3806/game/wuxia` 返回 `200`。
- 全量检查额外发现并修正三处类型契约：内容包嵌套规则可真正局部覆盖，盛事人物先排除空值再按脚程选择，自创武学保持 `WorldMartialArt` 字面量类型；均未改变既有玩法结果。

## 2026-08-31 · 武侠双版本与真正旧版恢复

- 用户确认“旧版”指提交 `9ae2e90` 的改版前自动演义，不是后来那套十二回华丽短篇。版本选择现明确区分“简陋测试版”和“开放江湖”。
- 将 `9ae2e90:src/components/wuxia` 的旧世界、事件片段、同行与战斗逻辑完整复制到独立的 `src/components/wuxia/legacy`，当前 v6 沙盘继续使用原目录，后续互不污染。
- `LegacyWuxiaGame.tsx` 恢复为原始黑底长卷、自动滚动、编号选择、“开始江湖演义”和“—— 全书完 ——”；只新增 `render_game_to_text` / `advanceTime` 测试接口，并为原组件在当前全局主题下发黑的选项文字补上可读颜色。
- 简陋测试版沿用原行为，每次从头演义且不伪造存档；开放江湖继续读取 `wuxia-novel-save-v6`。切换版本会卸载当前界面，两套状态不会串写。
- 新专项单测直接验证偶遇结伴入队、山贼战中同行者进入战场并实际夹击、最终绝学分支写到《完》；`pnpm wuxia:test` 当前 `11/11` 通过。
- system Chrome 双版本流程从桌面选择页进入旧版，13 次选择走到全书完，再切到开放版确认 v6 与独立存档，最后回到 390px 选择页；六张最终截图通过像素检查并逐张目检，控制台错误、失败请求和横向溢出均为 0。
- 通用 `develop-web-game` 客户端进入简陋测试版，截图和 `render_game_to_text` 均确认是原始开场，错误文件为 0。
- 最终门禁通过：目标源码 ESLint、`pnpm wuxia:test`、system Chrome 双版本长流程、通用动作客户端、`pnpm run check`、隔离 `.next-wuxia-build-editions` 的 `pnpm run build` 与 `git diff --check`；旧版 25 个隔离文件的 Git blob 哈希逐个匹配 `9ae2e90`，`http://127.0.0.1:3806/game/wuxia` 返回 `200`。

## 2026-08-31 · 开放江湖 v7 浏览器验收接线

- `scripts/verify-wuxia.cjs` 的共享状态断言升级到 v7，校验世界 / 主角 ID、年月日、年龄、家门、天下大事、排名、可选结局与历代人物；刷新续卷改由江湖册进入，同时保留原 `WUXIA_V6_SMOKE` 主动章节流程。
- 新增 `WUXIA_V7_LIFE_SMOKE=1` 聚焦流程：显式清理 v6 / v7 存档、创建世界、主动收年、确认年龄与 `year_break`、走三幕选择尾声、继续游历并返回江湖册。
- `WUXIA_V7_SUCCESSION=1` 在同一短流程后继续覆盖同世界继任：第二代获得新人生 ID，上一代写入档案并作为非 `hero` 人物留在世界；最终再次核对江湖册。脚本已通过 `node --check` 与 `git diff --check`，待最新开发服务启动后运行浏览器流程并目检截图。

## 2026-08-31 · 开放江湖 v7 生涯、结局与跨代收尾

- 完成年月日与缓慢增岁：玩家可主动“今年就这样吧”，长局达到年度幕数也会自动进入岁末；人物移动、盛事与天下大事按同一绝对日历继续运转。
- 家门支持结义、成婚、纳妾、亲生与收养子女；子女按出生年月成长，成年后拥有真实人物档案、路线与可追寻线索。结局可撤回继续，也可同世界另启一生或创建独立世界。
- 华山论剑、天下第一武道会、抵御外族、追剿魔头与挑战宗师均有跨年状态、贡献和结局；赛事逐轮结算，错过后仍会按周期重开，跨代记录保留原参赛人生归属。
- v7 江湖册支持最多 8 方独立世界、v6 迁移、坏槽隔离和长局限长。回归补齐后发现并修复活跃线索在存档裁剪时被已结束线索挤掉的问题。
- 继任时上一代成为可拜访的 `legend` 人物，旧关系、门派、项目、武学、排名与历代档案均留在同一世界；连续三代仍只有一个当前主角，ID 不冲突。
- 移动端“切换版本”由浮层改为顶部行，避免遮挡路线卡和江湖册文字。2K、1440 桌面、390×844 与 412×915 的生命周期 / 继任截图均通过像素检查和逐张目检。
- 最终验证通过：`pnpm run wuxia:verify`（34/34 逻辑测试、旧版 13 次选择到全书完、开放江湖 24 幕跨年长局）、v7 主动收年 / 结局 / 继任专项、通用 web-game 动作客户端、`pnpm run check`、`pnpm run build` 与 `git diff --check`。生产构建只保留既有 Browserslist 数据过期提示。
