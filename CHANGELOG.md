# 更新日志

本文件记录「裂隙阵线」自走棋对玩家可见的版本变化。版本规则与维护流程见
[`docs/autochess-release-guide.md`](docs/autochess-release-guide.md)。

## [0.2.3] - 2026-08-06

### AI 与测试

- 单场与整局结构化战斗日志容量提高到 `100,000` 条，`autoChessAI.logs()` 可以跨关卡读取，并为每条事件补充所属关卡。
- 新增 `autoChessAI.battles()`；每战按关卡保存双方开战站位、星级、战斗属性、完整事件和结算，不需要记录战斗中的每次移动。
- 操作轨迹容量从 `640` 提高到 `10,000` 条；`window.autoChessLastRun` 同时公开战斗档案、实际记录数、丢弃数和容量。
- 长局轨迹超出 `sessionStorage` 配额时会存入 IndexedDB，同标签页刷新后仍可恢复；新增跨两战超过 1,400 条事件与 1,200 条操作的回归测试。
- 托管以真人第 54 战的中线站位作为初始种子，不再把所有远程棋放在最远两列；每轮保留上一代冠军，由冠军产生换人和站位变异，只有真实战斗分更高的后代才接班并传入下一轮。

## [0.2.2] - 2026-08-06

### 调整

- AI 托管会用完整托管和真实战斗引擎预演每个候选协议的前三战；只有胜场严格更多时才推翻长期偏好，胜场相同时继续使用经过固定基准验证的偏好。
- 阵容从启发式候选中最多选四组进行真实战斗浅层推演，天赋也按下一战结果选择，让棋子自身数值、技能、功能性、星级和站位参与决策。
- 经济决策加入谨慎的利息门控：低血量、缺人口和立即合成时继续投入；至少两连胜且推演大幅领先时，可省下跨利息档的付费刷新，或在满候选席且正好差一金币时回收一张非目标一费单卡。

### 修复

- 棋盘和候选席都满时，目标候选席棋子会与场上非目标棋直接交换，不再因重复非法上阵保持选中直到备战超时。
- 满候选席换购只牺牲目标阵容外的一星单位，并保护推演选中的单位、升级素材和二三星单位。

### AI 与测试

- `autoChessAI.actions()`、`render_game_to_text().recentActions` 和 console action 日志记录每次操作前后的金币、等级、利息、阵容、商店与羁绊；`window.autoChessLastRun` 在终局、组件卸载和同标签页刷新后保留上一局轨迹。
- 固定 `73000-73023` 的 24 局基准中，平均终止战从 `13.42` 提升到 `14.38`，平均胜场从 `10.17` 提升到 `11.33`，主线通关率从 `12.5%` 提升到 `16.7%`；非法移动和选中停滞均为 0。
- 真人演示种子 `73042` 中，旧托管在第 7 战结束；新协议前瞻改选流量续航，主线通关并到第 17 战。新增策略与评测指南 `docs/autochess-autopilot-strategy.md`。

## [0.2.1] - 2026-08-06

### 调整

- 桌面工具栏恢复声音开关和音乐、音效音量快捷调节，版本与更新入口统一移入设置；移动端继续在设置中提供完整音频控制。

### 修复

- 选中棋子后可以直接点击桌面出售区回收，不再要求必须拖动；出售区会预先显示返还金币。
- AI 托管在棋盘和候选席都满时，会出售目标阵容外价值最低的候选席棋子，并立即买入已经选定的更高价值商店棋，不再因无空位停止运营。

### AI 与测试

- 控制台出售回归同时覆盖指定槽位出售和选中后出售；满席托管回归验证完整的“出售后继续购买”流程。

## [0.2.0] - 2026-08-05

### 新增

- 新增游戏内版本入口和更新日志。
- 新增 `window.autoChessAI` 控制台 API，覆盖协议选择、商店购买、刷新、锁店、升本、布阵、出售、开战、天赋选择、回合推进、重开和状态读取。
- 新增结构化战斗日志，记录目标切换、技能释放位置与方向、投射物命中、伤害和阵亡；日志同步进入控制台与 `render_game_to_text()`。
- 新增战斗快速结算操作，键盘和控制台均可触发。
- 新增版本一致性检查命令 `pnpm autochess:release:check`。

### 美术

- 果冻风纪、小红帽、绒绒的狗替换为统一的透明全身战棋形象。
- 首批新形象统一为 `512x512` RGBA、固定安全边距和小尺寸优先轮廓。

### 调整

- 扩充阶段化快捷键，让常用决策无需鼠标即可执行。
- 根据 36 局自动策略试玩和 3,640 组早期阵容波次基准，将前 8 波有效预算从 `2/5/9/18/16/17/21/32` 平滑为 `2/4/7/11/10/13/16/24`，保留第 4、8 波精英峰值，并让普通波留出构筑空间。
- 败局由存活敌人追加的核心伤害上限从 3 降为 2，减少连续两次失利后尚未来得及调整便结束对局的情况。

## [0.1.0] - 2026-07-17

### 新增

- 建立裂隙阵线的短局自走棋循环，包括自由布阵、羁绊、升星、经济、天赋和逐步加压的敌人波次。
- 加入可复现随机种子、确定性时间推进和 `render_game_to_text()` 自动化状态接口。
