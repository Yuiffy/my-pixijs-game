Original prompt: /goal 我们仓库里自走棋游戏demo，非常简陋，基本玩不了（能操作，但是没啥玩头，兵种、技能、平衡、游戏进程，都不太精心调整过，纯demo）。你来检查、完善这个游戏，让它变得是一个不用太完善但能让人总想开几把挑战一下的游戏。

## 目标

- 做成一局约 8—12 分钟、阵容选择清楚、战斗反馈明确、失败后愿意马上重开的轻量自走棋。
- 保留短局节奏，同时提供自由布阵、羁绊、升星、经济、随机强化和逐步加压的敌人波次。

## 已完成

- 2026-07-17：完成旧版审计与基线试玩，定位到固定阵型、内容重复、经济逻辑分散、战斗缺少反馈和本地统计接口报错等问题。
- 2026-07-17：重构为 8 战短局，加入 9 个可购买兵种、首领、7 类双人羁绊、三种开局协议、自动升星、精英/首领波次、8 种三选一战术烙印、利息与连胜经济。
- 2026-07-17：根据用户反馈，把 4×2 固定阵型替换成 6×4 交错战术网格（24 个落点）；备战坐标会映射进战斗，并保留 3→6 的上阵人数成长。
- 2026-07-17：网格同时支持点选换位与鼠标拖放；真实拖放测试已将单位从 (0,0) 移至 (5,3)，状态和画面一致。
- 2026-07-17：重做单画布界面，加入购买、移动、出售、刷新、开战、快捷键、全屏、技能特效、伤害/治疗/护盾反馈、结算和快速重开。
- 2026-07-17：加入可复现随机种子、文本状态接口和确定性时间推进，便于自动化验证多回合流程。
- 2026-07-17：处理护盾无限叠加和首战节奏偏慢问题；限制护盾、调整回能，把战斗上限收紧到 24 秒，并建立前三战教学坡度。
- 2026-07-17：完成两次烙印选择、精英波、失败结算、最高分保存与重开状态清理的长流程验证；控制台无游戏错误。
- 2026-07-17：验证 R 刷新、Space 开战、F 进入全屏、Esc 退出全屏；本地环境不再加载生产广告和统计脚本。
- 2026-07-17：删除已失去引用的旧 Phaser 场景、对象、UI、配置和系统原型；这些删除可通过 Git 历史恢复。
- 2026-07-17：`npm run build`、类型检查、静态生成全部通过。构建只保留仓库其他页面原有的 lint 警告，自走棋页面无新增警告。

## 验证清单

- [x] 完整试玩旧版并记录主要摩擦点。
- [x] 完成兵种、技能、羁绊、经济、强化和局内成长设计。
- [x] 分批实现并用 Playwright 覆盖开局、购买、自由布阵、战斗、胜负、强化、游戏结束与重开。
- [x] 验证真实鼠标拖放、键盘快捷键、全屏进出和本地持久化。
- [x] 通过生产构建和 TypeScript 校验。

## 后续可选

- 继续根据真人试玩数据微调单位数值、商店概率和后四战难度。
- 增加音效、音乐和更丰富的单位美术；当前版本优先保证玩法循环与可读性。

## 2026-07-25 · 天赋徽记栏

- [x] 确认重叠根因：备战页天赋长文本与敌方头像共用 `y=174`。
- [x] 为 12 个局中天赋定义唯一图标，并在天赋选择页、图鉴和备战记录中复用。
- [x] 将备战页长文本改为波次标题旁的紧凑徽记栏；开局天赋使用六边形、小天赋使用圆形、大天赋使用菱形，重复获得合并显示层数。
- [x] 增加天赋徽记 hover / 点击说明，展示名称、类型、定位、获得回合与完整效果；开局天赋也作为首枚徽记展示。
- [x] 敌方羁绊从单段文字拆为独立悬停项，说明面板使用敌方实际人数和档位。
- [x] 完成 TypeScript、专项回归与系统 Chrome 截图验证。
- 系统 Chrome 1440×900 专项验证：开局天赋、局中天赋、重复层数和敌方羁绊三个 hover 流程均通过；天赋栏与敌方头像保留 263 逻辑像素间距。
- 截图黑屏检查、整页截图人工检查、`render_game_to_text`、画布尺寸、控制台错误与失败请求交叉验证通过。
- 验证：`pnpm exec tsc --noEmit`、专项 `34/34`、完整自走棋套件 `146/146`；场景 ESLint 无 error，仅保留既有 `_tree` 未使用 warning。

## 2026-07-25 · 满员时保留商店羁绊高亮

- 用户反馈：场上棋子达到人口上限后，商店不应隐藏“换上该棋子可激活羁绊”的高亮提示。
- 已将羁绊价值提示与当前上阵空位解耦；场上满员但仍可购买时，Phaser 商店和 DOM/移动端商店都会继续显示待激活羁绊高亮。
- 金币和棋子储存空间仍独立控制购买可用性，不改变购买规则。
- 回归覆盖两套商店判定，禁止羁绊预测再次依赖 `boardCount` / `boardCap`，并保留场上同名单位不重复计羁绊。
- 系统 Chrome 专项验证：种子 1、火热整活、上阵 3/3 时，老弥仍高亮 `27期` 与 `粤帮`，`成熟` 保持暗态；桌面与 390×844 移动商店截图均已打开检查，文本状态、单画布尺寸、DOM 与控制台错误交叉检查通过。
- 验证：`pnpm exec tsc --noEmit` 与商店/Phaser 专项回归 `23/23` 通过；完整自走棋回归在并行的地面棒棒糖战斗改动落盘后为 `95/98`，失败项为已有的“满能量远程单位会先进入攻击距离再施法”，以及与本轮无关的北欧时停、礼小虎眩晕用例。

## 2026-07-17 · 兵种池扩充目标

- 用户反馈：9 个兵种无法合理支撑五档费用与 7 个羁绊，要求大幅扩充并自行提交、推送。
- 审计确认旧版只有 9 个 1—4 费商店单位，唯一“5 费”其实是不可购买 Boss；商店 4 格、备战席 5 格也偏小。
- 已完成新数据设计：30 个可购买兵种，1—5 费各 6 个；5 个阵营与 5 个职业各覆盖 6 个兵种，全部采用 2/4/6 三档羁绊。
- 新池保留原有 9 个单位并新增 21 个单位；每个费用档同时包含前排、输出、法术或切后等不同战术职责。

### 本轮 TODO

- [x] 设计 30 兵种、五档费用、均衡羁绊分布与后期商店概率。
- [x] 接入引擎中的三档羁绊、30 个技能和新敌方波次。
- [x] 将商店扩为 5 格、备战席扩为 8 格，并展示当前费用概率。
- [x] Playwright 验证新商店、羁绊、战斗、强化、结算和重开长流程。
- [x] 生产构建通过后，仅提交本轮相关文件并推送。

- 2026-07-17：TypeScript 校验通过；首轮浏览器状态确认 `purchasableUnits: 30`、每档 `[6,6,6,6,6]`、商店 5 张、备战席 8 格。
- 2026-07-17：实战截图确认新商店与网格无重叠，余烬Ⅰ、先锋Ⅰ能正确激活并作用于单位属性；首批新增单位技能可正常释放，控制台无错误。
- 2026-07-17：第 6 战状态曾由两只同名熔火刃把余烬推到Ⅱ阶，暴露出旧版“同名重复计羁绊”的捷径；现已改为同名只计一次，重复单位仍可上场和追三星，但不能虚增羁绊。
- 2026-07-17：羁绊徽章新增悬停说明，能展示阵营/职业、当前人数及三档完整数值；截图确认不会遮挡关键操作区。
- 2026-07-17：在第 6 战 3% 五费概率下实际刷出并购买虚空终猎者/时序泰坦；五费单位可进入备战席、换上战场并激活裂隙与夜袭，控制台无错误。
- 2026-07-17：长流程走完两次契印、后期敌方技能、失败结算与重新开局；最高分保留，局内状态清空。
- 2026-07-17：同名去重回归通过：两只烬角冲阵者只贡献 1 个余烬；换成四个不同余烬单位后，状态正确显示 `count: 4, level: 2` 和 65% 灼烧。
- 2026-07-17：生产构建通过；自走棋文件无 lint 或类型错误。构建输出中只剩仓库其他页面原有警告。
- 2026-07-17：数据不变量审计通过：30 个单位 ID 全部唯一、五档各 6 个、10 个羁绊各覆盖 6 个、每回合商店概率总和均为 100%。
- 2026-07-17：功能提交 `06c1ca0` 已推送到 `origin/master`。

## 2026-07-20 · 怕死受击后跳优化

- 用户反馈：怕死羁绊的受击后跳出现闪现感，且被远程单位攻击时可能越跳越远、无法重新接敌。
- 已完成：怕死单位受击后接入现有抛物线跳跃状态，使用 0.46 秒持续过程；后撤落点优先保持在自身攻击距离内，越界时沿攻击者周围的侧向弧线跳跃。
- 已完成：增加 0.72 秒后跳冷却，避免远程攻击连续触发后跳造成锁死。
- 已完成：更新怕死羁绊说明，并新增“非瞬移跳跃”和“越界改为侧跳”的回归测试。
- 验证：`pnpm autochess:test` 57/57 通过。
- 2026-07-20：TypeScript 校验通过；完整浏览器流程和专门的怕死 + 远程敌人场景均通过，控制台无运行时错误；截图确认跳跃中间态可见，远程场景落点为侧向弧线。
- 备注：生产构建代码编译通过，但仓库既有 ESLint 的 CRLF/格式错误仍会阻止 `next build` 在 lint 阶段退出成功；本轮未改动这些无关格式问题。

## 2026-07-20 · 合并自走棋图片与羁绊分支

- 已合并 `origin/feature/add-image-auto-chess`，保留裂隙首领头像、主播图片池、弹幕/舞台梦等新内容，并解决核心数据、引擎和类型定义冲突。
- 合并后移除旧的瞬时坐标推送，保留怕死的 0.46 秒真实跳跃；后撤会越出自身攻击距离时改走攻击者侧向弧线，并使用 0.72 秒冷却。
- 回归验证：`pnpm autochess:test` 59/59、`pnpm exec tsc --noEmit` 通过；`verify-autochess.cjs` 浏览器流程无错误，专门场景捕获到 `jumping: true` 且 `jumpFrom`/`jumpTo` 不同。

## 2026-07-22 · 战斗状态与备战区视觉反馈

- 已完成：美·鱿鱼点名减甲时，目标头顶显示持续 🦑 状态，并额外播放一次上浮鱿鱼 emoji；通用头顶标记补齐灼烧、减速、眩晕、跳跃和技能增益状态。
- 已完成：商店中已有同名棋子显示「已有 ×N」徽标、头像脉冲圈和强调边框，宽屏与紧凑布局均覆盖。
- 已完成：移除备战区左上角冗余「已激活」总结；羁绊横条按已激活优先排序；修正横条与「后方 · 远程与辅助 / 6 × 4 / 前线」说明行的间距。
- 已完成：提示层初始化增加空保护，兼容自动化虚拟时间早于 Phaser 场景创建的边界。
- 验证：`pnpm autochess:test` 69/69、`pnpm exec tsc --noEmit` 通过；`verify-autochess.cjs` 完整 Chrome 流程通过且控制台无错误；通用 action client headed 流程可结束，旧版 headless shell 截图仍受环境 WebGL 限制。

- 2026-07-22 追加：商店已有棋子提示改为按 1/2/3 星分桶；纯一星继续显示「已有 ×N」，出现二星或三星时显示对应星级数量，混合星级同时列出各档数量。
- 追加验证：`pnpm exec tsc --noEmit` 通过；通用 action client 可结束且无错误文件，截图仍受本机旧版 headless WebGL 环境限制。
- 2026-07-22 追加：战斗棋子星级移到动态名字右侧；头顶状态/debuff 标记字号降至 13，并固定在头像顶部附近，避免与星级叠加。
- 追加验证：TypeScript 检查与 diff 空白检查通过；临时自动化运行目录已清理。

## 2026-07-23 · AutoChess UI 指挥舱重构

### 视觉与内容方向

- 视觉 thesis：深夜指挥舱——冷墨蓝战场、青蓝系统反馈、金色只强调经济决策，整体克制但保留裂隙与远征的紧张感。
- 内容 plan：开局页负责建立远征目标并选择协议；备战页由原生画布承载战场内简报/羁绊/部署，DOM HUD 承载顶栏与战术商店；移动端用本回合提示 + 底部操作栏 + 抽屉承接商店、备战席、羁绊。
- interaction thesis：商店卡片 hover 横移确认可购买性；购买区持续展示已有棋子数、费用概率、利息/连胜；战斗态只保留一个 DOM 统计入口，移动端抽屉负责深层操作。

### 已完成

- 全量重做 `RiftHud.tsx` 与 `RiftHud.css`：新版远征顶栏、协议选择页、经济/战斗指标、费用概率、商店卡、已有棋子提示、锁店/刷新/升本/开战决策区、战斗状态条、结束页。
- 移动端新增商店/备战席/羁绊抽屉与“本回合快捷提示”，保留安全区和 48px 以上主要触控目标。
- 修正移动端 Phaser 备战视图：竖屏使用 480×1000 移动逻辑组合与 mobile board/bench slot，不再把 1120×720 桌面场景缩成中间小块；移动端由 DOM 抽屉负责商店与备战操作。
- 移除备战阶段画布 toast 与 DOM 的重复层级，保留工具栏状态提示；移除战斗阶段重复的原生统计按钮，统一到 DOM 战斗入口。

### 验证

- `pnpm exec tsc --noEmit` 通过。
- `node --test scripts/tests/autochess-sprite-rendering.test.mjs` 20/20 通过。
- Playwright action client 标题/备战流程无 console error；桌面端实测协议→购买→锁店/刷新→开战→确定性推进→战斗统计，状态接口与画面一致。
- 1280×720、390×844 截图已检查：桌面标题/备战/战斗、移动标题/备战、商店/备战席/羁绊抽屉均可读且无遮挡。
- `pnpm autochess:test` 当前 69/71：本轮未改动的引擎测试“满能量远程单位会先进入攻击距离再施法”仍失败；Phaser 静态检查已恢复 20/20。失败与本轮 HUD/布局变更无调用关系。
- `pnpm build` 已编译成功，但在仓库既有 ESLint 错误（`gameEngine.ts`、`layout.ts`、`RiftLineScene.ts`）阶段退出；新版 HUD 无新增 lint error，仅保留 `<img>` 优化提示。

### 后续可选

- 如果继续打磨移动端，可将战斗态也重排为竖屏专用战场；当前优先保证备战阶段和 PC 浏览器体验。
- 可另行清理仓库既有 lint 错误与远程单位施法回归失败，不应与本轮 UI 变更混在一起。

## 2026-07-23 · 修复重复商店渲染

- 用户反馈宽屏出现两个商店：确认是 React `RiftHud` 新商店与 Phaser `drawWideShop()` 旧商店同时渲染。
- 已关闭桌面 Phaser 旧商店及旧整备操作条，保留 React 商店作为唯一桌面决策区；移动端继续使用 DOM 抽屉。
- 新增 1500px 以上宽屏工作区扩展规则，让新版商店与 2048px 宽屏战场右侧对齐，不再留下旧商店位置的空白错位。
- 验证：2048×1080 截图只剩 1 个商店、1 个可见“开始战斗”按钮，Playwright 无 console error；`pnpm exec tsc --noEmit` 通过。

## 2026-07-23 · 商店说明与羁绊反馈

- 商店棋子支持桌面端 hover / 键盘 focus 说明面板：展示棋子定位、属性、技能描述、回能方式与完整羁绊。
- 商店羁绊 tag 放大，并复用原有“购买后达到下一档阈值”的判定；可激活项显示金色边框、光晕与上箭头，已激活项保留羁绊色强调。
- 移动端商店抽屉同步放大 tag，并验证 390×844 无横向溢出。
- 验证：`pnpm exec tsc --noEmit` 通过；HUD 文件 ESLint 无新增错误（仅原有 `<img>` 优化提示）；Playwright 桌面 hover 说明面板 1 个、待激活 tag 3 个、商店 5 张，移动端商店 5 张、无溢出、无 console error。

## 2026-07-24 · 羁绊明暗、阵型交互与出售区修复

- 羁绊标签改为同色明暗体系：普通羁绊压暗，待激活/已激活使用原色高亮、加粗和同色光晕，不再使用金色框或 ↑ 符号。
- 移除商店头像右下角小圆数字；费用只保留卡片右侧大数字。
- 恢复“已有”分星显示：纯一星显示 `已有 ×N`，混合或出现二星/三星时显示 `已有 3星×N 2星×N 1星×N`；有拥有单位的卡片增加绿色拥有态边缘和明显徽标。
- 修复空的 React 舞台层拦截 Phaser 棋盘事件的问题，阵型棋子 hover 说明与拖拽恢复。
- 桌面备战席新增暗红“拖到这里出售”区域，拖拽进入后亮红并显示返还金币，松开直接出售；右键出售和移动端出售入口保留。
- Phaser 棋子/羁绊 tooltip 在桌面备战阶段自动避开 React 商店，完整显示在商店左侧。
- 验证：`pnpm exec tsc --noEmit` 通过；HUD/Phaser ESLint 无新增错误（仅 `<img>` 与既有 `_tree` 警告）；Playwright 验证移动、出售、hover 说明与商店拥有态，无 console error；390×844 商店 5 张、已有徽标可见且无横向溢出。

## 2026-07-24 · 拖拽 hover 与出售金额反馈

- 拖拽棋子时屏蔽所有棋盘/备战席 slot 的 `pointerover`，经过其他棋子不再弹出说明；拖拽结束后 hover 说明恢复。
- 拖拽开始后出售区即时显示 `出售 +N 金币`，进入出售区显示 `松开出售 +N 金币`；拖拽幽灵移到按钮上方偏右，避免遮住返还金额。
- 验证：两个备战席棋子拖拽经过目标后成功交换且无 tooltip；拖拽备战席棋子出售成功，出售区金额可见，Playwright 无 console error，TypeScript 通过。

## 2026-07-24 · 默认 125% 视觉密度与 Phaser 清晰度

- 宽屏默认 UI 比例改为连续 `1.00–1.25`：`1280×720` 及更小保持原默认大小，空间增加时逐步放大，`1600×900` 以上达到类似浏览器 125% 的视觉密度。
- DOM HUD、工具栏、图鉴和 Phaser 宿主统一放在同一个缩放视口中；移动端仍保持 1.00 并继续使用竖屏专用布局。
- Phaser 改为由 React 宿主显式区分 CSS 显示尺寸与高 DPI backing buffer；渲染密度同时计入设备 DPR 和 UI 放大比例，并继续受 800 万像素预算约束。
- 浏览器验证：`1680×1050 → 1.250`、`1440×810 → 1.125`、`1280×720 → 1.000`、`390×844 → 1.000`，四种尺寸都无页面横向溢出。
- 在 1.25 比例下实际完成协议选择、商店购买、Phaser 棋子选中及棋盘到备战席移动，点击坐标与画面一致；浏览器无运行时 error。
- `pnpm exec tsc --noEmit` 与 Phaser 静态回归 `20/20` 通过；完整套件 `70/71`，唯一失败仍是既有“满能量远程单位会先进入攻击距离再施法”回归。
- 浏览器日志仍有 Phaser 4 对既有 geometry mask `setMask` 的 WebGL warning，本轮未改变该遮罩实现。

## 2026-07-24 · 通用技能视觉反馈

- 所有主动技能统一补上按棋子强调色绘制的施法能量圈；以后新增技能即使没有定制特效，也不会只剩技能名文字。
- 通用 `ring` 效果升级为范围染色、外圈冲击波和内圈扩散反馈，AOE 实际作用区域更容易辨认；`line` 效果增加沿路径飞行的亮核、尾迹和命中扩散圈，可作为即时子弹/光线的通用表现。
- 已有胡萝卜、鲨鱼、松针、钢镚等实体弹幕保持原样；爆发效果增加混合发光轮廓。Phaser 主场景与旧 Canvas 兼容渲染器同步实现。
- `render_game_to_text` 新增当前帧 `battle.visualEffects`，记录非文字效果和实体弹幕，便于自动化精确捕获施法帧；同时修复场景图层尚未初始化时 `refresh()` 提前重建的竞态。
- Chrome 实战分别捕获并检查施法圈 + AOE、飞行连线、松针实体弹幕和 390×844 竖屏 AOE；四类反馈都出现，控制台无 error。竖屏战斗棋盘偏小仍是既有后续项，本轮未改布局。
- `pnpm exec tsc --noEmit` 与 Phaser 静态回归通过；完整自走棋套件仍只有既有的“满能量远程单位会先进入攻击距离再施法”失败。

## 2026-07-24 · 合并胆小与怕死羁绊

- 用户要求移除与“怕死”定位重复的“胆小”羁绊，并将其成员统一归入“怕死”。
- 已完成：删除 `timid` 羁绊 ID、定义及闪避/移速战斗加成；小红帽、小岁鸟、小猫拳、能能弄你、恬豆、又一和狍子偶像改为拥有 `vanguard`（怕死）。
- 已更新数据与战斗回归测试，验证旧成员会计入怕死人数并获得怕死的攻击距离加成。
- 验证：`pnpm exec tsc --noEmit`、9 项数据测试和合并专项战斗测试通过；通用 Playwright 客户端成功进入备战且无 console error。
- 整页 Chrome 截图确认羁绊横条与商店标签只显示“怕死”、不再显示“胆小”，布局无错位；通用客户端的 canvas 截图仍受既有 WebGL 黑图问题影响。
- 完整套件当前 `70/72`：既有“满能量远程单位会先进入攻击距离再施法”仍失败；并行进入工作区的技能位移动画改动导致“小猫拳会闪现到最远敌人身后并推进击晕”新增失败，均与本次羁绊合并断言无关。

## 2026-07-24 · 技能位移过程与滑跪碰撞

- 新增由战斗引擎持有的 `dash / jump / push` 运动状态；位移坐标、碰撞和落地结算均按固定步长推进，Phaser 只读取同一状态渲染弧线、压缩与倾斜。
- 中单光一“滑跪”改为严格直线冲刺；逐帧扫掠路径上的敌人，每个目标只命中一次，并用短促的前向 + 侧向 `push` 过程撞开。
- 小岁鸟、饼干岁、又一、秋凛子、狍子偶像、沐霂的飞行/突进技能均取消瞬时改坐标，伤害、治疗、眩晕与护盾延后到抵达时结算。
- 小猫拳保留技能说明明确要求的起手闪现，随后施法者与目标同步推进，三连击和眩晕在推进结束时结算；冷笑话继续保留真正的瞬移。
- `render_game_to_text` 新增运动种类、技能、进度和起终点；专项回归覆盖滑跪中间坐标、沿途多目标一次命中、击退、跃击空中延迟结算和猫拳同步推进。
- Chrome 实测捕获滑跪起步、中段碰撞、完整击退落点，以及跃击空中和落地状态；截图与文本进度一致，控制台无 error。通用 Playwright 客户端仍受本机既有 WebGL 黑图限制。
- 最终验证：`pnpm exec tsc --noEmit`、Phaser 静态回归 `22/22`、位移专项 `3/3`、`git diff --check` 通过；完整套件 `74/75`，唯一失败仍是既有“满能量远程单位会先进入攻击距离再施法”断言。

## 2026-07-24 · 偷袭羁绊按己方交战提前起跳

- 旧逻辑只检查双方是否进入近身区域，远程队友已经开火时仍可能让偷袭成员继续等待约 3.4 秒。
- 战斗状态新增双方独立的持久交战标记；己方任一单位首次普攻、启动进攻技能或实际造成/承受伤害时，对应队伍立即记为已交战。
- 仍在等待的偷袭成员会在下一模拟帧响应己方交战标记并起跳；原倒计时继续作为双方长期不接触时的兜底。
- 固定步长回归构造了“双方距离超过旧近身阈值、远程队友已经出手”的场景，验证两名偷袭成员在等待时间仍大于 3 秒时提前跳向后排。
- Chrome 连续截图与文本状态确认：首次出手帧 `engagedTeams.player = true`，下一帧两名偷袭成员均进入真实抛物线跳跃；控制台无 error。
- 最终验证：`pnpm exec tsc --noEmit`、偷袭专项 `2/2`、`git diff --check` 通过；完整套件 `75/76`，唯一失败仍是既有“满能量远程单位会先进入攻击距离再施法”断言。

## 2026-07-24 · 邪恶外星人贯穿光线瞄准修复

- 根因：外星贯穿光线的起点使用施法者坐标，但终点固定为战场左右边缘和目标的 y 坐标；目标在斜上方或斜下方时，光束不会经过目标，视觉上总是偏向水平方向。
- 已完成：光束改为从施法者穿过当前目标并延伸至战场边界；贯穿伤害范围同步改为同一条前向射线，避免视觉轨迹与伤害判定不一致。
- 已新增斜上方目标回归，覆盖当前目标、目标后方同线敌人、射线外敌人与光束三点共线断言；专项测试和 TypeScript 检查通过。
- 浏览器使用种子 7 将邪恶外星人布置在左下、敌人置于右上，逐帧捕获确认光束穿过当前目标并向同方向延伸；控制台无 error。
- 完整测试仍只有既有的“满能量远程单位会先进入攻击距离再施法”失败；项目专用长流程脚本另在第一战等待结算时超时，外星人专项、类型检查和真实施法画面均通过。

## 2026-07-24 · 备战棋子属性显示口径修复

- 用户反馈：三星兔子射手在战斗中最大生命为 474，但备战悬浮说明仍显示一星裸值 150。
- 根因：备战棋盘 tooltip 直接读取 `UNIT_DEFS` 基础值，没有应用星级倍率、羁绊、开局协议和契印；战斗实体则使用了完整计算。
- 已完成：抽取并复用玩家部署战斗属性计算；棋盘与备战席预览会应用星级、当前羁绊、开局协议和契印，战斗生成也调用同一函数。
- 回归验证构造三星兔子射手 + 坚韧淬炼 + 第二阵风，备战与战斗均为生命 474、攻击 75、护甲 33、射程 230；初始能量也使用同一预览口径。
- Chrome 整页截图确认 tooltip 在商店左侧完整显示且无控制台错误；TypeScript、专项回归与 Phaser 静态回归通过。完整套件 `76/77`，唯一失败仍是既有“满能量远程单位会先进入攻击距离再施法”。

## 2026-07-24 · 泽音、贪吃岁与椰子栞技能调整

- 已完成：泽音美乐蒂改为 180 血近战首命，首次死亡触发“涅槃重生”切换为 72% 血量的远程形态，攻击 +36%、攻击间隔缩短 30%；贪吃岁改为“吃！”的自动 + 攻击双回能下一击强化吸血；椰子鸡栞改名椰子栞并改为“大声”范围伤害眩晕。
- 验证：TypeScript、差异检查与泽音/贪吃岁/椰子栞专项战斗测试通过。Chrome 页面编译并进入战斗，截图和日志无运行时错误；仓库整局脚本仍在既有结算等待处超时，完整测试仍保留既有“满能量远程单位会先进入攻击距离再施法”失败。

## 2026-07-24 · 技能按星级成长框架

- 用户要求技能能够随星级使用不同参数，并先为以技能为核心的北欧魔法师提升持续时间和范围。
- 已新增单位级三档技能配置与统一的星级参数、说明、成长摘要解析接口；未配置技能继续兼容原固定逻辑。
- 北欧时停的 1/2/3 星参数设为半径 `108/132/162`、持续 `1.8/2.5/3.4 秒`；战斗区域、停滞判定和范围特效共用解析后的参数。
- 商店说明展示完整三档成长，棋盘和战斗悬浮说明展示当前星级效果及成长摘要。
- 验证：`pnpm exec tsc --noEmit` 通过；数据测试 `10/10`，北欧时停三档战斗专项 `3/3` 通过，确认区域半径、持续时间与范围特效一致。
- `render_game_to_text` 已补充活动时停区中心、半径、剩余时间和总持续时间，便于浏览器验证持续控制区域而不只捕获施法瞬间。
- 最终完整套件 `80/81`：北欧时停的数据、战斗和文本状态测试全部通过；唯一失败仍是既有“满能量远程单位会先进入攻击距离再施法”断言。
- 浏览器使用种子 22 验证商店说明，完整显示 `1星 108/1.8秒 · 2星 132/2.5秒 · 3星 162/3.4秒`，面板无截断或遮挡。
- 购买北欧魔法师并进入第一战后捕获真实施法帧：文本状态为半径 `108`、总时长 `1.8`、剩余 `1.77` 秒，画面圆环与作用区域一致，控制台无 error。
- 截图：`.tmp/autochess/spark-skill-growth-shop.png`、`.tmp/autochess/spark-skill-level-1-cast.png`。

## 2026-07-24 · 七海、恬豆与三理理技能调整

- 已完成：七海大鲨鱼改为 5 秒“鲨鱼变身”，持续期间攻击 +85%、吸血 45%；恬豆改为“棒棒糖刘海”，投掷 5 颗可被双方碰撞消耗的 🍭，友军回复 14% 最大生命并加速，敌人受伤并减速；三理理改为“站我后面”，获得 22% 最大生命护盾并嘲讽 155 范围内敌人 3.2 秒。
- 技能状态和投射物同步进入 Phaser、Canvas 兼容渲染与 `render_game_to_text`；嘲讽会覆盖原先锁定目标，目标失效后自动恢复普通选敌。
- 验证：TypeScript、数据测试 10/10、三角色战斗专项 3/3 通过；完整自走棋套件 80/81，唯一失败仍是既有“满能量远程单位会先进入攻击距离再施法”。

## 2026-07-24 · 狍子偶像与雪绘技能调整

- 已完成：狍子偶像技能改为“捏捏摸摸”，持续 3.4 秒锁定最近敌人；双方无法行动，目标持续受伤，狍子偶像按伤害的 90% 治疗自身，并以连线和状态标记反馈引导。
- 已完成：雪绘改为近战，并将“同步视听”改为贴身范围挥斩和灼烧；优势时的骄兵、劣势时的哀兵仍会线性影响攻速和射程，但不再降低或提高移速。
- 已完成：战斗头像加入青蓝“哀”与橙红“骄”的强度光环，持续显示当前同步状态。
- 验证：TypeScript、数据测试和 Phaser 渲染静态测试通过；战斗专项 49/50 通过，唯一失败仍是既有“满能量远程单位会先进入攻击距离再施法”。

## 2026-07-24 · 蛙梓终场歌唱调整

- 已完成：蛙梓保留基础单体远程激光；满能量后进入 5.5 秒的“终场歌唱”，持续耗能并每 0.6 秒治疗全体友军 3.2% 最大生命。
- 歌唱期间普攻切换为射程 235 的火焰弹，主目标受到 90% 攻击力伤害，附近目标受 70% 溅射伤害，均附带范围灼烧；头像显示“歌”状态。
- 验证：蛙梓专项覆盖治疗、火焰弹、范围伤害、范围灼烧和能量耗尽复原；完整战斗测试仅保留既有的远程施法时机失败。

## 2026-07-24 · 理财与流量羁绊

- 新增关系羁绊“理财”（2/4）：低档每场结束额外 +2 金币；高档保留 +2 金币，并将利息改为每 5 金币 +1、无上限。
- 贪吃岁、暴龙岁、饼干岁、椰子栞、七海、梨安、三理理加入理财；七海同时移除“杂谈”。
- 流量补入贪吃岁、能能弄你、沐霂、病院坂灵；每个备战回合按流量等级重置 1/2/3 次免费刷新。
- HUD 已显示实际利息与剩余免费刷新次数。`pnpm exec tsc --noEmit` 通过；新增数据与引擎回归通过。
- Chrome 实测完成购买两名流量成员、首战、结算和进入第二回合；流量 I 保持激活，刷新按钮显示“免费 1”，理财标签也在商店卡正确可见，控制台无错误。
- 完整 `pnpm autochess:test` 为 85/86，唯一失败仍是既有“满能量远程单位会先进入攻击距离再施法”断言，与本轮经济羁绊无关。

## 2026-07-24 · 星汐、塔神与礼墨加入自走棋

- 已下载三人的公开 Bilibili 头像到 `public/images/livers` 并由游戏本地加载：星汐 `51030552`、帅比笙歌超可爱 `15641218`、礼墨 Sumi `435243735`；来源 URL 均返回 `200`。此前生成的本地头像已删除。
- 已移除“星塔墨契”及其全部属性加成和 ID，不再把三人绑定为专属羁绊。
- 星汐保留“山猪冲阵”；笙歌的“塔神”梗取自“脑袋尖尖、背后长了座塔”，技能调整为“尖塔压顶”范围伤害与眩晕；礼墨的东方龙、瓜咪与礼小虎称呼用于“礼小虎出击”范围伤害、眩晕和护甲削弱。
- 验证：`pnpm exec tsc --noEmit`、三人专项数据/战斗回归与 `git diff --check` 通过。两文件战斗套件 `65/66`，唯一失败仍是已有的“满能量远程单位会先进入攻击距离再施法”断言。

## 2026-07-24 · 击败赏金与十本人口

- 用户确认取消基础工资，改为只按实际击败的敌人结算：每颗星提供 1 金，1/2/3 星敌人分别价值 1/2/3 金；败局也保留已经取得的赏金。
- 已移除胜利基础 5 金、失败整备工资和精英固定奖励；利息、连胜、理财、契印与开局协议仍作为额外经济来源。
- 结算状态新增赏金总额、击败人数与分星计数；备战商店和移动端简报会预告全歼本战的总赏金。
- 等级改为标准 3 本起步，补齐 9 本、10 本；最高 10 人口，并新增对应升本费用与高费商店概率。
- 验证：赏金、连续升至十本、零赏金还款专项 `4/4`，数据与 Phaser 静态回归 `34/34`，`pnpm exec tsc --noEmit` 通过。
- 通用 Playwright 客户端确认文本状态含首战 `potentialBounty: 2`、`bookLevel: 3`；其 headless WebGL 截图仍为仓库已有黑图，因此另用应用内浏览器完成视觉验证。
- 浏览器实战确认备战赏金预告、败局按已击败的一星敌人只结算 1 金，以及 9/10 本概率表均清晰显示；控制台无 error。
- 完整 `pnpm autochess:test` 为 `91/92`，唯一失败仍是已有的“满能量远程单位会先进入攻击距离再施法”断言。

## 2026-07-25 · 利息节奏调整

- 普通利息改为每 5 金币提供 1 利息，最多计算 20 金币，即最高 4 利息。
- 理财Ⅱ改为每 4 金币提供 1 利息且不设上限；理财Ⅰ仍只保留每场额外 2 金币。
- 顶栏、商店、Phaser 规则提示、图鉴与文本状态统一读取实际利息，避免显示旧公式。
- 验证：利息专项 `2/2`、数据与 Phaser 静态回归 `34/34`、`pnpm exec tsc --noEmit` 通过。
- 通用 Playwright 文本状态确认 8 金对应 1 利息；应用内浏览器确认 9 金时顶栏和商店均显示 1 利息，玩法说明文案完整且控制台无 error。
- 完整 `pnpm autochess:test` 为 `92/93`，唯一失败仍是已有的“满能量远程单位会先进入攻击距离再施法”断言。

## 2026-07-25 · 流量羁绊成员调整

- 流量移除能能弄你、沐霂·领舞开场、病院坂灵。
- 流量加入毛神、泽音美乐蒂、恬豆·甜点转圈、三理理、雪绘、塔神。
- 更新数据测试与免费刷新专项测试；相关 71 项测试通过 70 项，唯一失败仍是既有远程施法断言。
- `pnpm exec tsc --noEmit` 与 `git diff --check` 通过。浏览器验证因本机已有 Next 进程占用端口且请求超时，未获得可用截图证据。

## 2026-07-25 · 跨区域升星位置保留

- 确认自动升星已优先保留场上同名同星棋子的原格；当其余材料位于备战席时，合成后的二星或三星仍留在场上。
- 新增引擎回归测试，覆盖一枚场上与两枚备战席同名一星棋子合成为二星的场景。
- 验证：新增回归通过；战斗专项仍仅有既有的“满能量远程单位会先进入攻击距离再施法”断言失败。

## 2026-07-25 · 主持成员与帕可 Pako

- 已按用户指定将主持成员收敛为轴轴的宝、北欧魔法师、中单光一、狍子偶像、帕可 Pako、沐霂；其余棋子移除主持。
- 沐霂移除怕死，改为主持 + 跳舞。
- Bilibili 公开账号确认帕可 Pako 为 UID `1978590132`、个人认证“bilibili 直播高能主播”；公开投稿包含全配音剧情实况、吐槽向长流程游戏与翻唱。
- 已下载该账号公开头像到 `public/images/livers/pako.jpg`；帕可作为 5 费主持 + 杂谈 + 流量后排控场加入商店。
- 新技能“全配音实况”攻击敌人最密集区域并短暂打断，随后给主持成员补充能量。
- 验证：数据测试 `13/13`、TypeScript 与 `git diff --check` 通过；完整自走棋套件 `95/96`，唯一失败仍是已有的“满能量远程单位会先进入攻击距离再施法”断言。
- 技能要求的共享客户端已运行；文本状态正常进入第 1 战且无错误文件，纯黑 WebGL 画布截图按仓库约定判为捕获路径失败并拒绝作为视觉证据。
- 系统 Chrome 图鉴确认公开头像、5 费定位、主持/杂谈/流量与技能说明完整显示；主持羁绊仅列出指定六人，沐霂不再出现在怕死名单。
- 系统 Chrome 专项战斗捕获到“全配音实况”范围圈与爆发、两名敌人受伤、帕可和轴轴的宝各获得 18 能量，控制台无 error；整页截图为 1440×900，抽样 501 种颜色且非纯色/近黑。
- 仓库长流程 `verify-autochess.cjs` 能生成正常的标题、备战和战斗整页截图，但其固定 30 秒等待在第 1 战结算处超时；帕可专项短流程已独立通过。

## 2026-07-25 · 自走棋视觉验证入口

- 仓库所有者明确要求：自走棋视觉验证不得先运行通用
  `web_game_playwright_client.js`。它强制使用 headless ANGLE/SwiftShader，
  已知会浪费时间生成纯黑 WebGL 截图。
- 第一次浏览器尝试就必须使用项目内 `verify-autochess.cjs` 的系统 Chrome
  与整页 `page.screenshot()` 流程；专项交互应扩展项目内 Chrome 脚本或使用
  Browser/Chrome 工具，不得回退到通用 SwiftShader 客户端。
- 截图仍需做纯黑、透明和近黑有效性检查，并与 `render_game_to_text`、
  DOM、画布尺寸及控制台错误交叉验证。

## 2026-07-25 · 技能圆圈视觉收敛

- 删除所有主动技能无条件生成的通用 `cast` 施法圈；技能名文字继续保留。
- 继续审计定制 `ring`：移除突进/跳跃起点、召唤起手、自强化/变身、投射物起手、单体连线、群体治疗中心等 20 处无范围含义的圆圈。
- 当前剩余圆圈仅用于护盾，以及实际 AOE 伤害、眩晕、嘲讽或全场冲击范围；投射物、位移、连线和召唤物继续使用自身已有视觉。
- 新增苹果派回归，确认技能起手保留爆发和实体弹幕但不再在施法者脚下生成圆圈；Phaser/Canvas 静态回归同步禁止恢复通用 `cast` 类型。
- 系统 Chrome 定点验证普通单体治疗、护盾和 AOE：`端茶倒水` 触发帧只有技能名与连线，`绿冻护甲` 保留护盾圈，`打翻火锅` 保留 `hotpot + ring` 作用范围；三张整页截图均已打开检查且无浏览器错误。
- 验证：`pnpm exec tsc --noEmit`、Phaser 静态回归 `22/22` 通过；完整 `pnpm autochess:test` 为 `95/96`，唯一失败仍是已有的“满能量远程单位会先进入攻击距离再施法”断言。

## 2026-07-25 · 流量吸血与贪吃岁增强

- 策划调整：流量成员全能吸血由 8%/15%/24% 提高到 12%/20%/32%；4/6 人档由仅远程友军 5%/10% 改为全体友军 8%/15%，并与成员加成叠加。
- 明确底层吸血原本已由统一伤害结算触发，覆盖普攻与技能；规则文案现统一称为“全能吸血”，并新增近战非流量友军用技能伤害回血的回归测试。
- 贪吃岁“吃！”的下一击攻击力倍率由 1.9 倍提高到 2.25 倍，保留该次 45% 额外吸血，避免伤害与续航同时过度膨胀。
- 验证：新增流量专项回归通过，确认 4 流量时成员叠加为 28%、近战和远程非成员均为 8%，且近战非成员的主动技能伤害会回血；数据测试 `13/13`、`pnpm exec tsc --noEmit` 与 `git diff --check` 通过。
- 完整 `pnpm autochess:test` 为 `96/97`，唯一失败仍是已有的“满能量远程单位会先进入攻击距离再施法”断言。
- 仓库长流程 `verify-autochess.cjs` 的系统 Chrome 截图均为有效非黑图，但仍在已有的首战结算等待处超时；专项系统 Chrome 验证确认流量和贪吃岁图鉴文案完整、无截断或遮挡，`render_game_to_text`、画布尺寸与 DOM 正常，控制台无 error。

## 2026-07-25 · 恬豆棒棒糖改为地面陷阱

- 用户要求：恬豆的棒棒糖不再作为远距离普通弹幕命中，改为扔在自身周围的地面，长时间保留并由单位踩踏触发。
- 已完成：5 颗糖果的落点限制在恬豆周围 72–108 像素的扇形范围；飞行阶段不碰撞，落地后保留 10 秒，友军踩到治疗并加速，敌人踩到造成伤害并减速，触发后消失。
- 已完成：Phaser 与兼容 Canvas 为落地糖果增加地面范围圈；`render_game_to_text` 暴露 `grounded` 与剩余时间。
- 验证：恬豆地面糖果专项战斗回归通过；`pnpm exec tsc --noEmit` 与 `git diff --check` 通过。完整 `autochess-battle-spawn` 仍只有既有的远程单位施法失败。

## 2026-07-25 · 绿冻护甲回能与低级三打一平衡

- 用户反馈：低级战斗中三名单位围攻果冻风纪时，纯受击回能会反复刷新绿冻护甲，难以打破。
- 果冻风纪改用独立的“稳态回能”：初始 `25/100`，每秒自动 `+5`，每次受击仅 `+1`，攻击不回能；自动部分在三人围攻时仍高于受击部分。
- 绿冻护甲由 `42%` 收到 `30%` 最大生命护盾；图鉴和技能说明同步展示新回能与护盾数值。
- 新增三项专项回归：护盾数值与破盾视觉状态、1 秒自动回能对比三次受击回能、单独面对第二波三名一星敌人时 24 秒内只释放一次护盾且护盾被打穿、生命降至 10% 以下或阵亡。
- 新增系统 Chrome 专项脚本 `scripts/verify-sun-guard-energy.cjs`，内置纯色、透明与近黑截图拒绝检查。
- Chrome 种子 2 实测：开战能量 `25`，精确推进 1 秒后为 `30`；第一波在 `11.9` 秒触发一次护盾，持久抗压加成后护盾为 `119`，控制台无错误。图鉴、自动回能和护盾触发三张截图均已打开检查。
- 验证：数据测试 `13/13`、绿冻护甲专项 `3/3`、`pnpm exec tsc --noEmit` 与专项 Chrome 流程通过；完整 `pnpm autochess:test` 为 `104/105`，唯一失败仍是已有的“满能量远程单位会先进入攻击距离再施法”断言。

## 2026-07-25 · 护盾贴身显示与额外圆圈清理

- 绿冻护甲不再生成施法 `ring`，只保留统一的技能名浮字；所有护盾都由角色身上的贴身盾图形表达。
- 战斗实体新增当前护盾池峰值；贴身盾的填充与描边透明度按 `shield / shieldPeak` 下降，护盾耗尽、死亡或重生时同步清零。
- `render_game_to_text` 暴露 `shieldPeak`，便于把视觉强度与战斗状态交叉核对。
- 再次审计全部 17 个残余 `ring` 生成点：均对应真实 AOE 伤害、眩晕、嘲讽或全场冲击范围；纯护盾、自强化、治疗、位移起手、召唤起手和投射物起手没有额外圆圈。
- 回归覆盖绿冻护甲无中心 `ring`、只生成一次技能名、护盾比例衰减和耗尽重置；Phaser 静态回归锁定透明度公式。
- 系统 Chrome 使用种子 2 捕获满盾 `167/167` 与受击后 `62/167` 两帧：技能名只显示一次，角色贴身盾明显变淡且没有额外大圈；火锅 AOE 仍保留实际命中范围。
- 截图：`.tmp/autochess-skill-vfx/shield-high.png`、`.tmp/autochess-skill-vfx/shield-low.png`、`.tmp/autochess-skill-vfx/aoe-cast.png`。三张均为 1440×900 有效非黑图，页面单画布、逻辑尺寸 1120×720，DOM、文本状态与控制台错误检查通过。
- 验证：`pnpm exec tsc --noEmit`、绿冻护甲专项 `1/1`、Phaser 静态回归 `25/25` 通过；完整 `pnpm autochess:test` 为 `101/102`，唯一失败仍是既有的“满能量远程单位会先进入攻击距离再施法”断言。
- 追加验证：本轮完整 `pnpm autochess:test` 为 `101/103`；新增失败的能量回收断言也不涉及本轮改动，恬豆专项 `1/1`、Phaser 静态回归 `25/25` 通过。
- 系统 Chrome 专项脚本 `scripts/verify-tiandou-lollipop.cjs` 以 seed 20 实际购买并开战，捕获 5 颗落地糖果，剩余时间约 9.5 秒，截图为 `.tmp/autochess/tiandou-lollipop-grounded.png`，控制台无 error。

## 2026-07-25 · 非自身中心 AOE 投送动画

- 将非自身中心 AOE 分为两类：椰子栞“大声”和帕可“全配音实况”为即时声束型；北欧时停、火锅云、礼小虎出击、尖塔压顶、烟头烫屁股、幽灵终演、终场谢幕为弹幕型。
- 弹幕型会从施法者放出带技能主题符号的缩小范围球，飞到施法时锁定的固定圆心后才同步生成 AOE、结算伤害与控制；飞行期间不会提前伤害或提前显示范围。
- 即时型会在施法者与 AOE 圆心之间生成亮线，并在同一模拟帧触发伤害、控制和范围视觉；自身中心 AOE 与位移落地 AOE 保持原逻辑。
- Phaser 与 Canvas 兼容渲染器均新增 `aoe_orb` 表现；`render_game_to_text` 的投射物状态包含对应技能 ID，便于识别归属和自动化捕获。
- 专项回归覆盖全部 7 个弹幕型技能的延迟结算，以及 2 个声束型技能的同步触发；`pnpm exec tsc --noEmit` 和 `git diff --check` 通过。完整 `pnpm autochess:test` 为 `102/103`，唯一失败仍是既有“满能量远程单位会先进入攻击距离再施法”断言。
- 系统 Chrome 专项脚本 `scripts/verify-remote-aoe.cjs` 使用 seed 22 捕获北欧时停飞行/落地，使用 seed 15 捕获大声声束/范围同帧；画布、文本状态与效果坐标一致，控制台无 error。
- 截图：`.tmp/autochess/remote-aoe-projectile-flight.png`、`.tmp/autochess/remote-aoe-projectile-impact.png`、`.tmp/autochess/remote-aoe-beam-impact.png`。三张均为 1440×900 有效非黑图，透明率和近黑率均为 0，已逐张打开检查。

## 2026-07-25 · 商店经济信息与备战价值层级

- 设计评审确认顶栏金币与商店经济栏重复；已移除顶栏金币，仅在战术商店保留金币、赏金、利息和连胜决策信息。
- 利息数字新增可聚焦、可点击的 info 说明：普通规则为每 5 金提供 1 利息、20 金封顶；理财Ⅱ为每 4 金提供 1 利息且无上限，并显示当前金币对应的实际收益。移动端说明在商店抽屉内就地展开，不遮挡概率与商店卡。
- 备战席为每个棋子常驻显示实际回收价值；二/三星使用与出售逻辑相同的 1/3/9 份价值，高于 5 金时使用明亮金色。单位 tooltip 将费用拆成右上角独立金色标签，长名字会自动截断避让。
- 抽取 `getUnitSellValue()` 统一备战显示、移动端出售按钮与实际返还金额，避免星级棋子显示基础价但返还总价。
- 新增 `scripts/verify-autochess-economy-ui.cjs`，使用系统 Chrome 覆盖桌面利息 hover、备战 tooltip、移动端利息点击展开与备战价值；DOM、`render_game_to_text`、画布尺寸和控制台错误交叉检查通过。
- 截图：`.tmp/autochess/economy-interest-desktop.png`、`.tmp/autochess/economy-bench-tooltip-desktop.png`、`.tmp/autochess/economy-interest-mobile.png`、`.tmp/autochess/economy-bench-mobile.png`。四张均已逐张检查，抽样颜色数 570–3495、近黑率最高 0.01%、透明率 0。
- 验证：`pnpm exec tsc --noEmit`、Phaser/HUD 静态回归 `26/26`、专项 Chrome 流程和 `git diff --check` 通过；完整 `pnpm autochess:test` 为 `104/105`，唯一失败仍是已有的“满能量远程单位会先进入攻击距离再施法”断言。

## 2026-07-25 · 帕可与轴伊费用及治疗技能调整

- 帕可 Pako 从 5 费下调为 1 费，并将生命、攻击、护甲、射程、攻速与移速收回到 1 费远程辅助档位。
- 帕可技能改为“天使摸鱼”：向受伤友军最密集处投出鱼形 AOE 弹，抵达后治疗 128 半径内的所有友军，不再造成伤害、眩晕或主持回能。
- 轴伊从 1 费上调为 4 费，基础属性提升到 4 费远程辅助档位。
- 轴伊“扔橘子”改为连续投出 5 颗实体橘子；每次发射重新选择生命比例最低的友军，治疗倍率依次为 `100% / 82% / 66% / 54% / 44%`。
- 新增系统 Chrome 专项脚本 `scripts/verify-pako-joi-heals.cjs`，交叉验证投射物文本状态、治疗统计、DOM、画布尺寸、控制台错误和截图像素。
- 专项数据测试 `13/13`，新增帕可与轴伊战斗回归均通过，`pnpm exec tsc --noEmit` 与源码 ESLint 通过；完整 `pnpm autochess:test` 为 `106/107`，唯一失败仍是既有的“满能量远程单位会先进入攻击距离再施法”断言。
- 系统 Chrome 捕获帕可鱼形弹飞行/落地和轴伊双橘子飞行/治疗完成四帧；帕可累计治疗 `111` 且敌方生命不变，轴伊五段累计治疗 `476`。四张截图均为 1440×900、抽样颜色至少 4096、近黑率与透明率均为 0，已逐张打开检查，控制台无 error。
- 截图：`.tmp/autochess/pako-joi-heals/pako-angel-fish-flight.png`、`.tmp/autochess/pako-joi-heals/pako-angel-fish-impact.png`、`.tmp/autochess/pako-joi-heals/joi-orange-volley.png`、`.tmp/autochess/pako-joi-heals/joi-orange-impact.png`。
## 2026-07-25 · 中单光一滑跪减速与短眩晕

- 滑跪保留原有直线扫掠、逐目标一次命中和击退逻辑，位移改为专属三次缓出曲线，前段冲刺、末段逐渐减速到停下。
- 被滑跪撞到的敌人新增 `0.45` 秒眩晕，并同步更新技能说明。
- 回归增加前后段位移增量比较和命中时眩晕时长断言。
- `render_game_to_text` 的战斗单位摘要新增 `stun`，并新增 `scripts/verify-guangyi-slide.cjs`，用系统 Chrome 捕获滑跪中段与碰撞帧。
- 种子 27 的 Chrome 验证：滑跪进度 `0.21–0.31` 时已完成约 `51%–67%` 路程，碰撞时敌人剩余眩晕 `0.33–0.39` 秒；两张 1440×900 截图均非黑、非透明，画布逻辑尺寸 1120×720，控制台无错误。
- 验证：滑跪专项 `1/1`、TypeScript 与差异检查通过；完整自走棋套件 `113/114`，唯一失败仍是既有“满能量远程单位会先进入攻击距离再施法”断言。

## 2026-07-25 · 升本费用降至零并跨本结转

- 修复自然减费被 `Math.max(1, ...)` 锁在 1 金的问题；当前升本费用现在可以正常降到 0。
- 新增未消费减费结转：费用已经为 0 后继续完成回合，溢出的每一点减费都会保留；免费升本时先抵扣下一本，仍有剩余就继续跨本级抵扣，玩家不会因为暂时不升本而损失进度。
- `render_game_to_text` 暴露 `upgradeDiscountCarry`；图鉴规则同步说明零费与结转，桌面商店和移动商店会直接显示结转数量。
- 新增引擎回归，覆盖 `1 → 0`、零费后结转、免费升本后 `9 → 8` 以及大额结转连续跨两本抵扣。
- 新增系统 Chrome 专项脚本 `scripts/verify-autochess-upgrade-discount.cjs`；实测 3 本费用降至 0、再等一回合结转 1，免费升到 4 本后金币不变且下一本费用为 8。桌面与移动 DOM、文本状态、画布尺寸、控制台错误和截图像素交叉检查通过。
- 截图：`.tmp/autochess/upgrade-discount-zero.png`、`.tmp/autochess/upgrade-discount-carried.png`、`.tmp/autochess/upgrade-discount-carried-mobile.png`、`.tmp/autochess/upgrade-discount-next-level.png`，四张均已逐张打开检查。
- 验证：专项引擎与 Phaser/HUD 静态回归 `2/2`、`pnpm exec tsc --noEmit`、`git diff --check` 和专项 Chrome 流程通过。升本核心改动完成时完整套件为 `106/107`，仅有既有远程施法断言失败；并行的滑跪/数值改动随后继续进入工作区，最终当前套件为 `101/107`，新增失败落在部署属性、成熟护盾、两条绿冻护甲和开局文案，均不经过本轮升本状态链。仓库长流程 `verify-autochess.cjs` 仍在已有的首战结算等待处超时。

## 2026-07-25 · 泽音涅槃特效与短时攻击后坐力

- 策划结论：泽音第二形态的生存改善应来自短时撤离窗口，而不是永久拉扯或额外提高生命；后坐力必须以当前攻击目标为参照，并把“落点仍在自身攻击距离内”作为硬约束，避免改善生存却造成停火。
- 重生新增专属 `rebirth` 特效：核心闪光、收放双环与旋转放射线共同表现形态切换，不复用有伤害语义的 AOE 圆圈；Phaser 与兼容 Canvas 渲染器同步实现。
- 重生后 4 秒内，每次普攻最多向目标反方向后退 34 像素，使用 0.16 秒 `push` 运动状态逐帧缓动；提交落点前会校验确实远离目标、没有占位冲突且保留 4 像素射程余量，任一条件不满足就取消该次后坐力。
- `render_game_to_text` 暴露 `rebirthRecoilTime` 与既有运动详情；战斗状态标记在撤离窗口显示“退”，技能说明同步写明持续时间和射程保护。
- 新增 `scripts/verify-zeyin-rebirth.cjs`，使用系统 Chrome 构造确定性重生场景，交叉验证涅槃特效、后坐力中间态、射程边缘不后退、4 秒后停止、画布尺寸、控制台/资源错误和截图像素。
- Chrome 实测：泽音从 `x=280` 向后缓动至 `x=246`，中间帧位于 `x=258`；与目标距离从 120 增至 154，仍小于 245 攻击距离。截图 `.tmp/autochess/zeyin-rebirth-effect.png` 与 `.tmp/autochess/zeyin-rebirth-recoil.png` 均为 1440×900、非纯色/透明/近黑图，并已逐张打开检查。
- 验证：泽音专项 `1/1`、Phaser 静态回归 `26/26`、`pnpm exec tsc --noEmit`、`git diff --check` 与专项 Chrome 流程通过；完整 `pnpm autochess:test` 为 `113/114`，唯一失败仍是既有“满能量远程单位会先进入攻击距离再施法”断言。仓库长流程 `verify-autochess.cjs` 仍在已有的首战结算等待处超时。

## 2026-07-25 · 天赋三档与同轮平衡

- 将局内成长明确拆为开局天赋、局中小天赋、局中大天赋：第 2 战后只出现小天赋，第 5 战后只出现大天赋；无限模式从第 14 战开始每 6 战交替两档。
- 两个局中档位各扩充到 6 项。小天赋统一收敛为约 8%–15% 常驻战力或 4 金净经济；大天赋以“德川家康”为锚，加入并校准抢先施法、持续治疗、斩杀、风险输出和开战护航等同档选择。
- 开局协议按起始棋子的 1/2 费价值重新补偿：成熟稳重与舞台梦增加金币，火热整活和持久抗压收回过强附加值，热点追踪与稳定输出保留方向性优势。
- 同档天赋在全部拿完前严格不重复；只有极后期耗尽该档 6 项后才回补旧项。回补项会标为“再次强化”，所有数值按层数生效。
- 图鉴、选择页、结算按钮、提示、历史记录和 `render_game_to_text` 均显示小/大档位、选择 ID、层数与历史；图鉴按三类分组展示。
- 新增 `scripts/tests/autochess-talents.test.mjs` 和系统 Chrome 专项 `scripts/verify-autochess-talents.cjs`，覆盖轮次分档、去重、耗尽回补、开局资产、关键数值、真实点击、图鉴滚动和截图有效性。
- 系统 Chrome 使用 seed 4 验证：第 2 战为花呗生活/弹幕加速/体能储备，第 5 战为德川家康/收割/出道推流；实际点击后历史档位正确，耗尽小天赋池后才出现“再次强化”。七张 1440×900 截图均非黑、非透明，逐张打开检查，控制台无 error。
- 截图：`.tmp/autochess/talents/opening-talents.png`、`talent-codex.png`、`talent-codex-major.png`、`talent-codex-major-bottom.png`、`minor-talents-round-2.png`、`major-talents-round-5.png`、`minor-talents-exhausted-refill.png`。
- 验证：TypeScript、专项数据/战斗/Phaser 回归 `46/46`、`git diff --check` 与系统 Chrome 专项通过；完整自走棋套件 `113/114`，唯一失败仍是既有“满能量远程单位会先进入攻击距离再施法”断言。仓库长流程 `verify-autochess.cjs` 仍在已有的首战结算等待处超时。

## 2026-07-25 · 连锁三星场上位置优先

- 修复连锁合成的保留位优先级：此前备战席一星先合出的二星会覆盖场上已有二星的优先级，导致最终三星错误留在备战席。
- 现在每一轮合成均先选择场上材料；仅在该轮没有场上材料时，才保留连锁合成刚生成的位置。
- 回归覆盖一枚场上一星的跨区二星，以及“场上一枚二星 + 备战席一枚二星与三枚一星”的完整连锁三星场景。
- 验证：两项升星回归 `2/2`、`pnpm exec tsc --noEmit` 与 `git diff --check` 通过。
- 系统 Chrome 已确认备战画布截图有效、非黑且布局正常；仓库长流程仍在已有的首局结算等待处超时，与此次合成保留位无调用关系。

## 2026-07-25 · 25 战主线与完整收入难度曲线

- 将主线从 8 战扩展为 25 战；第 5/10/15/20 战固定为精英关，第 25 战为终局首领。旧第 4/7 战精英标记与第 8 战提前首领峰值已收平。
- 第 26—40 战为普通无限，敌方预算按“5 利息 + 2 连胜 + 上一战实际星级赏金”逐战复投；第 41 战起为地狱无限，改按“20 利息 + 理财固定 2 金 + 2 连胜 + 上一战实际星级赏金”逐战复投。
- 敌方预算优先转换为人口和升星；10 人三星满编后，剩余预算才通过平方根属性倍率兑现，避免同一份经济增长被属性重复指数放大。删除原先每回合无上限 `+2` 护甲的隐藏成长，改为随星级和预算倍率小幅增加。
- 关键采样：第 26 战预算 96、预计结算收入 20；第 40 战预算 504，10 个三星敌人提供 30 赏金，叠加 20 利息、2 理财、2 连胜后，第 41 战预算增加 54 至 558。
- 理财Ⅱ改为每 4 金 1 利息、80 金封顶，即最高 20 利息；普通利息仍为最高 4。
- 主线天赋扩为第 2/5/10/15/20/25 战后交替小/大天赋；无限从第 31 战起每 6 战继续交替。
- `scripts/verify-autochess-progression.cjs` 使用系统 Chrome 验证第 5 战精英预警、第 26 战普通无限、第 41 战地狱无限、100 金理财仍为 20 利息、玩法说明和 390×844 移动端预警布局；五张截图均通过纯色、透明和近黑拒绝检查，已逐张打开确认无截断或遮挡，控制台与资源请求无错误。
- 截图：`.tmp/autochess/progression/round-05-elite-warning.png`、`round-26-normal-endless.png`、`round-41-hell-finance-cap.png`、`progression-rules.png`、`round-05-elite-warning-mobile.png`。
- 最终验证：`pnpm exec tsc --noEmit` 与 `git diff --check` 通过；完整 `pnpm autochess:test` 为 `116/117`，唯一失败仍是已有“满能量远程单位会先进入攻击距离再施法”断言。仓库长流程 `verify-autochess.cjs` 仍在既有首战 `result` 等待处超时。

## 2026-07-25 · 桌面羁绊多行收纳

- 修复桌面备战阶段羁绊数量多时横向越出主面板的问题；宽屏不再依赖 Phaser WebGL 几何遮罩和横向拖动，改为面板内最多三行的紧凑居中排列。
- 多行按数量均衡分配，18 种羁绊会稳定显示为 `6 / 6 / 6`，避免末行只剩一个标签；棋盘整体小幅下移并压缩行距，羁绊、方向说明、24 个部署格和备战席均不重叠。
- 同步更新仓库 Chrome 长流程与经济 UI 专项脚本中的棋盘坐标，并新增 `scripts/verify-autochess-trait-layout.cjs`，构造覆盖全部 18 种羁绊的九人阵容验证边界。
- 系统 Chrome 在 `2048×1104` 下实测 18 个标签逻辑边界为 `x=161.5..634.5`、`y=184..260`，全部位于 `x=48..748` 的主面板羁绊区内；截图 `.tmp/autochess/trait-layout/all-traits-desktop.png` 已打开检查，非黑、非透明，画布、文本状态与控制台错误交叉检查通过。
- 验证：`pnpm exec tsc --noEmit`、Phaser 静态回归 `26/26`、专项 Chrome 流程与 `git diff --check` 通过。完整 `pnpm autochess:test` 为 `114/115`，唯一失败仍是已有的“满能量远程单位会先进入攻击距离再施法”断言；源码 ESLint 无新增错误，仅保留已有 `_tree` 未使用警告。

## 2026-07-25 · 结算阵容固定行高与滚动视窗

- 用户反馈：战斗结算会随阵容数量增加持续缩小行高、头像和字号，希望先显示固定数量，剩余内容通过滚动条查看，同时保留棋子 hover 详情。
- 设计决策：继续使用现有 Phaser 结算层，避免只为单一面板引入第二套 DOM 状态与交互；两侧阵容改为独立的 6 行固定视窗，使用 48px 行高、固定 10/8px 字号，并显示当前可见名次范围。
- 已实现：我方与敌方各自支持鼠标滚轮、滚动槽点击和滚动块拖拽；只实例化当前可见行，因此被裁掉的行不会产生越界命中区域，可见行继续保留 hover / 点击详情。
- 验证：新增 `scripts/verify-autochess-result-scroll.cjs`，用系统 Chrome 构造 10 对 8 的结算数据，覆盖首屏 6 行、两侧独立滚动、滚动槽点击、滚动块拖拽、指标切换保持位置、滚动前后 tooltip、紧凑布局与完整文本排名；五张整页截图均通过纯色、透明和近黑拒绝检查，控制台无错误。
- 最终检查：`pnpm exec tsc --noEmit`、结算/Phaser 静态回归、`git diff --check` 和目标源码 ESLint 均通过（仅保留已有 `_tree` 未使用 warning）；完整 `pnpm autochess:test` 为 `116/117`，唯一失败仍是已有“满能量远程单位会先进入攻击距离再施法”断言。

## 2026-07-25 · 帕可治疗区增强与目标生命解耦

- “天使摸鱼”半径由 `128` 提高到 `145`；落地治疗后留下持续 `3.2` 秒的区域，每 `0.7` 秒重新治疗当前位于范围内的友军，共触发 4 次脉冲。
- 治疗量完全按帕可自身属性计算，不读取目标最大生命：落地为 `1.6 × 帕可攻击 + 8% 帕可最大生命`，每次脉冲为 `0.7 × 帕可攻击 + 2.5% 帕可最大生命`。
- 一星帕可落地约治疗 `40`、每次脉冲约 `16`、总计约 `105`；二星与三星随帕可自身属性成长。三星五费大核不会反向放大一星帕可的奶量。
- 治疗区是固定战场区域：走出后停止回血，后续走入的友军可以从下一次脉冲开始获得治疗；`render_game_to_text` 暴露范围、剩余时间和下次脉冲时间。
- 回归覆盖普通友军与 `10000` 最大生命模拟大核获得相同治疗、进出范围、四次脉冲和敌方不受影响。帕可/轴伊专项 `2/2`、帕可数据专项 `1/1`、`pnpm exec tsc --noEmit` 通过；完整自走棋套件 `116/117`，唯一失败仍是既有远程施法距离断言。
- 用户明确表示由其负责视觉验证，本轮停止后续浏览器截图流程。

## 2026-07-25 · 修复线上构建 ESLint errors

- 修复自走棋构建阻断项：图鉴天赋分组的 `tier` 变量遮蔽、波次计算的 `unit` 变量遮蔽、条件箭头表达式、循环内预算闭包和 `Math.pow` 限制。
- 目标源码 ESLint 已达到 `0 errors`，`pnpm exec tsc --noEmit` 通过；清理不一致的本地 `.next` 生成产物后，`pnpm run build` 完整通过，线上构建也已通过。
- 构建仍保留仓库既有非阻断 warning；`pnpm autochess:test` 当前为 `116/117`，唯一失败仍是已有“满能量远程单位会先进入攻击距离再施法”断言，与本轮 lint 修复无关。

## 2026-07-25 · 怕死后跳保持接敌移动

- 怕死单位受击后跳期间继续执行正常接敌移动，前进位移会同步平移整条跳跃轨迹，不再因 `jumpTime` 暂停近战单位接近远程目标；跳跃中仍不会提前普攻或施放技能。
- 跳舞成员处于 `danceDashTime` 冲刺期间不会被怕死后跳打断，受击仍正常结算，冲刺状态保留。
- `render_game_to_text` 新增 `jumpProgress` 与 `jumpAdvancing`，并新增系统 Chrome 专项脚本 `scripts/verify-vanguard-jump.cjs`。
- 引擎回归新增后跳前进和冲刺保护两项；专项 Chrome seed 2 捕获果冻风纪在 `jumpProgress 0.09`、`jumpAdvancing: true` 的后跳中段，x 坐标连续前进，截图 `.tmp/autochess/vanguard-jump-advancing.png` 已打开检查，画布和控制台无错误。
- 验证：`pnpm exec tsc --noEmit`、Phaser 静态回归 `26/26`、`git diff --check` 通过；完整 `pnpm autochess:test` 为 `119/120`，唯一失败仍是已有的远程施法距离断言。

## 2026-07-25 · 十六战主线、敌方羁绊与特殊角色

- 主线收紧为 16 战，第 4/8/12 战为精英关，第 16 战为终局首领；第 17—31 战为普通无限，第 32 战起进入地狱无限。
- 无限第 17 战预算锚点提高到 135，确保高于第 16 战首领的 125；普通无限继续复投 5 利息、连胜与击杀赏金，地狱无限复投 20 利息、理财固定收入、连胜与赏金。
- 敌方阵容改为围绕固定组队方案生成，并在战斗中实际获得羁绊属性和运行时效果，包括成熟护盾、流量吸血、余烬成长、贪吃恢复、跳舞冲刺、偷袭跳后排、粤帮支援等。
- 整备页敌方头像正上方显示已激活羁绊与档位，移动端简报同步显示；`render_game_to_text()` 暴露敌方羁绊数组。特殊角色不显示“毕业嘉宾”或“仅敌方可用”标签。
- 弥希Miki与初濑Hatsuse偶尔加入敌方阵容且不进入商店。弥希使用双段范围伤害和短眩晕，初濑攻击最多三名目标并将部分伤害转化为团队治疗。
- 数据回归确认第 1—40 战每关至少激活一条敌方羁绊；高存款但维持七人低星的截图阵容会在第 12 精英和第 16 首领战失败，要求玩家把存款继续转化为人口与星级。
- 验证：`pnpm exec tsc --noEmit`、目标 ESLint（0 error）、`git diff --check`、数据/战斗/Phaser 专项均通过；完整自走棋套件 `123/124`，唯一失败仍是已有“满能量远程单位会先进入攻击距离再施法”断言。
- 系统 Chrome 验证第 4 精英、第 17 普通无限、第 32 地狱无限、20 利息封顶、玩法说明和 390×844 移动端布局；5 张截图均非黑、非透明并逐张检查，文本状态、画布尺寸、控制台和资源请求交叉检查通过。

## 2026-07-25 · 第二关费用迁移、普通关储蓄空间与败局结算

- 修复轴伊升为 4 费后仍出现在第 2 战固定阵容的问题；第 2 战现为绒绒的狗、浣熊店员、好笑姐姐三名一费单位，总预算维持 5，并激活野性羁绊。
- 第 9—15 战的程序生成普通关使用 75% 基础预算，精英与首领预算保持不变；预算调整为 `17 / 22 / 26 / 81 / 38 / 45 / 53 / 125`，形成“精英投入、普通关恢复储蓄、下一次精英再检查”的循环。
- 赏金改为按敌方完整阵容星级结算，1/2/3 星分别提供 1/2/3 金，无论胜负都全额发放；实际击败数只保留为战斗统计。商店、备战简报、玩法说明和结算文案统一改为“阵容结算/结算金”。
- 回归覆盖前两战不出现高费棋、第二战正常三人阵容可通过、通过第 12 精英的阵容无需追加投入即可连续通过第 13—15 普通关，以及败局击败 `0/3` 仍获得完整 3 金结算。
- 系统 Chrome 实测第 2 战三名敌人均为一费且预算为 5；强制败局显示“阵容结算 3（敌军1星×3；击败 0/3）”，加 1 利息后收入 4。第 4 战桌面/移动预警、无限关、规则页和七张截图均逐张检查通过，控制台与资源请求无错误。
- 验证：`pnpm exec tsc --noEmit`、目标源码 ESLint（0 error）、`git diff --check` 与系统 Chrome 专项通过；完整 `pnpm autochess:test` 为 `126/127`，唯一失败仍是已有“满能量远程单位会先进入攻击距离再施法”断言。

## 2026-07-25 · 绿冻护甲三路回能提速

- 用户反馈此前稳态回能 `每秒 +5 / 受击 +1 / 攻击 +0` 过慢，果冻风纪有时未施放技能就阵亡。
- 按用户指定改为 `每秒自动 +8 / 每次受击 +3 / 每次攻击 +6`；初始 `25/100` 和 `30%` 最大生命护盾保持不变，图鉴与技能说明同步展示三种回能来源。
- 三打一专项允许果冻风纪正常攻击，并将三名一星敌人生命提高到不会提前减员；24 秒内果冻风纪共获得两次护盾后仍被击败，未恢复为无限续盾。
- 系统 Chrome seed 2 实测：暂停实时循环后 1 秒能量从 `26` 增至 `34`；战斗实体为自动 `+8`、攻击 `+6`、受击 `+3`，第一面盾在 `5.9` 秒触发，控制台无错误。
- 图鉴、1 秒自动回能和护盾触发三张截图通过纯色、透明与近黑拒绝检查，并已逐张打开确认文案与画面正常。
- 验证：数据测试 `17/17`、绿冻护甲专项 `3/3`、`pnpm exec tsc --noEmit`、专项 Chrome 与 `git diff --check` 通过；完整 `pnpm autochess:test` 为 `125/126`，唯一失败仍是既有的远程施法距离断言。仓库长流程 `verify-autochess.cjs` 仍在已有的首战 `result` 等待处超时。

## 2026-07-25 · 修复敌方偷袭跳回己方阵地

- 根因：`prepareAssassinJump()` 固定从 `battle.enemy` 选择目标，敌方偷袭者因此会把敌方后排当成跳跃目标。
- 修复：偷袭者现在从敌对阵营选择目标；我方按最大 `x` 取敌方后排，敌方按最小 `x` 取我方后排，落点方向和目标阵营保持一致。
- 新增敌方偷袭回归，使用第 17 战多个偷袭成员的生成阵容，确认敌方落点位于我方目标左侧且贴近目标；原有我方提前起跳回归继续通过。
- 新增 `scripts/verify-assassin-direction.cjs`，使用系统 Chrome seed 98 构造第 17 战敌方偷袭场景；中段截图确认敌方单位从 `x=900` 向我方侧移动，文本状态记录 `jumpToX≈236.7`，画布 `1440×853`，控制台和资源失败列表为空；截图已打开检查。
- 验证：敌方/我方偷袭专项均通过，`pnpm exec tsc --noEmit` 与 `git diff --check` 通过；完整 `pnpm autochess:test` 为 `127/128`，唯一失败仍是既有“满能量远程单位会先进入攻击距离再施法”断言。仓库长流程 `verify-autochess.cjs` 仍在既有首战 `result` 等待处超时。

## 2026-07-25 · 修正技能释放时机

- 小红帽“攻击弹幕”从 `selfOnHit`（受击释放）改为 `offenseReady`（满能量按攻击释放）；雅吨“辣福”和毛神“夺回人生”从受击释放改为 `offenseInRange`（进入攻击范围后释放）。现有明确自保技能的受击触发只剩果冻风纪，礼墨的并行“空气龙”改动保持 `selfBuff`。
- 新增技能类别数据回归和战斗回归：小红帽未受击即可施放；雅吨/毛神在攻击距离外不释放、进入距离后释放。
- 新增 `scripts/verify-skill-cast-timing.cjs`，使用系统 Chrome 构造三种确定性战斗场景；`render_game_to_text`、Canvas `1440×853`、DOM 和控制台错误交叉检查通过。三张截图 `skill-cast-sui.png`、`skill-cast-rift_brawler.png`、`skill-cast-meme.png` 均通过非纯色/非透明/非近黑检查并已逐张打开确认。
- 验证：技能专项 `4/4`、`pnpm exec tsc --noEmit`、`git diff --check` 通过；同步修正了远程进攻技能旧回归的过时断言，完整 `pnpm autochess:test` 为 `145/145`。仓库长流程 `verify-autochess.cjs` 仍在既有首战 `result` 等待处超时，但与技能触发逻辑无关。

## 2026-07-25 · 重复护盾角色回能审计

- 用户反馈绒绒的狗也会在多人围攻时反复刷新护盾，并要求排查其他护盾角色。
- 审计全部 11 个可重复护盾角色后，确认绒绒的狗、米米、星汐和沐霂仍共用旧 `bulwark` 模板（`受击 +20`），会让多人集火反向加速下一面护盾；四者现统一改为稳态护盾回能：`每秒 +8 / 攻击 +6 / 受击 +3`。
- 其余护盾角色原本使用自动或流转回能，受击仅 `+0～4`；成熟羁绊、全员护航和开局增幅只触发一次，不属于重复刷新风险，数值保持不变。
- 绒绒的狗技能说明同步明确自动充能为主、攻击与受击少量回能；舞台梦开局回归同步其基础能量变化，当前为 `25 + 10 = 35`。
- 新增数据守卫，禁止全部可重复护盾角色再次接入高受击回能；新增绒绒的狗自然三打一回归，以及覆盖 11 个角色的归一化三人持续集火压力回归。
- 系统 Chrome seed 149 实测绒绒的狗战斗实体为 `8/6/3`，自然第二关三打一只施放 1 次并在约 `11.6` 秒被击破，护盾归零；截图 `.tmp/autochess/shields/mossback-first-shield.png` 为 `1440×900`、非纯色/透明/近黑图，已打开确认护盾环、战场和 UI 正常，控制台与资源失败列表为空。
- 验证：数据 `18/18`、战斗 `93/94`、护盾压力父子项 `12/12`、`pnpm exec tsc --noEmit --incremental false`、目标源码 ESLint 与 `git diff --check` 通过；完整 `pnpm autochess:test` 为 `144/145`，唯一失败仍是已有“满能量远程单位会先进入攻击距离再施法”断言。仓库长流程首次在旧 3000 实例等待画布超时，切到有效的 3001 实例后仍在既有首战 `result` 等待处超时，专项系统 Chrome 流程通过。

追加：本轮技能释放时机修正后已同步更新该远程旧回归，完整 `pnpm autochess:test` 最终为 `145/145`；技能定向回归 `4/4`，系统 Chrome 技能专项与截图检查通过。

## 2026-07-25 · 礼墨空气龙与社恐

- 礼墨主动技能改为“空气龙”：短时隐身并降低敌方攻击优先级，视觉上将礼墨渲染为半透明；敌方会优先选择其他未隐身目标。
- 新增被动“社恐”：礼墨每次普攻后沿现有泽音变身后坐力轨迹把自己推离目标；破隐一击不再写入社恐描述。
- “空气龙”描述保留破隐一击效果：隐身期间第一次普攻会现身并发射礼小龙；礼小龙使用公开图片素材，封装为圆形头像框投射物。
- 新增 `scripts/verify-sumi-air-dragon.cjs` 的快速系统 Chrome 流程；种子 `9` 捕获到 `stealthTime: 3.29`、`sumiDragonReady: true` 和 `sumi_dragon` 投射物，`render_game_to_text` 与画面一致，控制台无错误。
- 截图 `.tmp/autochess/sumi-air-dragon/sumi-shop.png`、`sumi-preparation.png`、`sumi-air-dragon-stealth.png`、`sumi-little-dragon-projectile.png` 均已打开检查，非纯色、非透明、非近黑画面。
- 本轮最终验证：礼墨数据/战斗专项、完整 `pnpm autochess:test`（`145/145`）与 `git diff --check` 通过；类型检查被工作期间出现的另一组未提交天赋 UI 改动阻断，错误位于 `RiftLineScene.ts:835` 的 `Vector2[]` 参数，与礼墨改动无关，相关用户改动已保留。

## 2026-07-25 · 桌面羁绊恢复单行滚动

- 按用户反馈撤销此前桌面羁绊三行收纳方案：宽屏羁绊始终只占一行，少量羁绊在栏内居中，不再提前预留两行空白；棋盘方向说明、24 个部署格和行高恢复到原位置。
- 羁绊总宽度超过 700 逻辑像素时从左侧开始，保留鼠标横向拖动与滚轮浏览；滚动偏移限制在 `0..minimumOffset`，可完整抵达最后一个羁绊。
- 根因是 Phaser 4 `GeometryMask` 只支持 Canvas 渲染器；WebGL 现改用 Mask Filter 裁切，Canvas 继续使用几何遮罩，因此两种渲染路径都不会让标签越出主面板。
- 系统 Chrome 专项验证：3 个羁绊为单行且居中，18 个羁绊为单行；滚轮偏移 `-315`、拖动偏移约 `-250.11`、最终偏移精确停在 `-710`，WebGL 滤镜类型为 `FilterMask`，控制台无错误。
- 截图 `.tmp/autochess/trait-layout/few-traits-single-row.png`、`all-traits-row-start.png`、`all-traits-row-scrolled.png` 均通过纯色、透明和近黑拒绝检查，并已逐张打开确认起点、终点和面板裁切正常。
- 验证：`pnpm exec tsc --noEmit --incremental false`、Phaser 静态回归 `26/26`、完整 `pnpm autochess:test`（`146/146`）、目标源码 ESLint（`0 error`，仅保留既有 `_tree` warning）与 `git diff --check` 通过。

## 2026-07-25 · 帕可治疗区视觉重做

- “天使摸鱼”不再复用紫色大型 `ring` / `burst` 攻击特效，改为专属 `healing_field` 与 `healing_pulse`；治疗数值、145 判定半径、3.2 秒持续时间和 0.7 秒跳奶间隔均未改变。
- 实际范围只保留极淡的薄荷绿边界与低透明底色，中心和四个方位使用医疗加号明确表达友方恢复；每次治疗只在中心约 60 像素范围内柔和闪烁并上浮光点，不再高亮整个判定区。
- Phaser 专属绘制拆到 `phaser/healingEffects.ts`，场景类只负责特效分发；备用 Canvas 渲染同步实现同一视觉语义。
- 回归新增帕可不再生成大型攻击圆环、治疗色与尺寸、Phaser 接线及 Canvas 分支守卫。`pnpm exec tsc --noEmit --incremental false`、完整 `pnpm autochess:test`（`147/147`）、本次视觉源码 ESLint（`0 error`，仅保留既有 `_tree` warning）与 `git diff --check` 通过。
- 按用户此前要求，本轮未启动浏览器或截图视觉验证，由用户在实际游戏中验收观感。

## 2026-07-25 · 手机战斗性能优化

- 用户反馈手机浏览器战斗卡顿；研究 Phaser 官方性能文章、Phaser FPSConfig 文档与 MDN WebGL 最佳实践后，按“限制无收益刷新、缩小移动端 back buffer、缓存引用、复用短命对象、选择性处理高频视觉”的路径优化。
- 高刷新手机的 Phaser 游戏循环封顶 60 FPS；390×844、设备 DPR 3 的手机画布渲染密度由 2 限到 1.5，文字纹理同样限到 1.5，桌面仍保留最高 2。
- 投射物和技能特效容器加入有界对象池，缓存内部 Graphics/Text/Image 引用与静态投射物外观；棋子视图也缓存子节点引用，计时框只在进入最后 6 秒时重画。
- 压测发现 10 对 10 场景平均同时存在约 32 个飘字、峰值 49—59 个；手机渲染层现最多保留 18 个并发伤害/治疗数字，非文字技能特效、战斗模拟和统计不受影响，桌面不裁剪。
- 新增 `scripts/verify-autochess-performance.cjs`，用系统 Chrome、390×844、DPR 3、10 对 10、12 倍生命压力场景采样实际游戏更新率、组件耗时、长任务、CPU、堆、并发特效，并执行截图纯色/透明/近黑拒绝检查。
- 干净 dev server 实测：正常 CPU 为 `60.36` 次游戏更新/秒，战斗同步平均 `3.00 ms`、P95 `4.6 ms`，0 个长任务；飘字预算前同档为平均 `3.46 ms`、特效同步 `1.17 ms`，预算后分别为 `3.00 ms`、`0.77 ms`。4 倍 CPU 降速极端压力下为 `12.99` 次更新/秒；旧基线约 `8.55 FPS`。
- 最终性能截图 `.tmp/autochess/performance/final-mobile-cpu1-battle.png` 与 `final-mobile-cpu4-battle.png` 均通过检查并逐张打开确认；控制台、失败请求和错误响应为空。移动端专项 9 张截图也全部逐张检查。
- 验证：`pnpm exec tsc --noEmit --incremental false`、目标源码 ESLint、Phaser 静态回归 `28/28`、完整 `pnpm autochess:test`（`148/148`）、移动端 Chrome 专项和 `git diff --check` 通过。
- 仓库主 `verify-autochess.cjs` 仍在既有首战 `result` 等待处超时；本轮性能与移动端专项均能推进至战斗/结算并通过，故继续归类为旧长流程脚本问题。

## 2026-07-25 · 手机横竖屏战斗、商店与统计适配

- 粗指针手机在横竖屏都切换为低干扰 HUD：隐藏网页工具栏，备战状态压缩为悬浮信息，操作区固定到安全区；横屏商店改为右侧全高面板，五张商品卡无需滚动即可看全。
- 横屏战斗默认放大到 `1.18x` 并占满 `1138×640` 视口，支持单指拖动画面、双指缩放、滚轮缩放，以及减小、复位、放大按钮；测试覆盖拖动、按钮缩放与双指缩放到 `2.4x`。
- 手机结算改为 DOM 自适应布局，用“我方阵容 / 敌方阵容”标签切换单队列表，并保留输出、治疗/护盾、承伤指标切换；8 人阵容在独立列表内滚动，横屏和 `390×844` 竖屏均无页面溢出。
- 竖屏备战底部新增始终可见的“出售”按钮，选中棋子后显示售价并可直接完成出售，不再依赖不可见的拖拽回收区；桌面端原工具栏、商店和拖拽出售交互保持不变。
- 新增系统 Chrome 专项 `scripts/verify-autochess-mobile-ui.cjs`；横屏备战/商店/战斗/缩放/双方统计、竖屏备战出售/统计、桌面备战共 9 张截图位于 `.tmp/autochess/mobile-ui/`，均通过纯色、透明和近黑拒绝检查并已逐张打开确认，控制台与失败请求为空。
- 验证：`pnpm exec tsc --noEmit --incremental false`、完整 `pnpm autochess:test`（`147/147`）、目标源码 ESLint、`git diff --check` 与系统 Chrome 专项均通过。

## 2026-07-26 · 战斗双方羁绊态势栏

- 战斗顶部新增双方羁绊态势栏：左侧我方、右侧敌方，tag 直接显示羁绊名、实际人数与当前档位；数据分别来自我方当前上阵阵容和本战敌方阵容，不复制战斗数值。
- 桌面 hover / 键盘 focus、触屏点按 tag 都会显示完整说明，包括羁绊定位、基础机制、当前生效效果和所有档位数值。
- 中央图标按钮支持收起与展开；桌面默认展开，小屏和粗指针设备默认收起。收起态缩为居中的双方颜色摘要，竖屏放在缩放战场上沿外侧，避免长期遮挡棋子。
- 双方 tag 保持单行并可横向浏览；说明层会按阵营靠左或靠右展开，竖屏改为全宽详情，三种视口均无页面横向溢出。
- 新增 `scripts/verify-autochess-battle-traits.cjs`，使用系统 Chrome 覆盖 `1440×900`、`390×844`、`1138×640` 的展开、hover/点按说明、默认收起和手动切换；DOM 羁绊数量与 `render_game_to_text()` 中的名称、人数、档位一致，画布尺寸、控制台和失败请求均通过交叉检查。
- 六张截图位于 `.tmp/autochess/battle-traits/`，均通过纯色、透明和近黑拒绝检查并逐张打开确认。最终验证：`pnpm autochess:test`（`149/149`）、`pnpm exec tsc --noEmit --incremental false`、HUD ESLint、脚本语法、`git diff --check` 与 `pnpm run build` 通过；构建只保留仓库已有的 Browserslist 数据过期提醒。

## 2026-07-26 · 时停领域特效对象池崩溃修复

- [x] 根因确认：`syncChronospheres()` 把持续时停领域专用容器以伪造 `BattleEffect` key 写入通用 `effectViews`；通用同步下一帧将其回收到 `effectViewPool`，后续短命特效复用时因缺少 `effectViewParts` 而崩溃。
- [x] 持续时停领域改用独立 `chronosphereView` 生命周期，不再与通用短命特效共享 Map 或对象池。
- [x] 审计全部 Phaser 视图池；投射物池和特效池都在入池、出池两端校验对应 WeakMap 元数据，异类容器会被销毁而不是复用。
- [x] 新增静态回归，禁止恢复 `rift-chronosphere` 伪 key / `as unknown as BattleEffect`，并锁定两个对象池的元数据防线。
- [x] 使用系统 Chrome 复跑北欧时停专项并检查截图、文本状态、画布、DOM 与控制台。
- [x] 运行完整自走棋测试、目标 ESLint 和最终差异检查。
- 系统 Chrome 专项使用 seed 22 捕获北欧时停弹幕飞行与落地：落地状态为半径 `108`、剩余 `1.75s` 的持续领域；随后继续验证另一场声束范围特效，对象池跨特效类型复用正常，控制台错误为空，Canvas 为 `1440×853`。
- 三张专项截图均完成像素抽样和人工检查，颜色数为 `2424—2998`，透明与近黑比例均为 `0`；画面中的飞行弹、时停领域和声束范围圈清晰可见。
- 最终验证：`pnpm exec tsc --noEmit --incremental false`、专项静态回归 `3/3`、完整 `pnpm autochess:test`（`150/150`）、目标场景 ESLint、测试脚本语法、`git diff --check` 与 `pnpm build` 全部通过；构建只保留既有的 Browserslist/Baseline 数据过期提醒。仓库主 `verify-autochess.cjs` 仍在已知的首战 `result` 等待处超时。

## 2026-07-26 · 战斗羁绊栏轻量化

- 按用户反馈移除双方羁绊栏的常驻底板、描边、阴影与模糊；展开态改为 18px 高的透明文字标签，仅保留羁绊色细下划线和色点，hover / focus 时才临时出现弱背景。
- 阵营标题、标签间距、人数档位字号和中央收起按钮同步缩小；横向滚动条隐藏但仍保留滚轮、触摸横滑与键盘交互。收起态宽度进一步压缩，手机仍默认收起。
- 系统 Chrome 专项改用 8 人、6 个已激活羁绊的密集阵容，覆盖桌面、390×844 竖屏与 1138×640 横屏；六张截图均通过非纯色、非透明、非近黑检查并逐张确认，展开栏高度约 20—22.5px，无页面横向溢出，说明层、收起/展开和双方数据均正常。
- 最终验证：`pnpm autochess:test`（`151/151`）、`pnpm exec tsc --noEmit --incremental false`、HUD ESLint、专项脚本语法与 `git diff --check` 全部通过。

## 2026-07-26 · 备战预警文案简化

- 动态首领说明统一为“首领预警：敌人非常强大，请倾尽所有资源应对，否则可能会失败。”；动态精英说明改为直接提示升级阵容与调整站位。
- 普通无限与地狱无限的备战说明只提示敌人会持续变强；地狱顶部进度提示和入口 toast 同步移除利息、连胜、赏金、复投等内部计算说明。
- 完整经济公式继续保留在玩法说明中，避免备战页重复展示策划计算过程。
- 数据回归禁止普通、精英、首领和地狱动态波次说明重新出现“利息/连胜/赏金/复投/总价值”；toast 回归锁定三类短预警。
- 系统 Chrome 复现用户截图对应的第 21 战首领页，说明为单行短警告；第 17、32 战、固定精英、桌面/移动端及玩法说明共 8 张截图均完成有效性检查和人工检查，控制台与失败请求为空。
- 验证：专项 `113/113`、完整 `pnpm autochess:test`（`151/151`）、`pnpm exec tsc --noEmit --incremental false`、目标源码 ESLint、验证脚本语法与 `git diff --check` 通过。

## 2026-07-26 · 礼墨加入贪吃羁绊

- 礼墨 `traits` 新增 `gluttony`，保留原有神秘/远程定位；现有贪吃机制会自动将她标记为贪吃成员并提供成长与治疗脉冲。
- 数据回归更新礼墨羁绊列表；贪吃战斗回归加入礼墨并确认 `gluttonyHolder` 与成长层数正常增加。
- 验证：礼墨数据专项 `18/18`、包含贪吃的战斗专项 `95/95`、`pnpm exec tsc --noEmit --incremental false` 与 `git diff --check` 通过；浏览器画面由用户后续自行验证。

## 2026-07-26 · 浣熊店员稳态治疗回能

- 用户反馈浣熊店员仍存在受击回能异常；确认其生存型治疗技能仍使用旧 `bulwark` 模板：`初始 15/90、自动 0、攻击 +6、受击 +20`，多人集火会反向加速自我治疗。
- 浣熊店员现改为稳态回能：`初始 25/100、每秒 +8、攻击 +6、受击 +3`；治疗量、70% 生命触发阈值、属性与羁绊均未调整，技能说明同步明确自动为主、攻击与受击为辅。
- 数据守卫从 11 个重复护盾角色扩展为全部重复生存技能角色，禁止治疗或护盾技能重新接入高受击回能。
- 新增一星浣熊店员自然三打一回归，要求仍能正常治疗至少一次、最多两次并最终被击败；成熟羁绊计时夹具同步固定敌方 `baseAttack=0`，避免动态属性刷新恢复伤害后提前结束测试。
- 验证：浣熊/数据/成熟专项 `3/3`、完整 `pnpm autochess:test`（`151/151`）、`pnpm exec tsc --noEmit --incremental false`、目标源码 ESLint与 `git diff --check` 通过。按用户要求停止后续浏览器与截图验证，由用户自行验收画面。

## 2026-07-26 · 无限后段可见压力重做（进行中）

- 策划方向确定：敌方不接玩家天赋；后期压力改由可见人口、星级和主题编队承担。
- 人口曲线已调整为第 17—21 战 10 人、第 22—25 战 11 人、第 26—28 战 12 人、第 29—31 战 13 人、第 32 战 14 人；此后每两战增加 1 人且不设硬上限。
- 无限首领开始轮换“时停合唱团 / 终场续航团 / 高费压制团”，分别使用多名北欧魔法师、蛙梓和梨安等核心；首领仍只保留一个暴君本体，并继续激活正常羁绊。
- 超过 10 人的敌军使用五行出生阵型；升星循环已随实际敌方人数扩展，所有新增单位都能正常消耗预算升至三星。
- 新增人数后，第 31 战预计场面赏金由 30 增到 39；第 32 战预算递推仍完整包含 20 利息、连胜、理财和上一战场面结算金。
- 备战敌情区与移动端顶部直接显示敌方实际人数；`render_game_to_text()` 同步公开 `enemyCount` 和 `formationTheme`，头像预览数量受限时也不会误判总人口。
- 战斗平衡抽查：10 名三星五费棋子并携带 5 项强天赋时，第 40、41 战仍可通过，第 42、43 战会失败，第 46 战高费压制团也会明确终结，毕业阵容不会再连续很多回合只看表演。
- 系统 Chrome 已验证第 17、21、22、32 战和移动端敌军人数显示；截图均通过纯色、透明与近黑拒绝检查并逐张打开确认，控制台与失败请求为空。
- 最终验证：完整自走棋套件 `156/156`、TypeScript、目标源码 ESLint、脚本语法和 `git diff --check` 全部通过。

## 2026-07-26 · 桌面商店下半区详情可见性

- 用户反馈桌面商店下方单位的悬浮说明会越出视口，底部内容无法看全。
- 根因是五张商店卡共用 `top: -5px` 向下展开；现将桌面商店第三至第五张详情改为从卡片底边向上对齐，并同步把指向箭头移到右下侧。前两张和移动端商店保持原布局。
- 新增静态回归锁定下半区详情与箭头的向上定位，并新增 `scripts/verify-autochess-shop-tooltips.cjs` 系统 Chrome 专项；在 `2048×1000` 视口逐一检查五张卡片，并将第五张固定为用户截图中的露蒂丝。
- 第三至第五张详情底边分别为约 `614 / 701 / 787px`，均完整位于视口内；Canvas 为 `2048×948`，`render_game_to_text()`、DOM、控制台和失败请求交叉检查通过。
- `third-card-detail.png` 与 `fifth-card-detail.png` 均通过纯色、透明和近黑拒绝检查并逐张打开确认，技能、回能说明和箭头均无裁切。
- 最终验证：完整 `pnpm autochess:test`（`157/157`）、`pnpm exec tsc --noEmit --incremental false`、验证脚本语法和 `git diff --check` 通过。主 `verify-autochess.cjs` 仍复现既有首战 `result` 等待超时，专项系统 Chrome 流程通过。

## 2026-07-27 · 米米加入杂谈羁绊

- 米米（`nagisa`）新增现有 `mystic` 羁绊，显示名称为“杂谈”；她现在拥有 `川妹 / 偷袭 / 杂谈`。
- 未新增重复羁绊定义，直接复用杂谈已有的开战能量与施法返还机制。
- 验证：数据回归 `20/20`、完整 `pnpm autochess:test` `157/157`、TypeScript 与 `git diff --check` 通过；本轮未启动浏览器专项。

## 2026-07-27 · 羁绊最高档可达性审计

- 用户反馈 `27期` 只有 5 名成员，却仍使用 `2/4/6` 档；按可购买棋子池审计后，确认 `27期` 为 5 人、`偷袭` 为 5 人、`深夜档` 为 4 人、`成熟` 为 5 人，四者的 6 人档都无法由玩家阵容达到。
- 四个羁绊统一收敛为 `2/4` 两档，并删除不可达的第三档效果；其他羁绊的最高阈值均不超过可购买成员数，保持不变。
- 新增数据不变量回归：自动统计每个羁绊的唯一可购买成员数，要求最高阈值可达且阈值/效果数组长度一致；同时锁定四个本轮修正羁绊的成员数与 `2/4` 档位。
- 系统 Chrome 图鉴专项确认 `27期`、`深夜档`、`偷袭`、`成熟` 均只显示 `2 名 / 4 名`；Canvas `1440×853`，截图 `.tmp/autochess/trait-thresholds/trait-thresholds-codex.png` 通过非纯色/透明/近黑检查，控制台和失败请求为空。
- 验证：数据回归 `21/21`、完整 `pnpm autochess:test` `158/158`、`pnpm exec tsc --noEmit --incremental false`、`git diff --check` 和羁绊布局专项通过；主 `verify-autochess.cjs` 仍在既有首战 `result` 等待处超时。

## 2026-07-28 · 狍子偶像近距离捏捏摸摸

- 狍子偶像（`lovely`）的技能触发从 `engage` 改为 `offenseInRange`，远距离满能量时会先接近目标，不再远程发动“捏捏摸摸”。
- 技能说明同步标注需要接近敌人；战斗回归覆盖远距离不施法与进入近距离后正常 channel。
- 验证：专项 `119/119`、完整 `pnpm autochess:test` `158/158`、`pnpm exec tsc --noEmit` 与 `git diff --check` 通过；本轮未启动浏览器专项。

## 2026-07-28 · 北欧魔法师升费与时停回能限制

- 北欧魔法师从 2 费升为 3 费，基础属性由 `142/21/6` 调整为 `165/25/8`（生命/攻击/护甲），保留脆皮远程控场定位；费用池由 `7/10/9/9/5` 调整为 `7/9/10/9/5`，因此没有额外把高费棋降到 2 费。
- 时停球落地后，北欧魔法师的能量从满值按星级持续时间线性降至 0；领域存在期间自动、普攻、受击和羁绊返还能量均无效，投射物飞行与领域持续期间也不能重复施法。
- 领域寿命与施法者能量同步，能量提前归零或施法者阵亡都会立即结束领域；结束帧保持明确的 0 能量，下一帧才恢复正常回能。
- 第 3、4 战因升费增加的敌军价值已用波次系数抵消，预算继续保持 `9/18`，不暗中提高前期教学难度。
- 无限首领“时停合唱团”的 6 人模板由 3 个北欧位降为 1 个；第 21 战实际北欧数量从 6 个降为 2 个，普通“深夜声场”和“高费压制团”的单北欧模板保持不变。
- 系统 Chrome 曾捕获 seed 110 的时停落地、中段与结束帧：能量 `54/100` 时领域剩余 `0.97/1.8s`，结束帧能量为 `0` 且领域消失；用户随后决定自行继续试玩，不再追加浏览器验证。
- 验证：北欧/关卡专项、完整 `pnpm autochess:test`（`158/158`）、`pnpm exec tsc --noEmit --incremental false`、验证脚本语法和 `git diff --check` 全部通过。

## 2026-07-28 · 战斗羁绊字号与横向浏览

- 战斗羁绊主文字从 8px 降到 7px，人数从 7px 降到 6px，标签高度从 18px 收到 16px；阵营标题同步降到 7px，说明浮层字号保持不变。
- 双方标签行使用备战羁绊栏同款交互：滚轮纵向输入按 `0.35` 倍转换为横向位移，鼠标移动超过 6px 后才开始拖动，并抑制拖动后的误点击；非 passive 原生监听会阻止页面纵向滚动，触屏继续使用原生横滑。敌方使用安全右对齐，少量标签靠右，溢出时不会产生不可达的左侧内容。
- 系统 Chrome 使用 10 人、16 条我方激活羁绊验证：可视宽 519px、内容宽 1009px，滚轮输入 `180` 从 `scrollLeft=0` 平滑移到 `63`；4px 小幅移动保持在 `0`，超过阈值的拖动可到末端 `490`，最后一个标签完整可见。
- 额外使用 800×700 窄桌面、第 17 战 9 条敌方羁绊验证敌方滚轮与拖动；桌面、窄桌面、390×844 竖屏和 1138×640 横屏共八张截图均通过有效性检查并逐张确认，控制台与失败请求为空。
- 最终验证：`pnpm autochess:test`（`158/158`）、`pnpm exec tsc --noEmit --incremental false`、HUD ESLint、专项脚本语法与 `git diff --check` 全部通过。

## 2026-07-28 · 主动技能与偷袭目标分工

- 根因确认：北欧魔法师和饼干岁共用同一个固定半径密集选点；多个偷袭成员也都选择最后排排序后的第一个敌人，因此容易把技能和跳跃重复压在同一目标上。
- 北欧时停改为按当前星级的实际领域半径统计可覆盖人数；覆盖人数相同时选择更紧凑的群心，不再使用与技能范围脱节的固定 125 半径。
- 饼干岁改为锁定与自身欧氏距离最远的敌人，冲到其身旁后再结算原有范围伤害、眩晕和护盾；技能说明同步更新。
- 偷袭只在敌方最深一列内选人，并优先选择尚未被其他偷袭成员预订、且与自己纵向路线更接近的目标；多人时会分流，最后排目标不足时才允许重叠。落地后短暂保持该目标，避免立刻转头。
- 新增目标策略回归，覆盖北欧三人密集区、饼干岁孤立最远敌人、双偷袭分散锁定最后排且不得为了分流跳前排。
- 系统 Chrome 专项 `scripts/verify-target-strategies.cjs` 使用 seed 206/207 验证：北欧落点 `(674,292)` 覆盖 3 人，饼干岁锁定另一侧最远敌人；两名偷袭分别锁定 `e-6-0` / `e-6-1` 并沿上下两条路线跳跃。三张 1440×900 截图通过非纯色、非透明、非近黑检查并逐张确认，Canvas 为 1440×853，文本状态、DOM、控制台和失败请求均正常。
- 最终验证：完整 `pnpm autochess:test`（`159/159`）、`pnpm exec tsc --noEmit --incremental false`、`pnpm build`、核心数据/引擎 ESLint、专项脚本语法与 `git diff --check` 全部通过。

## 2026-07-28 · 偷袭改为最后排集火

- 用户复议后撤销“偷袭分散目标”：偷袭的战术价值应是绕后快速减员，因此多个成员需要集中火力。
- 最终规则为所有偷袭成员共同锁定敌方最深一列中生命比例最低的目标；比例相同时比较当前生命值和稳定 ID，保证全队得到同一结果。
- 多名成员仍通过现有落点碰撞检查落在目标不同侧面，避免棋子实体完全重叠；落地后短暂保持集火目标。
- 回归改为验证双偷袭同时锁定同一个虚弱后排且不改跳前排；羁绊说明同步改为“集中跃向最后排中最虚弱的目标”。
- 系统 Chrome seed 207 确认两名偷袭都锁定 `e-6-0`，落点分别为 `(964,280)` 与 `(964,218)`；中段截图 `.tmp/autochess/target-strategies/assassin-backline-focus.png` 已通过像素有效性检查并打开确认，文本状态、Canvas、控制台和失败请求正常。
- 验证：完整 `pnpm autochess:test`（`159/159`）、`pnpm exec tsc --noEmit --incremental false`、`pnpm build`、核心数据/引擎 ESLint、专项脚本语法与 `git diff --check` 通过。

## 2026-07-28 · 商店费用概率降档

- 3—10 本商店概率改为更平滑的低高费过渡；6 本起不再突然出现 40% 的 4/5 费，10 本由 `0/4/12/32/52` 收敛为 `5/10/20/40/25`。
- 满级保留 5% 一费概率，继续支持低费核心追三；五费单卡在 5 格商店中的每次刷新期望约为 0.25 张，仍可追三星，但成本明显高于旧表。
- 战术图鉴“商店概率”页继续展示 3—10 本完整表格，并明确说明满级仍保留低费棋；表头和留白同步压缩，390px 手机可同时看到全部八列。
- 数据回归锁定完整概率表、每级合计 100%、满级一费必须大于 0%，以及满级 4/5 费合计不超过 65%。
- 系统 Chrome 专项 `scripts/verify-autochess-shop-odds.cjs` 覆盖 `1440×900` 与 `390×844` 图鉴；八行 DOM 数据、Canvas 尺寸、控制台和失败请求均通过，两个截图通过纯色、透明与近黑拒绝检查并逐张打开确认。
- 最终验证：`pnpm autochess:test`（`159/159`）、`pnpm exec tsc --noEmit --incremental false`、目标 ESLint、专项脚本语法、`git diff --check` 与 `pnpm build` 全部通过；构建仅保留既有的 Browserslist/Baseline 数据过期提醒。

## 2026-07-28 · 角色特色、施法距离与贪吃成长重做

- 数据层新增明确的 `abilityRange`：目标型主动技能只会选择施法距离内的单位，图鉴会显示技能距离并按费用升序排列。
- 七海大鲨鱼升为 5 费近战，主动改为有限距离内最远目标冲锋；落地后持续耗能、提升护甲、嘲讽近敌，并在受击时发射带短眩晕的 `⛏️` 反击。
- 饼干岁降为 4 费，施法距离限制为 300，基础属性、落地伤害、范围、眩晕与护盾均已下调。
- 病院坂灵改为复活技能：至少 5 具可用尸体才会施放，按星级最多复活 5/7/10 名己方或敌方阵亡单位，以半血幽灵加入施法者队伍；幽灵与已消费尸体不能循环复活。
- 贪吃定时成长和击杀都会叠层；每层增加 6% 攻击力并同步扩大真实碰撞半径，原“不影响体积”说明已删除。
- 进攻、突进、治疗、护盾和持续治疗/补能均按有限技能距离选择目标；相关“全队/全场”旧文案同步改为“施法距离内”。
- 新增 `scripts/verify-character-reworks.cjs` 系统 Chrome 专项：40 个图鉴条目费用升序；七海目标距离 `390.8 ≤ 430`，持续形态能量 `87.35/100`、护甲 `+42`、嘲讽和矿镐反击正常；4 层贪吃令半径 `26 → 28.6`、攻击 `47.15 → 58.466`；三星 Rei 复活 10 名半血己方幽灵并消费 10 具尸体。
- 五张 `1440×900` 专项截图均通过非纯色、非透明、非近黑检查并逐张打开确认；Canvas 为 `1440×853`，`render_game_to_text()`、图鉴 DOM、控制台和失败请求交叉验证正常。
- 最终验证：完整 `pnpm autochess:test`（`162/162`）、`pnpm exec tsc --noEmit --incremental false`、目标源码 ESLint、专项脚本语法、`git diff --check` 与 `pnpm build` 全部通过；构建仅保留既有的 Browserslist/Baseline 数据过期提醒。仓库主 `verify-autochess.cjs` 仍在既有首战 `result` 等待处超时，专项系统 Chrome 流程通过。

## 2026-07-29 · 椰子栞、饼干岁与小岁鸟技能换位重做

- 椰子栞由 2 费远程升为 3 费近战，基础属性提高到 `240 生命 / 29 攻击 / 18 护甲`；“海獭冲击”会冲向 300 距离内最远敌人，落地造成范围伤害、短暂眩晕并获得自身护盾。
- 饼干岁保留 4 费近战定位，主动改为“暖男回复”：冲向 360 距离内最虚弱友军，为其治疗、添加护盾，并无伤击退沿途与落点小范围敌人；专属回能为 `36/90` 初始、每秒 `+18`、攻击 `+4`、受击 `+2`、施法返还 `10`。
- 小岁鸟改为近战，“连续肘击”会连续发动 3 次冲撞，每段对沿途敌人造成伤害、击退与短暂眩晕；文本状态新增剩余肘击次数，便于验证连续技中间态。
- 椰子栞升费涉及的第 3、12 战固定波次系数已同步抵消，避免角色费用调整暗中提高关卡预算；二星七海加三星饼干岁的七人高存款阵容仍能通过第 12 战精英，但会被第 16 战首领终结。
- 系统 Chrome 专项使用确定性时间推进：椰子栞一次命中并眩晕 3 人；饼干岁完成弱者锁定、途中击退、落地治疗护盾与范围击退且不造成伤害；小岁鸟完成三段冲撞并击退敌人。
- 九张 `1440×900` 专项截图均通过非纯色、非透明、非近黑检查并逐张打开确认；Canvas 为 `1440×853`，`render_game_to_text()`、控制台和失败请求交叉验证正常。
- 最终验证：完整 `pnpm autochess:test`（`163/163`）、`pnpm exec tsc --noEmit --incremental false`、目标源码 ESLint、专项脚本语法、`git diff --check` 与 `pnpm build` 全部通过；构建仅保留既有的 Browserslist/Baseline 数据过期提醒。

## 2026-07-29 · 固定关费用与高费棋出场审计

- 用户截图确认第 5 战“毛茸茸团建”仍沿用七海旧治疗定位；虽然七海升到 5 费时已将系数改为 `sqrt(16/10)`、有效预算仍为 16，但旧说明和过早投放 5 费冲锋核心都不再符合当前角色设计。
- 第 5 战将七海大鲨鱼替换为 4 费饼干岁，说明同步改为治疗、护盾与集火提示；原始阵容费用由 10 降为 9，系数改为 `sqrt(16/9)`，有效预算继续保持 16。
- 七海只保留在第 7 战“五系禁卫”，作为第 8 战精英前有意展示的 5 费核心。
- 新增固定关费用回归：逐关锁定 1—8 战原始总费用 `[3,3,12,9,9,12,16,10]`、最高单卡费用 `[2,1,5,3,4,5,5,5]`、有效预算 `[2,5,9,18,16,17,21,32]`，并显式白名单化所有 5 费棋的固定关出场位置。
- 系统 Chrome 实际生成第 5 战阵容为 `绒绒的狗 ×2 / 饼干岁 / 雅吨 / 兔子射手`；引擎与 `render_game_to_text()` 一致，截图通过非纯色、非透明、非近黑检查并人工确认无七海、无 HUD 裁切，控制台和失败请求为空。
- 最终验证：完整 `pnpm autochess:test`（`164/164`）、`pnpm exec tsc --noEmit --incremental false`、目标源码 ESLint、专项脚本语法、`git diff --check` 与 `pnpm build` 全部通过；构建仅保留既有的 Browserslist/Baseline 数据过期提醒。

## 2026-07-29 · 星汐山猪冲阵持续冲锋重做

- 为避免与椰子栞“海獭冲击”同质化，星汐不再冲向密集区后结算范围伤害、眩晕和自身护盾。
- 新“山猪冲阵”施放后把能量拉满，并在约 4.8 秒内持续耗至 0；期间屏蔽自动、普攻、受击和羁绊回能，也无法普攻。
- 冲锋以 235 速度持续前进，最大转向速度为每秒 1.2 弧度；四侧战场边缘会反射对应方向分量，保留低转向、高惯性的山猪冲锋感。
- 冲锋路径碰撞敌人时不造成伤害，只造成 78 距离击退与 0.42 秒眩晕；目标离开接触范围后可以被同一次冲锋再次撞击。
- Phaser 表现新增冲锋形变、`冲` 状态、短尾迹、`撞飞` 与 `反弹` 反馈；`render_game_to_text()` 新增冲锋方向和累计命中数。
- 数据与战斗回归覆盖持续耗能、回能锁定、禁止普攻、低转向、无伤撞飞、重复碰撞、边缘反弹和能量耗尽退出；完整 `pnpm autochess:test` 为 `167/167`。
- 系统 Chrome 专项 `scripts/verify-seki-charge.cjs` 验证：冲锋中段能量 `91.67/100`，首次撞击将目标推至 `x=413.3`、剩余眩晕 `0.27s` 且伤害为 `0`；右边缘反弹后方向 `x=-0.998`，耗能结束后状态与方向清零。
- 两张 `1440×900` 整页截图通过非纯色、非透明、非近黑检查并逐张打开确认；Canvas 为 `1440×853`，文本状态、控制台与失败请求交叉检查正常。
- 最终验证：`pnpm autochess:test`（`167/167`）、`pnpm exec tsc --noEmit --incremental false`、目标源码 ESLint、专项脚本语法、`git diff --check` 与 `pnpm build` 全部通过；构建仅保留既有的 Browserslist/Baseline 数据过期提醒。主 `verify-autochess.cjs` 仍在既有首战 `result` 等待处超时。

## 2026-07-29 · 病院坂灵Rei回能与复活削弱

- Rei 改用独立幽灵回能：开场 `25/100`，仅每秒自动回复 5 点，攻击与受击均不回能；首次从开场状态约 15 秒满能，施法清空后约 20 秒再次满能。
- 复活人数改为一至三星 `2/3/5`；发动门槛同步使用当前星级人数，只统计施法距离内尚未被复活的尸体。差一具时不会施法或消耗能量，出现第 `2/3/5` 具后才发动。
- 幽灵复活生命由原单位最大生命的 50% 降为 25%，己方和敌方尸体仍会统一转为施法者阵营。
- 多 Rei 原本已通过 `corpse.consumed` 排除已用尸体；现在先原子式标记本次全部尸体、再生成幽灵，并增加双三星 Rei 连续消费两组各 5 具不同尸体的回归，明确保证不能重复复活。
- 系统 Chrome 专项显示“幽灵复活 · 5 名幽灵加入我方”，文本状态确认 `25` 初始能量、`5/0/0` 自动/攻击/受击回能、5 个幽灵均为 25% 生命；`rei-five-ghost-revival.png` 为有效 1440×900 截图并已打开检查，Canvas、控制台和失败请求正常。
- 最终验证：完整 `pnpm autochess:test`（`167/167`）、`pnpm exec tsc --noEmit --incremental false`、目标源码 ESLint、浏览器脚本语法、`git diff --check` 与 `pnpm build` 全部通过；构建仅保留既有的 Browserslist/Baseline 数据过期提醒。

## 2026-07-29 · 好笑姐姐近距离冷笑话短跳

- 好笑姐姐（`rift_stalker`）的“冷笑话”施法距离收紧为 `240`；距离外会继续接近目标，进入范围后才选择其中最远的敌人起跳。
- 原先直接调用 `relocateFighter()` 改写坐标、同帧结算伤害与护盾，现改为 `0.42s`、弧高 `64` 的真实短跳；跳跃途中不造成伤害也不获得护盾，落地后才结算原有 `1.4×` 伤害与 `12%` 最大生命护盾。
- 位移技能审计确认：好笑姐姐、椰子栞、七海、小岁鸟、滑跪、暖男回复、友谊、阿梓、沐霂等均使用引擎 `dash/jump/push` 运动状态；唯一保留直接重定位的是小猫拳明确设计的闪现起手，且已有出发、落点、轨迹与后续同步推进表现。
- 回归覆盖远距离不释放、近距离起跳、施法帧不瞬移、途中不提前结算、落地伤害/护盾，以及全能吸血等待落地伤害后触发；静态守卫收紧为只允许一处明确闪现。
- 系统 Chrome 专项使用安装的 Chrome channel：半空帧目标生命/护盾为 `99999 / 0`，落地后为 `99935.52 / 17.52`；`skill-cast-rift-stalker-midair.png` 与 `skill-cast-rift-stalker-landed.png` 均为有效 `1440×900` 整页截图，Canvas `1440×853`，已逐张打开确认抛物线、影子、落点圈、护盾与伤害反馈，控制台和失败请求为空。

## 2026-07-29 · 好笑姐姐改为 😂 冷笑话弹幕

- 本节取代上面的短跳方案：偷袭羁绊继续负责开战切入后排，主动技能不再重复位移；好笑姐姐会在 `240` 施法距离内向最远敌人发射短程 `😂` 弹幕，命中后才造成技能伤害和眩晕。
- 技能彻底移除护盾。基础防守面板回到 `146 HP / 8 护甲`，攻击提高到 `26`；专属“笑点回能”为开战 `40/100`、每秒 `+20`，让脆皮刺客能在阵亡前稳定打出第一次技能。
- 一至三星技能分别为 `2.7× / 3.1× / 3.6×` 攻击伤害和 `0.85s / 1.05s / 1.3s` 眩晕；弹速 `1200`、碰撞半径 `18`，兼顾短距离可读性与近战走位中的命中可靠性。
- Phaser 与 Canvas 表现均新增 `emoji_burst`：命中点的 `😂` 从 `0.62×` 放大到 `2.1×`，并在 `0.62s` 内渐隐；普通弹幕和命中特效使用 Emoji 字体，不影响其他投射物分支。
- 战斗边界回归确认：一星好笑姐姐可击败同面板一费脆皮兔子射手，但仍无法越费单挑一星二费雅吨；技能全过程护盾与护盾统计均保持为零。
- 系统 Chrome 专项：飞行帧目标为 `99999 HP / 0 眩晕 / 0 护盾`，命中后为 `99918.27 HP` 并带剩余短眩晕，`emoji_burst` 已生成；`skill-cast-rift-stalker-flight.png` 与 `skill-cast-rift-stalker-impact.png` 均为有效 `1440×900` 截图，Canvas `1440×853`，已逐张打开确认，无控制台错误或失败请求。
- 最终验证：`pnpm autochess:test`（`176/176`）、TypeScript、目标源码 ESLint、浏览器脚本语法、`git diff --check` 与 `pnpm build` 均通过；构建仅保留既有的浏览器数据过期提醒。
- 最终验证：完整 `pnpm autochess:test`（`169/169`）、`pnpm exec tsc --noEmit --incremental false`、目标源码 ESLint、浏览器脚本语法、`git diff --check` 与 `pnpm build` 全部通过；构建仅保留既有的 Browserslist/Baseline 数据过期提醒。

## 2026-07-29 · 雪烛技能盾与沐霂领舞救场

- 新增 3 费雪烛Yukisyo，使用官方公开头像与“赛博占卜师白虎神”定位；`78/100` 高初始能量让“八门镇式”较早主动释放，但不会在开战时自动触发。
- 八门镇式为施法距离 300 内友军添加持续 4 秒的独立技能盾，1—3 星数值为 `70 + 26% / 120 + 36% / 200 + 50%` 目标最大生命；重复获得取较高值并刷新时间，不无限叠加。
- 伤害入口新增攻击/技能来源分类：普通攻击绕过技能盾；主动技能、技能弹幕及其衍生灼烧优先消耗技能盾，再消耗通用盾和生命。战场使用紫色外环与“术”标记，并在 tooltip、统计和 `render_game_to_text()` 中分别展示技能盾。
- 沐霂由近战冲阵群伤/群盾改为远程单体救援；满能时按“场地控制 > 持续压制 > 普通硬控 > 35% 以下低血”选择一名友军，以 0.35 秒舞带轨迹拉到身后，落地治疗、添加通用盾、净化普通控制并打断持续压制。
- 时停没有减时或净化逻辑：沐霂自己处于时停时不能施法，被救目标的外部拉动可以继续，只有实际离开时停范围后才恢复；无危险目标时不消耗能量。
- 完整 `pnpm autochess:test` 为 `174/174`；TypeScript、目标源码 ESLint、`pnpm build` 与 `git diff --check` 通过。系统 Chrome 专项捕获雪烛技能盾、沐霂拉援中段与落地三张 `1440×900` 截图，均通过纯色、透明和近黑拒绝检查并逐张确认；Canvas 为 `1440×853`，文本状态、控制台和失败请求正常。

## 2026-07-29 · 果冻风纪满区逃生

- 找到公开的 `noir-hedgehog/PetViewer` 满区宠物素材：灰泽满 chibi 头加柔软虫形身体；战斗变身使用其 `assets/manqu/favicon.png`，本地来源记录在 `assets/autochess/ASSET_SOURCES.md`。
- 果冻风纪继续保持 2 费，删除原“30% 最大生命护盾 + 破盾 5 枚钢镚”；新主动“满区逃生”在满能且受击时触发，避免把护盾、输出、闪避、回血和位移全部叠在同一张低费棋上。
- 一至三星分别变身 `1.25/1.35/1.5` 秒；期间停止攻击和回能，增加 `105/115/125` 移速，获得 `55%/60%/65%` 闪避，每秒回复 `4%/5%/6%` 最大生命，并持续选择能最大化与最近敌人距离的逃跑方向。
- Phaser 在变身期间切换虫形贴图并加入轻微蠕动形变，技能结束立即恢复灰泽满头像与基础移速；图鉴补充全角色通用的技能星级成长展示。
- 战斗回归覆盖逃跑距离、逐帧回血、变身期不攻击/不回能、额外闪避、结束后恢复普攻，以及不再产生护盾和攻击弹幕。
- 系统 Chrome seed 2 验证：一星满区从 `x=500` 逃到 `x≈327`，变身中移速 `149`、能量保持 `0`、无护盾和弹幕，结束时累计回复约 `15/305` 生命并恢复移速 `44`；图鉴、普通回能、施法、持续形态和结束形态五张 `1440×900` 截图通过非纯色/透明/近黑检查并逐张确认，Canvas 为 `1440×853`，控制台与失败请求为空。
- 主 `verify-autochess.cjs` 首次浏览器尝试仍复现既有的首战 `result` 等待超时；专项系统 Chrome 流程通过。
- 最终验证：完整 `pnpm autochess:test`（`175/175`）、`pnpm exec tsc --noEmit --incremental false`、目标源码 ESLint、浏览器脚本语法、`git diff --check` 与 `pnpm build` 全部通过；五张专项截图已再次逐张打开复核。首次构建遇到并行任务争用 `.next/export`，并行构建结束后重试成功；构建仅保留既有的 Browserslist/Baseline 数据过期提醒。

## 2026-07-29 · 塔神“开挂”死亡转移

- 将塔神原“尖塔压顶”替换为“开挂”：主动施法只进入待触发状态，塔神死亡时才把增益转移给最近的存活队友。
- 一至三星分别提供攻击 `+45%/+65%/+90%`、护甲 `+25/+38/+55`、攻速 `+45%/+65%/+90%`、移速 `+45/+65/+90`，持续到本场战斗结束；同名增益不叠加，只保留更强档位。
- 战斗表现新增待触发“待挂”、继承后“挂”标记、金色脉冲光环、死亡连线与“哈哈哈我开挂了 这游戏怎么这么简单啊！”战场横幅。
- 已完成类型检查、目标源码 ESLint、测试脚本语法与相关数据/战斗回归 `137/137`；覆盖未发动死亡、最近目标、四项属性、同档不叠加和高星覆盖低星。
- 系统 Chrome 专项使用 `scripts/verify-tower-hack.cjs` 验证施法待触发和死亡继承：一星目标从攻击/护甲/攻速间隔/移速 `17.25/24/1.20s/40` 提升到 `25.01/49/0.83s/85`，远处队友未获得增益。
- `tower-hack-armed.png` 与 `tower-hack-transferred.png` 均为有效 `1440×900` 整页截图，Canvas `1440×853`；通过纯色、透明和近黑拒绝检查并逐张打开确认，文本状态、控制台和失败请求均正常。
- 主 `verify-autochess.cjs` 首次浏览器尝试仍在既有首战 `result` 等待处超时；专项系统 Chrome 流程通过。
- 最终验证：完整 `pnpm autochess:test`（`177/177`）、`pnpm exec tsc --noEmit --incremental false`、目标源码 ESLint、验证脚本语法、`git diff --check` 与 `pnpm build` 全部通过；构建仅保留既有的 Baseline/Browserslist 数据过期提醒。

## 2026-07-29 · 备战敌方部署图

- 备战敌情摘要新增“站位”入口，桌面位于敌军阵容旁，移动端位于敌军数量与价值旁；打开后使用模态战术图完整覆盖画布、商店与底部操作区。
- `enemyFormationPosition()` 成为敌方备战预览和真实战斗出生点的唯一坐标来源：10 人以内使用三行疏阵，超过 10 人使用五行密阵，避免界面预告与实战漂移。
- 部署图展示完整战场方向、敌方头像、费用和星级；悬停、键盘聚焦或触摸棋子会切换单位详情、基础属性、羁绊和当前星级技能。
- 弹层打开时 `R` 与 `Space` 不会穿透到刷新商店或开战；`Esc`、关闭按钮和点击遮罩都可关闭，`render_game_to_text()` 同步公开弹层开关及每名敌军的 `x/y/row` 站位。
- 系统 Chrome 专项 `scripts/verify-enemy-formation-preview.cjs` 使用第 32 战 14 人三列五行密阵验证桌面 `1440×900` 与移动端 `390×844`；两张截图通过纯色、透明与近黑拒绝检查并逐张打开确认，Canvas 分别为 `1440×852.75` 与 `390×844`，控制台和失败请求为空。
- 主 `verify-autochess.cjs` 首次浏览器尝试仍在既有首战 `result` 等待处超时；专项系统 Chrome 流程通过。完整 `pnpm autochess:test` 为 `179/179`，TypeScript、目标源码 ESLint、浏览器脚本语法、`pnpm build` 与 `git diff --check` 通过；构建仅保留既有的 Baseline/Browserslist 数据过期提醒。

## 2026-07-29 · 梨安终场谢幕视觉强化

- 不改梨安伤害、范围和回能数值，将原普通小型 `✦` AOE 投送改为专属“谢幕星”：起手增加聚光闪光，飞行阶段使用旋转星核、双层尾迹和脉冲外环。
- 落地新增“终场舞台”表现：双侧聚光、十二道放射舞台纹、同心圆与中心星芒同步出现，仍严格等到弹幕抵达后才结算和显示。
- 实际获得 15 点能量的友军会收到从落点发出的星辉连线，并显示环形回响与 `+15 能量`；技能距离外友军既不回能也不出现假反馈。
- Phaser 主渲染器与备用 Canvas 渲染器均支持 `finale_star`、`finale` 和 `energy_pulse`，新增战斗回归锁定起手、飞行、落地与范围内外反馈。
- 系统 Chrome 专项 `scripts/verify-lian-finale.cjs` 覆盖 `1440×900` 飞行/落地和 `390×844` 手机落地；三张截图通过纯色、透明与近黑拒绝检查并逐张打开确认，Canvas 分别为 `1440×853` 与 `390×844`，文本状态确认梨安落地后获得 15 能量，控制台和失败请求为空。

## 2026-07-29 · 七海矿镐弹道与命中特效

- 七海“凿凿冲击”的反击矿镐弹速由 `620` 降至 `320`，保留原伤害与 `0.24s` 短眩晕；典型近身反击现在会留下清楚可辨的飞行过程。
- 矿镐使用独立 `pickaxe` 弹幕样式；命中时在目标头顶生成金色撞击闪光，并让 `⛏️` 在 `0.72s` 内沿弧线向上翻转弹起。Phaser 与 Canvas 两套渲染均使用相同轨迹。
- 战斗回归锁定 `320` 弹速、命中伤害、眩晕、头顶特效位置和弹起 Emoji；静态渲染回归同时保留原 `😂` 命中特效并覆盖矿镐旋转分支。
- 系统 Chrome 分别在发射后 `160ms` 和命中弹起阶段截图；`nana-pickaxe-counter.png` 可见弹道中段矿镐，`nana-pickaxe-impact.png` 可见目标头顶闪光和弹起矿镐。两张 1440×900 截图通过非纯色、透明与近黑检查并已打开确认，Canvas、文本状态、控制台和失败请求正常。
- 最终验证：完整 `pnpm autochess:test`（`180/180`）、`pnpm exec tsc --noEmit --incremental false`、目标源码 ESLint、浏览器脚本语法、`git diff --check` 与 `pnpm build` 全部通过；构建仅保留既有的 Baseline/Browserslist 数据过期提醒。

## 2026-07-29 · “成熟稳重”开局加强

- 用户反馈“成熟稳重”只给成熟角色增加 6% 护盾，开局强度明显落后于其他协议。
- 根因不仅是数值偏低：原实现被包在成熟羁绊已激活的分支内，只有一名开局浣熊店员时效果实际为零，凑出羁绊后也只覆盖成熟成员。
- 改为所有友军开战获得 8% 最大生命护盾，与成熟羁绊成员盾独立叠加；初始金币 +2 保持不变。单成熟成员时也立即生效，两成熟成型后成员获得 10% 羁绊盾加 8% 开局盾。
- 新增战斗回归覆盖未激活羁绊时的全队 8% 护盾，以及激活后成员盾叠加；完整 `pnpm autochess:test` 为 `182/182`。
- 系统 Chrome 专项 `scripts/verify-mature-starter.cjs` 确认标题卡文案、第一战护盾和文本状态：浣熊店员为 `18.56/232`、兔子射手为 `12/150`，均为 8%；两张 1440×900 截图通过纯色、透明和近黑拒绝检查并逐张打开确认，Canvas 为 1440×853，控制台与失败请求为空。
- 主 `verify-autochess.cjs` 仍在既有首战 `result` 等待处超时；专项系统 Chrome 流程通过。最终验证：`pnpm exec tsc --noEmit --incremental false`、目标 TypeScript ESLint、脚本语法、`git diff --check` 与 `pnpm build` 均通过；构建仅保留既有的 Baseline/Browserslist 数据过期提醒。

## 2026-07-29 · 南町慢速烟头连投

- 南町“烟头烫屁股”由单颗远端范围投送改为依次扔出 3 枚烟头；发射间隔 `0.16s`、弹速 `260`，明显慢于普通远端技能的约 `620`。
- 三枚烟头优先分配给敌人最密集处附近的不同目标，敌人不足时循环投掷；每枚命中造成 `0.9×` 攻击伤害并施加 `0.55×` 攻击量的 3 秒灼烧。
- 移除旧版范围伤害与 `0.5s` 眩晕，改为弹体真实碰撞后逐枚结算；Phaser 与备用 Canvas 均新增 `cigarette` 样式，以放大的 `🚬`、四段漂移烟雾和命中“灼烧”标签强化辨识度。
- 战斗回归覆盖 3 枚、`0/0.16/0.32s` 延迟、`260` 弹速、伤害、灼烧、无眩晕和不再走远端 AOE 投送分支；数据与静态渲染回归同步锁定新文案和两套渲染。
- 系统 Chrome 专项 `scripts/verify-nightin-cigarettes.cjs` 捕获桌面/手机飞行帧与桌面命中帧；三张截图通过纯色、透明和近黑拒绝检查并逐张打开确认，文本状态显示 3 个灼烧目标、至少 2 个实际掉血目标且眩晕均为 0，Canvas 为 `1440×853`，控制台和失败请求为空。
- 最终验证：完整 `pnpm autochess:test`（`183/183`）、`pnpm exec tsc --noEmit --incremental false`、目标源码 ESLint、专项脚本语法、`git diff --check` 与 `pnpm build` 全部通过；构建仅保留既有的 Baseline/Browserslist 数据过期提醒。

## 2026-07-29 · 备战双方部署图与固定详情高度

- 原敌方部署图升级为“双方部署图”：同时显示我方当前场上棋子与敌方本轮阵容，青色代表我方、红色代表敌方；双方单位均可悬停、聚焦或点击查看详情，我方属性使用当前实际战斗加成口径。
- 我方预览与真实战斗出生共用 `playerFormationPosition()`，`render_game_to_text()` 也为场上棋子公开 `x/y/row`；备战席棋子不生成虚假的场上坐标。
- 弹窗在桌面、手机横屏和手机竖屏均使用稳定高度；详情的身份、属性、羁绊和技能区域改为固定网格，长羁绊或技能文本在内部滚动，不再因 hover 不同棋子改变整个弹窗高度。
- 中轴说明沿战场分界线纵向排列，不遮挡双方最前排棋子；桌面与移动端最终截图已逐张打开确认，双方标记、详情内容和页脚图例均清晰可读。
- 系统 Chrome 专项使用 4 名我方棋子和第 32 战 14 名敌军验证；连续 hover 三名不同敌军时弹窗高度均为 `670.5px`。桌面 `1440×900` 与移动端 `390×844` 截图通过纯色、透明和近黑拒绝检查，控制台与失败请求为空。
- 主 `verify-autochess.cjs` 仍在既有首战 `result` 等待处超时；专项流程通过。最终验证：完整 `pnpm autochess:test`（`183/183`）、TypeScript、目标源码 ESLint、专项脚本语法、`git diff --check` 与 `pnpm build` 全部通过；构建仅保留既有的 Baseline/Browserslist 数据过期提醒。

## 2026-07-30 · 塔神改为近战

- 为让塔神更容易进入前线阵亡并触发“开挂”转移，将攻击类型由远程改为近战，攻击距离由 `225` 收紧到标准近战的 `52`；保留原 `238 生命 / 18 护甲 / 48 移速`，不额外补坦度。
- 数据与战斗生成回归固定 `melee / 52`；新增行为回归确认塔神在远距离不会隔空普攻，会主动接近敌人并在进入近战范围后造成伤害。
- 系统 Chrome 专项在运行态确认 `attackType = melee`、`range = 52`，同时“待挂”、死亡转移、最近队友四项增益和喊话链路均保持正常；两张 `1440×900` 截图通过纯色、透明和近黑拒绝检查并逐张打开确认，Canvas、控制台和失败请求正常。
- 主 `verify-autochess.cjs` 仍在既有首战 `result` 等待处超时；专项流程通过。最终验证：完整 `pnpm autochess:test`（`184/184`）、TypeScript、目标源码 ESLint、相关脚本语法、`git diff --check` 与 `pnpm build` 全部通过；构建仅保留既有的 Baseline/Browserslist 数据过期提醒。

## 2026-07-30 · 梨安终场谢幕范围与瞬发节奏修正

- 终场谢幕不再按梨安自身的施法距离给友军回能；伤害、15 点回能和回能发光统一使用落点中心 `140` 半径，落点外友军不会获得能量、连线或环形脉冲。技能说明同步改为“落点范围内”。
- 落地舞台寿命由 `1.18s` 缩短到 `0.58s`，回能脉冲由 `0.92s` 缩短到 `0.46s`，落点连线由 `0.72s` 缩短到 `0.36s`，中心爆光由 `0.52s` 缩短到 `0.32s`；`0.7s` 后所有终场落地视觉均已清空。
- 战斗回归明确摆放一名落点内友军和一名“梨安附近但落点外”友军，锁定只有前者回能与发光；完整 `pnpm autochess:test` 为 `184/184`，TypeScript 与 `git diff --check` 通过。
- 系统 Chrome 专项改为按真实逐帧时序推进弹幕，并覆盖飞行、落地瞬间、`350ms` 衰减、`700ms` 清空及手机落地。五张截图均通过纯色、透明和近黑拒绝检查并逐张打开确认；Canvas 为桌面 `1440×853`、手机 `390×844`，控制台与失败请求为空。

## 2026-07-30 · 大文件职责拆分

- 为自走棋数据、引擎、Phaser 渲染器和 DOM HUD 建立明确模块边界，并保留 `gameData.ts` / `gameEngine.ts` 兼容入口。
- `AutoChessEngine` 将状态、随机数、阵容、进度、战斗创建、伤害结算、技能与投射物系统迁出；入口由 6,226 行降至约 3,100 行。
- `RiftLineScene` 删除 React HUD 已接管的旧 Phaser UI，并迁出棋子、投射物、技能效果和召唤物视图；场景由 3,424 行降至约 2,000 行。
- `RiftHud` 拆为羁绊、敌方部署、商店和移动端面板；武侠页面将世界创建、片段结果归并和回合抽签从 React hook 中迁出。
- 删除无运行时入口的旧 Canvas fallback；静态测试改为读取实际模块，不再让死代码仅为源码正则断言存活。
- 架构说明与后续拆分触发条件见 `docs/AUTOCHESS_MODULE_SPLIT.md`。
- 最终验证：`pnpm exec tsc --noEmit --incremental false`、目标源码 ESLint、完整 `pnpm autochess:test`（`184/184`）、`git diff --check` 与 `pnpm build` 通过。
- 已将主 `verify-autochess.cjs` 改为点击 DOM HUD 的“开始战斗”按钮；系统 Chrome 完整流程覆盖首战胜利、次战失败、强化选择、第三战备战、全屏与移动端，控制台错误为空。17 张截图均通过非纯色、透明与近黑拒绝检查，并通过汇总图和关键原图人工复核。

## 2026-07-30 · 贪吃改为生命与体型成长

- 贪吃不再提高攻击力，深夜档保留为战斗内攻击成长羁绊；贪吃改为最多 8 层饱腹，每层增加 `2.5%` 最大生命和 `4%` 体型，满层合计 `+20%` 最大生命、`+32%` 体型。
- 定时进食仍每 3 秒叠层；亲手击杀可额外叠层，但同一棋子的击杀叠层有 3 秒冷却。增加最大生命时同步补上等额当前生命，避免成长被表现成掉血。
- 2 人档每次为成员回复 `2.5%` 最大生命；4 人档改为成员 `3.5%`、全队非成员 `1%`，成员满 8 层时获得一次 `10%` 最大生命护盾。
- 战场名字持续显示 `饱x/8`，`render_game_to_text()` 同步公开当前层数、上限和击杀叠层冷却。
- 按用户要求，本轮未运行测试、构建、开发服务器或浏览器验证，以免干扰正在进行的并行重构。
- 并行重构中的 `scripts/tests/autochess-battle-spawn.test.mjs` 仍有旧版“贪吃提高攻击”的断言；本轮没有修改该测试文件，待重构收束后按新生命成长规则同步。

## 2026-07-30 · 满区大幅降低闪避并提高回血

- 根据实战反馈，将满区的生存预算明确转向肉眼可见的回复：一至三星闪避由原始 `55%/60%/65%` 降为 `20%/25%/30%`，每秒回复由 `4%/5%/6%` 提高为 `20%/22%/24%` 最大生命。
- 持续时间仍为 `1.25/1.35/1.5` 秒，因此总回复由 `5%/6.75%/9%` 提高为 `25%/29.7%/36%`；撤离移速、停止攻击/回能和受击触发规则不变。
- 系统 Chrome seed 2 实测一星满区从 `205/305` 回复到 `281/305`，实际回复 76 点（约 25% 最大生命）；中段生命已达到 `244.95`，结束后恢复 44 基础移速。五张 `1440×900` 截图通过非纯色、透明和近黑拒绝检查并逐张打开确认，Canvas、文本状态、控制台和失败请求正常。
- 满区专项 `5/5`、TypeScript、目标源码 ESLint、浏览器脚本语法、`git diff --check` 与 `pnpm build` 通过。完整自走棋套件为 `182/184`，仅失败于上一节已记录的两个贪吃旧断言（仍要求攻击成长及旧说明），与满区改动无关。

## 2026-07-30 · 浣熊店员与犬绒低费技能重做

- [x] 检索角色公开资料与直播切片：十六萤采用“手机过热自动连上 PK”与尾巴自保梗；犬绒采用小狗星球“给苏打饼干打孔、给趣多多点巧克力豆”的熟练工设定。
- [x] 浣熊店员主动改为“手机过热 · 自动PK”：敌人进入 150 范围后释放，获得 18% 最大生命护盾并短暂嘲讽范围内敌人 1.4 秒。
- [x] 犬绒主动改为“小狗星球熟练工”：给最低血量的两名友军分饼干，无盾时提供 15% 最大生命护盾，有盾时回复 12% 最大生命。
- [x] 新增过热手机/PK 波纹，以及飞行苏打饼干/巧克力曲奇专属 Phaser 特效。
- [x] 补齐数据、战斗与渲染回归。
- [x] 完成类型检查、完整测试、构建和系统 Chrome 视觉验证。
- 专项系统 Chrome 验证确认：浣熊店员获得 `41.76/232` 护盾（18%），附近两名敌人均被嘲讽；犬绒分别提供 `45.75/305` 护盾（15%）与 `18/150` 治疗（12%）。两张 `1440×900` 整页截图均通过纯色、透明与近黑拒绝检查并逐张打开确认，Canvas 为 `1440×853`，控制台和失败请求为空。
- 主 `verify-autochess.cjs` 完整流程覆盖首战胜利、次战失败、强化选择、第三战整备、全屏和移动端；全部 17 张截图通过像素检查并逐张打开确认，画面非黑屏且无明显遮挡，控制台错误为空。
- 最终验证：TypeScript、目标源码 ESLint、专项脚本语法、数据/战斗/渲染专项回归、`git diff --check` 与 `pnpm build` 均通过。完整 `pnpm autochess:test` 为 `184/187`；三项失败均为本轮外既有断言：两个贪吃用例仍要求旧版攻击成长，另一个“一星好笑姐姐仍不能越费单挑二费斗士”当前结果相反。

## 2026-07-30 · 好笑姐姐短射程骚扰与低血撤步

- 公开梗调研参考 Bilibili `BV1am4y1T7XL`“大猩猩生气为什么很安静？（前辈冷笑话）”及其“未知夜Michiya（搞笑沪姐）”合集；合集中的“高冷姐姐的幻灭”“未知夜此人杀心极重”“（左右看）（流汗）（后退）（扭头转身）泥给路哒哟”等标题共同支持“偷袭时杀心极重、讲完冷笑话后高冷幻灭并撤退”的战斗身份。`BV1cNG66LEKA` 的公开标题与标签补充了成熟、高挑、俯视感，但本轮不额外增加第三羁绊，避免扩大平衡范围。
- 好笑姐姐不再使用 `82` 移速、`52` 射程的高速近战模板，改为 `ranged / 145 射程 / 58 移速`；偷袭仍负责开战切后排，落地后会停在短射程攻击，不再继续冲进前排送死。
- 基础面板由 `146 生命 / 26 攻击 / 8 护甲 / 0.88s` 调整为 `180 生命 / 23 攻击 / 12 护甲 / 1.08s`。生存预算明显提高但仍低于一费前排，普攻节奏放慢以保留 `2.7/3.1/3.6x` 冷笑话弹幕的爆发重心。
- 😂 弹幕继续只对敌人造成伤害和眩晕，不提供护盾。自身低于等于 `65%` 生命且目标已经逼近时，命中后会沿远离目标方向短撤最多 `36px`，撤步终点不会退出自己的攻击距离，并显示“泥给路哒哟”；远距离命中不会无条件风筝近战。
- 战斗边界回归确认：一星好笑姐姐稳定击败同面板一费脆皮后排；在最有利的孤立一对一中可险胜一星二费雅吨，但只剩约 `20%` 生命，团队战仍会被集火惩罚；全程护盾与护盾统计保持为零。
- 系统 Chrome 专项使用确定性时钟捕获飞行与命中两帧：飞行帧可见真实 😂 弹体，命中帧可见放大 😂、冲击圈、伤害、眩晕、分行喊话与撤步；两张 `1440×900` 整页截图通过纯色、透明和近黑拒绝检查并逐张打开确认，Canvas 为 `1440×853`，控制台与失败请求为空。
- 定向好笑姐姐回归 `4/4`、TypeScript、目标源码 ESLint、浏览器脚本语法、`git diff --check` 与 `pnpm build` 通过。完整 `pnpm autochess:test` 为 `185/187`；仅剩前一节已记录的两个贪吃旧断言，均仍要求已经移除的攻击成长，与本轮无关。

## 2026-07-30 · 花礼随机迎客松与 75mm 大吧唧

- 花礼“大黑鼠”的主动技能改为 `50/50` 随机使出迎客松或 75mm 大吧唧，两者共用 `118` 小范围；技能名与说明同步改为“迎客松·大吧唧”。
- 迎客松使范围内敌人嘲讽 `0.8s`，并降低 `20%` 移速 `1.5s`；已有更强减速时不会被覆盖。大吧唧造成 `1.5×攻击力` 范围技能伤害，并眩晕 `0.65s`。
- 移除旧松树炮台、松针弹幕和 `🌲` 表现；Phaser 改为自绘绿色横枝迎客松、翠绿针叶与迎宾范围圈，以及粉色 75mm 圆徽章、坠落与冲击圈。
- `render_game_to_text()` 公开减速剩余时间、减速倍率和两种花礼视觉效果；数据、战斗与渲染回归分别锁定文案、随机分支、范围内外结算、强减速优先级和独立自绘效果。
- 系统 Chrome 专项覆盖桌面与 `390×844` 手机端两种随机结果；四张截图均通过纯色、透明与近黑拒绝检查并逐张打开确认，效果可辨、界面无重叠，控制台和失败请求为空。
- 最终验证：花礼定向回归 `3/3`、TypeScript、目标源码 ESLint、专项脚本语法、`git diff --check` 与 `pnpm build` 通过。完整 `pnpm autochess:test` 为 `186/189`；3 项失败均为工作区既有旧断言，其中 2 项仍要求贪吃增加攻击力，1 项仍要求浣熊店员说明包含旧“PK”文字，与花礼技能链路无关。

## 2026-07-30 · 浣熊店员“打开开关”

- 按用户确认的“紫色心情”梗替换上一版“手机过热 · 自动PK”：技能名改为“打开开关”，释放时获得 `18%` 最大生命护盾并进入 `4 秒`开关状态；犬绒技能保持不变。
- 开关持续期间角色立绘轻微振动；每名敌人第一次以普通攻击有效命中浣熊店员时，自身眩晕 `0.5 秒`。每名攻击者每次开关只触发一次，技能伤害、重复普攻和开关结束后的攻击均不触发。
- Phaser 新增紫色开关胶囊、中心脉冲、紫色护盾与持续 `ON` 标记；反震时绘制浣熊到攻击者的紫色折线电弧，并显示“反震”。开关胶囊抬到角色上方，避免被立绘遮住。
- `render_game_to_text()` 公开开关剩余时间和已反震攻击者数量；数据、战斗和渲染回归锁定新名称、说明、护盾比例、持续时间、首次反震和不可重复触发。
- 系统 Chrome 专项确认护盾为 `41.76/232`（18%）、持续时间为 `4`、首次攻击者眩晕 `0.5` 秒、重复攻击眩晕为 `0`；犬绒仍提供 `45.75/305`（15%）护盾与 `18/150`（12%）治疗。三张 `1440×900` 截图通过纯色、透明和近黑检查并逐张打开确认，Canvas 为 `1440×853`，控制台和失败请求为空。
- 主 `verify-autochess.cjs` 系统 Chrome 流程覆盖首战胜利、次战失败、强化选择、第三战整备、全屏和移动端；17 张截图已逐张打开确认，画面非黑屏且无新增遮挡，控制台错误为空。
- 最终验证：TypeScript、目标源码 ESLint、专项脚本语法、`git diff --check` 与 `pnpm build` 通过。完整 `pnpm autochess:test` 为 `187/189`；仅剩两项此前已记录的贪吃旧断言，仍要求已经移除的攻击成长，与本轮无关。

## 2026-07-31 · 多北欧时停领域视觉修复

- 用户反馈敌我场上存在多个北欧魔法师时，时停规则有时生效但看不到对应球形领域。
- 根因确认：战斗状态支持多个 `chronospheres`，Phaser 场景却只维护单个 `chronosphereView` 并只渲染 `zones[0]`，因此第二个及后续领域没有持久视觉。
- 已改为按施法者 `sourceFid` 独立维护领域容器；每个领域分别更新位置、半径、颜色和寿命，结束时只销毁自己的视图，仍与通用短命特效对象池隔离。
- 已更新静态回归，锁定多领域遍历、按施法者创建/清理视图，并禁止恢复 `zones[0]` 单领域渲染。
- 系统 Chrome 专项在敌我各取一个施法源，同时显示 `(430,285,r112)` 与 `(690,430,r92)` 两个领域；前者结束后后者仍独立保留。两张 `1440×900` 截图通过纯色、透明和近黑拒绝检查并逐张打开确认，Canvas 为 `1440×853`，文本状态、控制台和失败请求均正常。
- 主 `verify-autochess.cjs` 系统 Chrome 流程通过，17 张标题、整备、拖拽、战斗、结算、强化、全屏和移动端截图已逐张检查；TypeScript、目标源码 ESLint、专项静态回归 `36/36`、浏览器脚本语法与 `git diff --check` 通过。完整 `pnpm autochess:test` 为 `188/190`，仅失败于已记录的两条贪吃旧断言，与本轮无关。

## 2026-07-30 · 三星椰子栞海獭冲击眩晕成长

- 海獭冲击原先不分星级固定眩晕 `0.35 秒`；伤害与护盾会随三星面板自然成长，但控制没有成长且表现只有约 21 帧，不符合三星三费棋子的投入。
- 眩晕改为一至三星 `0.5/0.75/1 秒`，范围、伤害倍率、护盾比例、回能与施法距离保持不变；图鉴和棋子说明会展示三档星级成长。
- 新增三星系统 Chrome 专项：海獭冲击在实际落点同时命中 3 人，引擎与 `render_game_to_text()` 均记录每人剩余眩晕 `0.88 秒`；`1440×900` 截图通过纯色、透明和近黑拒绝检查并打开确认，冲击圈、三星标识和三名目标的眩晕 `✦` 均可见，Canvas 为 `1440×853`，控制台和失败请求为空。
- 定向数据与战斗回归、TypeScript、目标源码 ESLint、专项脚本语法和 `git diff --check` 通过。完整 `pnpm autochess:test` 为 `188/190`；仅失败于已记录的两条贪吃旧断言，仍要求已经移除的攻击成长，与本轮无关。

## 2026-07-30 · 七海凿凿冲击持续时间收紧

- 平衡审计确认旧版持续 `5.5 秒`，同时提供 `+42` 护甲、`155` 范围持续嘲讽和每次受击触发的 `0.24 秒` 矿镐眩晕；在 `24 秒` 战斗上限中占用过长，兼具高坦度与团控的窗口明显超标。
- 持续时间改为随星级成长的 `2.8/3.2/3.6 秒`，护甲、嘲讽范围、矿镐伤害和短眩晕保持不变；定位收敛为中短时间冲阵控场，而不是长时间接管交战。
- 图鉴与棋子技能说明展示三档时长；回归测试锁定一星能量消耗速率，并确认结束后卸下额外护甲、停止刷新嘲讽且受击不再发射矿镐。
- 系统 Chrome 专项覆盖图鉴、冲锋、矿镐飞行/命中和技能结束状态；5 张 `1440×900` 截图通过纯色、透明和近黑拒绝检查并逐张打开确认，文本状态、控制台与实际 4xx 资源请求无游戏错误。
- 七海定向回归 `2/2`、TypeScript、目标源码 ESLint、浏览器脚本语法和 `git diff --check` 通过。完整 `pnpm autochess:test` 为 `188/190`，仅失败于已记录的两条贪吃旧断言。生产构建已编译成功，但在页面数据收集阶段因其他本地 Next 进程共用 `.next` 报缺少 `/_document`，未将该环境竞争误判为七海回归。
## 2026-07-31 · 泽音涅槃可靠性调整

- 用户反馈泽音经常首命送不掉，或复活后立即被集火击杀，导致二阶段火力无法发挥。
- 首命增加 4 秒兜底：首次死亡仍会立即涅槃；若开战 4 秒仍存活，也会主动涅槃，避免优势局整套被动不触发。
- 重生会清除灼烧、眩晕、嘲讽、减速与减甲，解除敌方既有锁定，并获得 1 秒涅槃护体；护体期间免疫伤害且敌方会优先选择其他目标，旧飞行弹道也无法在重生帧补杀。
- 保留原有 72% 生命远程形态、攻击与攻速增幅，以及 4 秒射程内普攻后撤；技能说明、头顶“护”状态和 `render_game_to_text` 已同步。
- 专项战斗测试覆盖致死涅槃、4 秒满血兜底、仇恨清除、护体免伤与结束恢复、后撤和射程边界；TypeScript 与差异检查通过。
- 系统 Chrome 专项通过：3.9 秒仍是首形态，4.02 秒自动涅槃；护体剩余 0.88 秒时承受 99999 测试伤害仍保持 130/130，敌方锁定为空；后撤距离由 120 增至 154，仍在 245 射程内。
- `zeyin-rebirth-effect.png` 与 `zeyin-rebirth-recoil.png` 均为有效 1440×900 截图，已逐张打开检查；Canvas、文本状态、控制台和资源请求交叉验证正常。
- 扩展回归为 180/182；两项失败来自工作区已有的贪吃成长改动与旧断言不一致（仍要求攻击力成长/旧文案），与泽音逻辑无调用关系。

## 2026-07-31 · 沐霂甩舞带救场表现

- 用户反馈“领舞救场”虽然已有拉援位移，但通用直线特效仍像受援目标自己冲刺；现改为专用 `mumu_whip` 舞带表现。
- 救援分为约 24% 的甩带蓄势与后续抛甩：舞带先从沐霂处弯曲飞向目标，目标留在原地；套环命中后双层青白舞带绷紧，目标沿二次贝塞尔弧线越过沐霂并落到身后。
- 引擎和 Phaser 共用 `motionPaths.ts` 的命中时机、缓动和控制点；目标途中增加轻微离地、倾斜与压缩，避免表现状态和战斗坐标脱节。落地后的时停撤离、持续压制打断、净化、治疗和护盾数值保持不变。
- `render_game_to_text()` 的视觉效果补充第三点坐标，专项回归锁定甩带阶段目标静止、舞带起点/套住点/落点、弧线路径、越肩中间态和最终落地结算。
- 系统 Chrome 专项使用安装的 Chrome channel 与整页截图：出手帧目标保持 `(620,350)`；套住后到 `(484,316)`；越肩帧到 `(323,325)`，已越过沐霂 `(360,350)`；最终落在身后 `(290,350)`。五张 `1440×900` 截图均通过纯色、透明和近黑拒绝检查并逐张打开确认，Canvas 为 `1440×853`，控制台和失败请求为空。
- 沐霂定向回归 `4/4`、TypeScript、目标源码 ESLint、浏览器脚本语法、`git diff --check` 与 `pnpm build` 通过。完整 `pnpm autochess:test` 为 `188/190`；两项失败均来自工作区已有的贪吃成长改动与旧断言不一致，与本轮无关。

## 2026-07-31 · 雪烛技能盾削弱与轴伊橘子充能

- 雪烛“八门镇式”保留高初始能量、300 施法距离、4 秒持续和技能盾不叠加机制，只将一至三星护盾从 `70 + 26% / 120 + 36% / 200 + 50%` 下调为 `50 + 20% / 100 + 30% / 180 + 42%` 目标最大生命。
- 一星系统 Chrome 实测雪烛自身获得 `91` 技能盾、能能获得 `77.6`，均精确符合 `50 + 20% 最大生命`；技能盾环和“术”标记清晰可见。
- 轴伊“扔橘子”现在只选择其他友军；每颗橘子除逐次减弱的治疗外，按一至三星额外补充 `3/4/6` 能量，轴伊不能用橘子给自己回能。施法门槛也只检查其他友军，只有轴伊本人低血时不会浪费满能量。
- 充能命中复用现有 `energy_pulse` 战场反馈。系统 Chrome 捕获两颗橘子同时在途、中途 `+3 能量` 脉冲和技能结束三帧；一星五颗结束后目标累计获得 `15` 能量，轴伊自身保持 `0`。
- 两组系统 Chrome 专项均使用安装的 Chrome channel 和整页截图；有效截图为 `1440×900`、Canvas `1440×853`，近暗比例约 30%，已逐张打开确认，文本状态、控制台和失败请求正常。首次 headless 截图只有 DOM HUD 而无 WebGL 战场，已按仓库规则判为捕获路径失败并改用 headed Chrome 重试。
- 定向数据与战斗回归 `5/5`、TypeScript、目标源码 ESLint、浏览器脚本语法和 `git diff --check` 通过。完整 `pnpm autochess:test` 为 `189/191`；仅失败于已记录的两条贪吃旧断言，与本轮无关。未运行生产构建，避免与多个现有 Next 进程争用共享 `.next`。

## 2026-07-31 · 梨安终场谢幕输出职责收敛

- [x] 梨安攻击力由 `43` 降至 `39`，保留 `0.8 秒`高速普攻节奏。
- [x] 新增专属“谢幕蓄势”回能：`120` 最大能量、每秒 `+6`、每次普攻 `+8`、每次受击 `+4`，攻速仍能明确加快施法但不再使用原蓄势回能的每次普攻 `+16`。
- [x] 终场谢幕范围保持 `140`，伤害由 `1.55×` 提高至 `1.8×攻击力`；删除落点内友军 `+15` 能量、星辉连线和能量脉冲。
- [x] 技能说明收敛为直接高伤害范围轰击；轴伊已有的单目标橘子充能保持不变。
- [x] 梨安定向数据与战斗回归 `2/2` 通过，`git diff --check` 无空白错误。
- [x] TypeScript、目标源码 ESLint、专项脚本语法和 `git diff --check` 通过。完整自走棋套件为 `190/192`；仅失败于已记录的两条贪吃旧断言，与梨安链路无调用关系。
- [x] 系统 Chrome 专项确认五名范围目标均受到 `80.73 = 44.85×1.8` 实际伤害，四名友军能量保持 `0`，无能量脉冲、控制台错误或失败请求。
- 五张飞行、桌面落地、手机落地、`350ms` 衰减和 `700ms` 清空截图均通过纯色、透明与近黑拒绝检查并逐张打开确认；Canvas 为桌面 `1440×853`、手机 `390×844`。
- 原 `3100` 端口虽返回 200 但未生成游戏 Canvas，按仓库规则判为旧监听服务失效；专项改用隔离的 `3101` 当前源码开发服务完成。

## 2026-07-31 · “打开开关”特效缩小

- 按用户反馈将开关启动表现整体缩到原来的约七成：脉冲半径由 `58` 降为 `42`，胶囊由 `68×34` 缩为 `48×24`，旋钮由 `12` 缩为 `8`，`ON` 字号同步从 `11` 降为 `9`。
- 护盾比例、4 秒振动、每名攻击者首次普攻反震与 0.5 秒眩晕均未改动；持续护盾环和反震电弧保持原有可读性。
- 系统 Chrome 专项三张 `1440×900` 截图通过纯色、透明和近黑检查并逐张打开确认；缩小后的开关仍可辨认且不遮挡技能名、角色或血条，Canvas、文本状态、控制台和失败请求正常。
- TypeScript、目标源码 ESLint、浣熊渲染专项 `1/1` 与 `git diff --check` 通过。

## 2026-07-31 · 雪绘骄兵收敛与哀兵全队振奋

- [x] 骄兵必败的满额攻速与射程惩罚由 `-50%` 收敛到 `-20%`，保留顺风代价但不再把四费近战削成半个棋子。
- [x] 哀兵必胜保留雪绘自身最高 `+50%` 攻速与射程，并让其余存活友军按同一战力差获得最高 `+25%` 攻速；多名雪绘不叠加团队倍率。
- [x] 团队振奋随劣势缩小自动回落；状态结束后队友攻速、状态字与光环均恢复基线。
- [x] 哀兵切换文案改为“哀兵必胜 · 全队振奋”，并为响应队友复用蓝色脉冲环、持续光环与“哀”状态字。
- [x] `render_game_to_text()` 公开每名棋子的同步方向与强度，便于验证团队响应。
- [x] 雪绘战斗专项 `2/2`、数据回归、TypeScript、目标 ESLint、浏览器脚本语法与 `git diff --check` 通过。
- [x] 系统 Chrome 专项确认骄兵满额时雪绘为 `45` 射程、`1.10s` 攻击间隔；哀兵满额时为 `84`、`0.59s`，两名队友分别由 `1.20s → 0.96s`、`0.90s → 0.72s`，符合 `+25%` 攻速。
- [x] 两张 `1440×900` 全页截图通过纯色、透明和近黑拒绝检查并逐张打开确认；橙色骄兵只作用于雪绘，蓝色哀兵光环与“哀”状态扩散至队友，未遮挡角色名或血条。Canvas 为 `1440×853`，控制台和失败请求为空。
- 完整 `pnpm autochess:test` 为 `190/192`；两项失败均为工作区已记录的贪吃旧断言，仍要求已经移除的攻击成长与旧说明，与雪绘改动无关。

## 2026-07-31 · 小岁鸟三段固定距离冲刺

- [x] 确认旧实现每段以“目标当前位置 + 穿身距离”为终点，冲刺长度随目标距离变化，视觉上存在近似无限冲刺的问题。
- [x] 将单段冲刺和技能施法距离统一为 `240`；三段均从各自起点沿当前目标方向固定冲刺 `240`，不再按目标位置动态延长。
- [x] 临近战场边界时优先选择最接近目标方向、且能完整走完 `240` 的合法转向，避免通用边界夹取把某一段截短。
- [x] 技能说明明确标注三段固定距离；战斗回归记录三段运动轨迹，确认每段均为 `240` 且第一段能够命中施法距离内目标。
- [x] 小岁鸟战斗专项 `2/2`、岁己数据专项 `1/1`、TypeScript、目标源码 ESLint、专项脚本语法与 `git diff --check` 通过。
- [x] 主 `verify-autochess.cjs` 在当前源码开发服务 `3101` 完整通过；旧 `3100` 仍是返回 200 但没有游戏 Canvas 的失效生产监听。
- [x] 系统 Chrome 专项确认超出技能距离时满能不施法，进入距离后第一段命中，三段实测为 `240 / 240 / 240`；两张 `1440×900` 全页截图通过纯色、透明和近黑拒绝检查并逐张打开确认，Canvas 为 `1440×853`，文本状态、控制台和失败请求正常。
- 完整 `pnpm autochess:test` 为 `193/195`，小岁鸟新增回归均通过；两项失败是工作区已记录的贪吃旧断言，仍要求已移除的攻击成长与旧说明，与本轮改动无调用关系。

## 2026-07-31 · 大黑鼠近战控场可靠性

- 定位由错误的“后排控制”收敛为“近战怪话”：生命 `176 → 232`、护甲 `16 → 22`，移速 `86 → 64`，避免脆皮近战越过己方前排率先送死。
- 技能时机由 `offenseReady` 改为 `offenseInRange`，并把技能距离固定为真实效果半径 `118`；满能量后会等待进入近战攻击距离再释放，不再在约 `360` 距离空放小范围技能。
- 新增专属“怪话回能”，初始能量 `20 → 45`、后续仍为每秒 `+20`；第一句怪话由约 `4 秒`提前到 `2.75 秒`，三人普攻集火压测中可在倒下前自然开出技能。
- 迎客松保留 `0.8 秒`嘲讽和 `1.5 秒 / 20%`减速，并按覆盖人数获得 `18% + 每人 6%` 最大生命护盾，上限 `42%`；75mm 大吧唧继续承担 `1.5×` 范围伤害与 `0.65 秒`眩晕，随机两面分别偏防守和进攻。
- 定向数据/战斗回归 `4/4` 通过：覆盖近身施法、远距离不空放、单人与多人护盾、封顶，以及三人集火下能开盾但长期仍可击破。
- TypeScript、目标源码 ESLint、浏览器脚本语法和 `git diff --check` 通过。完整自走棋套件为 `192/194`；仅失败于已记录的两条贪吃旧断言，仍要求已移除的攻击成长与旧说明。
- 系统 Chrome 专项确认两名近敌触发迎客松时获得 `30%` 最大生命护盾；大吧唧对两名近敌各造成约 `46.6` 伤害并眩晕 `0.65 秒`，远敌不受影响。桌面/手机共四张截图通过纯色、透明与近黑拒绝检查并逐张打开确认，控制台与失败请求为空。

## 2026-07-31 · 满区夹击逃跑与后坐力强制让路

- 满区不再每帧只看最近敌人；变身开始时从 16 个候选方向中同时评估最近敌人安全距离与所有敌人的加权总距离，锁定本次逃跑方向，只有抵住战场边界几乎无法前进时才重新计算。
- 逃跑方向写入战斗文本状态。左右对称夹击回归确认满区稳定沿垂直方向移动，不再左右转向却原地踏步；所有敌人的总距离会实际增长。
- 满区位移和明确标记的攻击后坐力获得局部强制让路：只推动本帧接触路径上的单位，不递归推动整条队列；普通接敌、同队让位和通用碰撞分离参数保持原行为。
- 后坐力取消“先找空地再后退”的绕路落点，改为保持远离攻击目标的直线后撤；路径单位会被有限地向前或侧面挤开，落点仍受战场边界与攻击射程保护。
- 图鉴文案同步为“计算周围敌人后锁定最安全方向逃跑”，各星级说明补充远离周围敌人与挤开阻挡；持续时间、闪避和回复数值均未改动。
- 定向回归 `9/9` 通过，覆盖左右夹击、满区阻挡、泽音阻挡后坐力、礼墨后坐力和普通同队让位；TypeScript、目标 ESLint、两份浏览器脚本语法与 `git diff --check` 通过。
- 系统 Chrome 满区专项：左右敌人总距离 `200 → 288.6`，满区沿同一方向移动 `104.1px`，阻挡者移动 `110.1px`；泽音后坐力目标距离 `120 → 144px`、落点 `154px`，阻挡者侧移 `63.5px`。共 12 张 `1440×900` 截图通过纯色、透明和近黑拒绝检查并逐张打开确认，Canvas 为 `1440×853`，文本状态、控制台与失败请求均正常。
- 完整自走棋套件为 `195/197`；仅失败于已记录的两条贪吃旧断言，仍要求已经移除的攻击成长与旧说明，与本轮移动链路无调用关系。

## 2026-07-31 · 泽音能量涅槃与二阶段火球

- 本方案取代同日较早的“固定 4 秒主动涅槃 + 1 秒免伤 + 低锁定优先级”：删除固定计时、涅槃护体、仇恨清除和低优先级，敌方锁定与在途弹道会延续到第二条命。
- 首命改为 `110` 生命、`0/100` 能量的近战形态，只按每秒 `+20` 自动回能；首次倒下会立即涅槃，存活时则在约 `5 秒`能量充满后涅槃，玩家可直接通过能量条看到进度。
- 涅槃会清除灼烧、眩晕、嘲讽、减速与减甲，并以一星 `200` 生命的远程形态重生；保留重生后前 `4 秒`普攻后撤，但不提供伤害免疫。
- 第二阶段继续自动回能，满能量时向 `320` 范围内目标发射 `🔥` 火球；一至三星分别造成 `1.6/1.8/2.1` 倍直接伤害，并在 `3 秒`内追加 `0.8/1/1.25` 倍攻击的灼烧伤害。
- 系统 Chrome 专项实测首命能量 `50/100`、重生生命 `200/200`、重生后普通伤害正常生效且敌方保留锁定；一星火球造成约 `85.08` 直接伤害与 `42.54` 总灼烧，后撤距离由 `120` 增至 `154` 且仍在 `245` 普攻射程内。
- 五张 `1440×900` 截图覆盖蓄能、涅槃、火球飞行、火球命中与后撤，均通过纯色、透明和近黑拒绝检查并逐张打开确认；Canvas、文本状态和控制台无游戏错误。
- 泽音定向回归通过；脚本语法、TypeScript、目标源码 ESLint 与 `git diff --check` 通过。完整 `pnpm autochess:test` 为 `194/197`；三项失败均为工作区已有的贪吃攻击成长和满区旧文案断言，与泽音链路无关。

## 2026-08-01 · 紧凑桌面拖拽坐标对齐

- 用户反馈窄高浏览器下拖动棋子时，绿色目标高亮与实际槽位错位。
- 根因：`drawPreparation()` 计算了 `compact`，但仍以 `false` 绘制桌面棋盘/备战席；当 `fitScale < 0.72` 时，画面使用宽版槽位，而 `locationAt()` 和拖拽目标标记使用紧凑槽位。
- 修复：桌面 `drawSlot()` 的棋盘与备战席统一传入 `compact`，让绘制、命中和高亮共用同一布局 profile；新增静态回归禁止恢复宽版参数。
- 新增 `scripts/verify-autochess-drag-coordinate.cjs`，覆盖 `773×790 CSS px + DPR 1.25`（对应用户约 `966×988` 截图）、`800×800` 紧凑桌面、常规宽屏及 DPR `1/1.25/1.5`；拖拽交换、目标高亮、`render_game_to_text`、截图和失败请求检查通过。
- 修复后系统 Chrome 截图已打开检查，控制台错误与失败请求为空；定向浏览器脚本覆盖 21 个视口/DPR 组合并全部通过，主 `verify-autochess.cjs`、TypeScript、静态回归与完整 `pnpm autochess:test`（`198/198`）均通过。

## 2026-08-05 · v0.2.0 AI 战术台、首批统一立绘与早期波次调整

- 本轮目标：建立版本号、更新日志和后续维护规范；逐步替换粗糙立绘；让键盘与控制台覆盖完整对局并输出可供 AI 解析的反馈/战斗日志；用多局测试发现并修正早期节奏痛点。
- 版本更新到 `0.2.0`。新增根目录 `CHANGELOG.md`、游戏内版本按钮与更新日志弹窗、`docs/autochess-release-guide.md` 和一致性检查 `pnpm autochess:release:check`；版本源、发布日期、游戏内摘要与 `package.json` 由检查脚本锁定。
- 新增 `window.autoChessAI`：支持状态/日志读取、协议选择、五格商店购买、刷新、锁店、升本、场上/备战席选中与移动、出售、开战、天赋选择、结算继续、重开、确定性推进、console 开关和原始 action。各阶段同时补齐数字键、`R/L/U/E/Space/S/D/Enter/Delete/Escape/V` 等快捷操作，`render_game_to_text()` 会公开当前可用动作。
- `EngineBridge` 将 action、phase、toast 和刷新后的完整商店名称同步到 console。战斗事件包含时间、类型、消息、双方角色与坐标、方向、技能名、飞行物、命中点、伤害量/类型和阵亡；`S` 或 `autoChessAI.skipBattle()` 会以 60Hz 确定性推进到结算，并记录快速结算控制事件。
- 首批统一立绘为果冻风纪、小红帽和绒绒的狗。身份/配色参考分别为原素材 `/images/livers/hazel.png`、`/images/materials/red/1d5ad005aff0b4b648a0f1ef6b8d0cd71954091502.png`、`/images/livers/mofu.jpg`；共同生成约束是全身三分之四战棋角色、清晰小尺寸轮廓、保留身份配色并把技能职责转译为服装/道具、无文字/边框、纯色键背景。审核源保存在 `.tmp/autochess-portraits/v0.2.0/`，经色键软边缘抠图和 `512×512` RGBA 安全边距归一化后发布到 `public/images/autochess/portraits/sun-guard.png`、`sui.png`、`mossback.png`，单位使用 `portraitStyle: "sprite"`。
- 平衡测试使用最终版节奏/经济/羁绊 AI 各 12 局（共 36 局、种子从 61000 起），另对前四波枚举 455/455/1365/1365 组一星低费阵容并以 3 个战斗种子复测。旧预算下三种策略平均终止于第 `6.17/6.42/6.33` 波，平均胜场 `1.17/1.42/1.33`；第 2/3/4 波可用阵容只有 `14.1%/6.2%/1.2%`，属于过早的特定答案检查。
- 前八波有效预算由 `2/5/9/18/16/17/21/32` 平滑为 `2/4/7/11/10/13/16/24`，败局存活敌人追加伤害上限由 `3` 降为 `2`。修改后第 2/3/4 波可用阵容升至 `33.0%/13.0%/8.1%`；相同种子下三种策略平均终止波次升至 `7.50/7.83/11.33`，平均胜场升至 `2.67/3.25/7.25`。
- 没有根据混杂的商店出现率/站位相关性直接修改单卡。羁绊 AI 有 3/12 局抵达第 16 波但无通关，样本少且阵容质量偏弱；后续先增强中后期购买与阵容估值，再用更多真人/高质量 AI 样本判断终局首领和第 5、7 波，不因这三个样本继续削弱首领。
- 新增 `scripts/verify-autochess-ai.cjs` / `pnpm autochess:verify:ai`。系统 Chrome seed 1 实测键盘选择“舞台梦”，console 刷新得到“好笑姐姐/能能弄你/帕可Pako/贪吃岁/绒绒的狗”，再通过 API 购买、移入备战席并复位；最终重跑从截图时点快速结算推进 `588` 步（`9.8s`）后胜利。
- 专项记录 `94` 条战斗事件且全部在 console 找到同 ID：包括 `0.02s` 好笑姐姐锁定果冻风纪、`3.78s` 能能弄你从 `(385.5,185.5)` 朝 `(0.952,0.305)` 方向释放苹果派、投射物在 `(741,299.5)` 命中兔子射手造成 `7.91` 技能伤害，以及普通攻击伤害/阵亡/快速结算事件。console/page error 与 4xx/5xx 请求均为空。
- `release-notes.png`、`preparation-generated-portraits.png`、`battle-ai-control.png`、`result-after-skip.png` 均为 `1440×900`，通过纯色、透明、近黑检查并逐张打开确认；Canvas 为 `1440×853`，三个新资产均实际返回 200，弹窗、整备、战场和结算无裁切或重叠。主 `verify-autochess.cjs` 的 17 张桌面/移动/全屏截图也已逐张检查。
- 最终功能验证基线：`pnpm autochess:test` `203/203`、`pnpm exec tsc --noEmit`、目标源码 ESLint、`pnpm autochess:release:check`、新脚本语法、主 Chrome 流程与 `git diff --check` 均通过。

## 2026-08-05 · AI 观战、随时托管与后台战斗

- 入口决策：不再增加独立入口页。现有标题页已经承担开局选择，因此在协议卡前加入“亲自指挥 / AI 观战”分段选择；AI 模式既可由玩家指定协议，也可让 AI 自选协议开局，减少重复页面并保留开局决策权。
- 新增 `AutoChessAutopilot`，与面向控制台的 `window.autoChessAI` 分离。托管会逐步购买、升本、按近战/远程重新布阵、选择天赋、开战和战后继续；战斗保持正常速度播放，不调用快速结算。桌面工具栏、移动端小型控制组和设置面板均可随时开启托管或接管。
- 设置面板整合 AI 托管、后台继续战斗、游戏声音与音量。后台战斗默认关闭并持久化到本地；开启后 `EngineBridge` 使用隐藏期间真实经过的墙钟时间以固定小步补推进，避免仅取消暂停后仍被后台 `requestAnimationFrame` 降频拖慢。`render_game_to_text()` 同步公开托管、后台设置和页面隐藏状态。
- 新增 `scripts/verify-autochess-autoplay.cjs` / `pnpm autochess:verify:autoplay`。系统 Chrome seed 1 实测 AI 自动购入并布阵后进入首战；开启后台时战斗 `0.7s → 1.7s`，关闭后隐藏 `0.9s` 保持在 `2.0s`；桌面与 `390×844` 手机端控件、设置和标题页模式均通过。
- 专项 7 张截图和主流程 17 张截图均通过纯色、透明、近黑拒绝检查并逐张打开确认；Canvas、`render_game_to_text()`、console/page error 与 4xx/5xx 请求交叉检查正常。移动端新增控制与既有缩放、快速结算、统计和备战操作无重叠。
- 最终验证：`pnpm autochess:test` `206/206`、`pnpm exec tsc --noEmit --incremental false`、目标源码 ESLint、`pnpm build`、专项系统 Chrome、主 `verify-autochess.cjs` 与 `git diff --check` 均通过。

### 后续可选

- 收集真人观战样本后再调整托管的商店估值、羁绊权重与中后期保留金币；本轮先保证策略可解释、全流程稳定并且玩家能随时接管。

## 2026-08-06 · 音量快捷调节与设置内版本入口

- 桌面顶部工具栏移除独立版本按钮，恢复声音开关、音乐音量和音效音量快捷控制；滑杆带独立可访问名称，并沿用既有音频偏好持久化。
- “版本与更新 v0.2.1”移入游戏设置，点击会先关闭设置再打开现有更新日志；桌面设置不再重复展示音频项。
- 移动端顶部工具栏原本隐藏，因此继续在设置中保留游戏声音、音乐和音效控制；`390×844` 下音频项、版本入口和关闭按钮均完整可见。
- 专项静态回归新增桌面音量、版本入口归属和移动端音频样式约束；系统 Chrome 自动化验证两条顶部滑杆可调、版本入口跳转、桌面/移动显示条件、后台战斗推进与暂停均正常。
- `pnpm autochess:test` `209/209`、TypeScript、目标源码 ESLint、专项脚本语法、专项系统 Chrome 和主 `verify-autochess.cjs` 均通过；专项 8 张及主流程 17 张截图已逐张检查，无黑屏、裁切或控件重叠，console/page error 与失败请求为空。

## 2026-08-06 · v0.2.1 满席换购与点击出售

- 用户观察到 AI 托管在棋盘与 8 格候选席都满后不再继续运营；确认根因是 `AutoChessAutopilot.purchaseAction()` 在无空位时直接返回，托管没有出售决策。
- 托管现在会先锁定一张买得起且符合既有阵容估值的商店棋，只从目标阵容以外的候选席单位中选择最低价值牺牲位；候选棋必须比牺牲位更有价值，并且不会出售与候选棋同名的升级素材，也不会为了腾位直接出售场上单位。
- 出售与购买通过待购计划绑定：出售成功后的下一次托管动作优先买入原目标，避免卖完后转去升本、刷新或做其他操作。满席回归实测动作顺序为 `sell → shop`，候选席 `8 → 7 → 8`，阵容总数保持不变。
- 桌面 Phaser 出售区新增独立点击命中区；选中棋子时显示“点击出售 +N 金币”，点击派发无位置 `sell` 动作，拖动中仍保留“出售 / 松开出售”反馈，并用拖动状态阻止一次松手触发两次出售。
- 控制台出售回归覆盖 `autoChessAI.sell("board", slot)` 与 `select(...) → sell()`；满席托管回归确认只出售候选席低价值单位并继续买入目标棋。
- 版本提升到 `0.2.1`，同步 `package.json`、游戏内更新日志、`CHANGELOG.md` 和验证脚本；本版本同时收录当日桌面音量快捷调节与设置内版本入口调整。
- 系统 Chrome seed 1 实测选中小红帽后点击出售区：金币 `6 → 7`、单位数 `3 → 2`、选中状态清空，console 同时收到成功的 `sell` action 与“已回收”反馈；随后战斗正常开始并快速结算胜利。
- `release-notes.png`、`preparation-selected-click-sell.png`、`preparation-generated-portraits.png`、`battle-ai-control.png`、`result-after-skip.png` 均为 `1440×900`，通过纯色、透明与近黑拒绝检查并逐张打开确认；Canvas 为 `1440×853`，无重叠或裁切，54 条战斗事件全部同步到 console，page/console error 与失败请求为空。
- 最终验证：`pnpm autochess:test` `209/209`、`pnpm exec tsc --noEmit --incremental false`、目标源码 ESLint、`pnpm autochess:release:check`、两份浏览器脚本语法、系统 Chrome 专项和 `git diff --check` 均通过。

### 后续可选

- 用更长的托管样本观察满席置换频率和候选席升级素材保留率；当前策略刻意不卖场上单位，也不会为价值不升反降的商店棋腾位。

## 2026-08-06 · v0.2.2 托管真实前瞻、谨慎经济与可恢复轨迹

- 用户连续真人试玩同一种子 `73042`，第二局到第 `21/31` 战、`30,998` 分，明显高于旧托管第 7 战。旧页面在终局时已经卸载 `render_game_to_text` 与 `autoChessAI`，只能确认终局，不能可靠还原操作和站位；本轮没有臆测真人过程。
- 满棋盘与满候选席时，托管会把推演目标候选直接与场上非目标棋交换，避免反复非法上阵后保持选中直到备战超时；固定基准的非法移动和最长连续选中均为 0。
- 阵容不再只按费用与羁绊相加：从启发式、当前阵容、单卡替换和羁绊阈值候选中最多选四组进入真实战斗推演；天赋按下一战真实推演选择。满席换购保护推演目标、同名升级素材和二三星单位。
- 尝试过固定利息保留、边际利息保留、收益档位保留、逐商店战斗推演和强经济门控，均因胜场、通关率或运行时间回退而拒绝。最终只在至少两连胜、真实推演大幅领先时省下跨档付费刷新；候选席全满且正好差一金币升息时，最多出售一个非目标、非升级素材的一星一费单卡。低血量、缺人口和立即合成始终优先投入。
- 协议选择改为每个候选完整预演前三战。第一版把同为三胜时稳扎稳打的额外生命错误当成长期优势，24 局前 12 样本退步；最终版只在预演胜场严格更多时推翻静态偏好，平局继续沿用长期基准偏好。
- 固定 `73000-73023`、前 16 战的 24 局基准：旧安全出售版平均终止战 `13.42`、平均胜场 `10.17`、通关率 `12.5%`；最终版为 `14.38`、`11.33`、`16.7%`，非法移动与选中停滞均为 0。最终产物为 `artifacts/autochess-autopilot-v022-final-24.json`。
- 种子 `73042` 强制协议对照：稳扎稳打第 7 战、辣福灼烧第 12 战、流量续航主线通关并到第 17 战；最终动态前瞻稳定选择流量续航。仍低于真人第 21 战，后续应使用新轨迹继续学习中后期经济与站位。
- 新增 `autoChessAI.actions()`、`render_game_to_text().recentActions` 和每步 before/after 快照；`window.autoChessLastRun` 在终局、组件卸载和同标签页刷新后保留版本、状态与最多 640 条操作，避免真人样本再次随终局丢失。
- 新增 `docs/autochess-autopilot-strategy.md`，明确当前采用分层规则、候选生成与有界真实战斗推演，而非在无高质量数据时训练端到端模型；后续模型应作为合法候选评分器，并以版本化真人轨迹与固定种子留出集晋级。
- 最终验证：`pnpm autochess:test` `213/213`、TypeScript、目标源码 ESLint、脚本语法、`pnpm autochess:release:check` 与 `git diff --check` 通过。系统 Chrome 主流程、AI 专项和托管专项通过；AI 专项记录 54 条战斗事件并全部在 console 找到同 ID，终局 11 条操作在同标签页刷新后完整恢复。主流程 17 张、AI 专项 5 张、托管专项 8 张截图均通过纯色/透明/近黑拒绝检查并逐张打开确认，无黑屏、裁切、文字越界或控件重叠。

## 2026-08-06 · v0.2.3 长局复盘与托管精英进化

- 用户真人演示最终到第 `54` 战、`212,202` 分。旧版只保留当前战约 `320` 条事件、整局约 `500` 条控制台记录和 `640` 条操作，无法完整复盘长局；现将单战及整局事件容量提高到 `100,000`，操作轨迹提高到 `10,000`。
- `autoChessAI.logs()` 改为跨关卡读取并为事件补充 `round`；新增 `autoChessAI.battles()`，逐战保存双方开战站位、星级、战斗属性、完整事件与结算。`window.autoChessLastRun` 同时公开战斗档案、容量和丢弃统计。
- 小轨迹继续写入 `sessionStorage`；事件超过 `5,000` 后改存 IndexedDB，终局、组件卸载和同标签页刷新后仍可恢复，避免浏览器同步存储配额截断长局。
- 从用户提供的终局日志恢复九人阵容：病院坂灵、邪恶外星人、暴龙岁、梨安·终场谢幕、七海大鲨鱼、雪烛Yukisyo、椰子栞、雪绘、北欧魔法师。邪恶外星人在约第 `11` 格中线位置，`8.601s` 仍释放第三次贯穿光线；病院坂灵位于约第 `24` 格前沿角位，显示真人采用远程前推、复活单位承伤，而不是全员最远后排。
- 托管改为有界精英进化：以启发式阵容、当前场上阵容和上一回合冠军谱系组成最多三个父代；真实战斗选出冠军，再产生一个换人变体和两个站位变体。父代继续参赛，只有实战分更高的后代才接班，冠军阵容类型与站位型带入下一回合继续变异。
- 站位型包括真人中线、中央楔形和两翼分散；真人中线将普通远程前推到中线，并让病院坂灵优先落在前沿角位。同一回合相同阵容与站位的评分会缓存，协议选择改为前四战轻量预演，控制同步推演成本。
- 固定 `73000-73023`、前 16 战基准：旧三战前瞻版平均终止战 `14.38`、平均胜场 `11.33`、主线通关率 `16.7%`；精英进化版提升到 `15.50`、`13.21`、`37.5%`，非法移动为 `0`。种子 `73042` 的强制协议对照中，稳扎稳打为第 11 战/6 胜，流量续航为第 16 战/14 胜，辣福灼烧为第 16 战/13 胜，四战前瞻选择流量续航。
- 最终基准产物为 `artifacts/autochess-autopilot-v023-evolution-24.json`，另保存 `73042` 三个固定协议的逐战 JSON；产物目录按仓库约定不纳入 Git。
- `pnpm autochess:test` 共 `216/216`，TypeScript、目标 ESLint、版本一致性与 `git diff --check` 均通过；系统 Chrome 主流程、AI 专项和托管专项通过，事件跨战、终局战斗档案、刷新恢复与 console 同步均已验证。
- 主流程 17 张、AI 专项 5 张与托管专项 8 张截图均通过纯色、透明和近黑拒绝检查，并已逐张人工打开确认；桌面、移动端、全屏、战斗、结算、设置与版本弹窗无黑屏、裁切或控件重叠。

### 后续可选

- 继续收集完整真人 `autoChessLastRun` 样本，用版本化留出种子决定新变异是否晋级；伤害榜若含敌我同名单位，应先按队伍拆分再用于评分。

## 2026-08-06 · 托管第 20 战验收

- 旧 24 局基准在第 16 战截停，因此此前只能确认“最高至少 16”，不能把 16 当成真实终止战。本轮将 `73000-73023` 全部放宽到第 32 战或 `gameover`，真实最高终止战为第 `26` 战，种子 `73001` 与 `73020` 均达到该成绩并获得 23 胜。
- 共 5 个固定留出种子实际赢下第 20 战并进入第 21 战：`73003`、`73005`、`73006`、`73014`、`73020`。其中 `73020` 最终终止于第 26 战，不是靠第 20 战败局残血跨过门槛。
- 基准脚本新增 `--require-win-round`，会统计指定战次的获胜局数，并在任一受测局未赢下目标战时返回失败；新增 `pnpm autochess:verify:autopilot-20`，固定以种子 `73020` 验收第 20 战胜利。
- 完整逐战产物保存在忽略提交的 `artifacts/autochess-autopilot-v023-73000-group32.json` 等四个分组文件中；24 局非法移动与最长选中停滞均为 `0`。
- 最终 `pnpm autochess:verify:autopilot-20` 输出 `requiredWinRoundPasses: 1`、第 21 战后剩余 5 核心并通过；`pnpm autochess:test` `216/216`、TypeScript、脚本语法、版本一致性与 `git diff --check` 均通过。

## 2026-08-06 · 托管经济修正与离线进化训练

- 用户观察托管经常买满候补席、保留无用棋子且缺少利息意识；旧 `73000-73005` 长局样本中候补席满席 67 回合，平均每战利息 `1.88`。
- 已有同名三星时，托管不再购买额外同名棋；布阵后每回合最多出售三张同名一星绝对废件。其他单卡只在真实预演稳胜、非目标、非升级素材且卖一张刚好跨利息档时出售，候补席不必全满。
- 拒绝了普通购买跨利息档硬门控和按候补数量清到 5/7 格的激进方案；第一版平均终止战从 `20.83` 退步到 `18.00`。最终修正版同组六局提升到 `23.17`，平均胜场 `20.17`、主线通关率 `66.7%`、平均每战利息 `2.24`，人口不足与非法移动均为 0。
- 将储备金币、升本节奏、付费刷新预算与稳胜阈值拆为可训练策略向量；新增 `pnpm autochess:train:autopilot`，适应度依次观察总胜场、前 12 战败场、终止战，再用血量、金币与棋子回收价值、升本投入、利息和战斗血量差打破同分。
- 训练与长局验证使用不同种子。两轮短训练冠军都因长局回退被拒绝：第一轮 `23.17 → 22.17`，第二轮独立验证默认 `22.33`、候选 `16.67`；网页默认策略参数未被替换。
- 新增 8-worker 常驻训练池与种子亲和调度；相同 `5×2` 任务由 `254.6s` 降到 `82.7s`，提升 `3.08×`。跨策略 LRU 战斗缓存键包含种子、回合、协议、排序后的天赋、敌方波次、我方阵容/星级/站位，实测命中率 `12.0%`。
- 训练缓存按棋子/波次/天赋数据、战斗引擎和预演源码的 SHA-256 内容哈希持久化到 `artifacts/autochess-rollout-cache/`；同版本第二次小样本重跑未命中为 `0`，首次 `5.3s`、复用后 `4.0s`，平衡来源变化会自动切换新缓存目录。
- 定向回归覆盖三星废件、非满席跨档出售、立即合成、跨实例相同战局命中、不同天赋不命中和同组天赋换顺序命中；当前为 `18/18`，TypeScript、目标源码 ESLint 和三份训练/基准脚本语法检查通过。

## 2026-08-06 · 固定战局训练与最小稳胜经济

- 战斗预演随机性改由协议、排序后的天赋、敌方实际组成/强度和我方单位/星级/站位共同派生；原始游戏种子不再进入最终缓存身份。跨游戏种子但双方开战棋盘相同会复用结果，敌方属性或天赋不同不会误命中。
- 托管在刷新前会利用空候补席暂购已有素材或同店对子，尝试合成二星/三星；本回合未进入目标阵容的暂购棋只在出售确实提升利息时回收。
- 利息出售从“单张刚好补下一档”改为最多 18 个场上/候补候选的动态规划组合，先计算可达到的最高利息档，再以未来价值损失最小的组合逐张出售。
- 真实预演会从冠军阵容中逐个剔除单位，保留固定战局下仍达到稳胜阈值的最小阵容；四名不同理财单位组成的阵容只要稳胜就优先，并保护已经获得的 4 金币利息本金，目标上限为 80 金币/20 利息。
- 专项 `22/22`、TypeScript 与目标 ESLint 通过；尚待固定种子训练、长局留出验证和系统 Chrome 回归后决定是否保留当前默认参数。

## 2026-08-06 · 利息风险档与双种子稳健预演

- 将刷新、好棋购买、合成与升本的经济容忍度改为 `0–20` 档离散利息风险；`0` 档只花当前利息档以上的零头，其他档表示本轮最多允许牺牲的利息档数，同时不突破原有基础现金储备。
- 风险预算锚定备战开始金币，避免连续刷新把“降一档”重复解释成每次再降一档；卖棋产生的瞬时峰值不会永久抬高整轮门槛。普通经济按 `5` 金一档，四理财按 `4` 金一档并正确保护 `80` 金/`20` 利息。
- 商店棋可原价卖回，因此暂购素材和立即合成仍允许先拿再清算；非可逆消费与最终保留由风险档约束。默认保息刷新为 `0` 档，追星、稳血、好棋和计划升本允许花到基础储备；训练器维度扩展到完整 `0–20` 档。
- 基准输出新增逐回合动作计数，便于直接定位经济分叉。拒绝了批量冻结商店候选、扩大到六个真实阵容和单纯提高刷新/稳血阈值：这些方案均未超过第 `17` 战，且扩大候选使单局耗时从约 `29s` 增至 `37s`。
- 每个阵容改用两个由完整战局派生的固定战斗种子复测，并以较差结果作为安全分；缓存键包含预演种子分支，跨实例相同战局仍可复用。`73020` 提升到第 `24` 战、`21` 胜，前 `12` 战全胜并实际赢下第 `20` 战；离线完整长局约 `63s`。
- 最终验证：利息/缓存专项 `26/26`，完整自走棋套件 `226/226`，TypeScript、目标源码 ESLint、脚本语法、版本一致性与 `git diff --check` 通过。正式 `autopilot-20` 输出 `requiredWinRoundPasses: 1`、前 `12` 战全胜。
- 系统 Chrome 托管专项 8 张截图与主流程 17 张截图全部通过非黑屏检查并逐张打开确认；桌面、手机、全屏、整备、战斗、设置和结算无裁切或重叠，`render_game_to_text`、后台推进、console/page error 与失败请求交叉检查正常。

## 2026-08-07 · 商店棋子底色

- 用户反馈商店棋子头像缺少与场上棋子一致的背景色。
- 商店卡、移动端备战席和结算头像改用单位定义中的 `color` 作为头像底板，边框继续使用单位 `accent`。
- 系统 Chrome 商店 tooltip 与移动端 UI 回归通过；桌面 2048×1000、移动横屏 1138×640、竖屏 390×844 截图均非黑屏且头像底色可见，`render_game_to_text`、画布尺寸、console/page error 与失败请求交叉检查正常。
- `pnpm autochess:test` `226/226`、`pnpm exec tsc --noEmit`、目标文件 ESLint 与 `git diff --check` 通过。

## 2026-08-07 · 多风格托管、天眼规划与固定战局缓存修正

- 托管设置新增稳健、均衡、搏上限三种独立风格，并将“天眼商店”作为单独的信息权限；玩家可以组合选择风险偏好和是否预览未来目标。商店 RNG 与战斗 RNG 已分离，预览不会改变真实商店序列或战斗随机状态。
- 运营策略覆盖保息、逐档牺牲利息稳血、暂购商店素材、追二星/三星、组合出售候补席与场上闲棋、四理财冲 80 金/20 利息、预演最小稳胜阵容、危险回合升本及整队救援阵容枚举；训练和基准输出同时记录均值、方差、最低/最高轮次与逐回合动作。
- 修复持久化战斗缓存的身份漏洞：我方战斗 `fid` 现在由棋盘槽位派生，贴边避让方向也只由实际出战顺序决定，战斗档案从 `fid` 还原槽位。相同双方棋盘、天赋和平衡版本不再因购买历史 UID 不同而产生不同结果，新增“同棋盘不受购买 UID 影响”回归测试。
- 旧稳健停止线 `10020` 会把固定双种子预演已经获胜但较慢的 `9989` 附近战局误判为不安全，继续运营后反而可能从胜局改成败局。单变量参数对照后仅将稳健风格的 `safeWinRolloutScore` 调整为 `9975`；现金储备与空搜上限组合实验因效果更差而未采用。
- 最终短样本 `artifacts/autochess-human-policy-v35-survival-fixed-win-threshold-73020-r12.json` 覆盖种子 `73020–73023`：四局前 12 战 `48/48` 全胜、均保持 20 血，平均净资产 `124.5`、平均每战利息 `1.79`，非法移动与选中停滞为 `0`，满候补席仅 2 回合。
- 修正缓存身份后重新生成可信长局 `artifacts/autochess-human-policy-v35-survival-fixed-win-threshold-73020-gameover.json`：种子 `73020` 第 31 战死亡、共 28 胜，第 1–25 战全胜并实际赢下第 20 战；第 12 战凑齐四理财，第 20 战达到 80 金/20 利息，逐战胜负预测与实际结果一致。旧 v33 第 41 战数据受 UID 缓存漏洞污染，不再作为结论。
- 均衡与搏上限保留不同风险分布：同一批种子前 12 战中，均衡平均 `11.5/12` 胜且第 12 战胜率 `75%`；搏上限普通/天眼平均 `11.25/12` 胜且第 12 战胜率 `50%`。本批商店没有出现可利用的未来目标分叉，因此普通与天眼轨迹相同，不代表权限失效。
- 系统 Chrome 托管专项验证三种风格、天眼、后台推进开启/关闭和真实交互；主 `verify-autochess.cjs` 覆盖拖放、两轮战斗、结算、天赋、全屏和 `390×844` 移动布局。共 25 张截图通过非黑屏检查并逐张打开确认，`render_game_to_text`、Canvas、console/page error 与失败请求交叉检查正常。
- 最终验证：AI 控制专项 `43/43`、完整自走棋套件 `244/244`、TypeScript、目标源码 ESLint、四份训练/验证脚本语法、版本一致性与 `git diff --check` 全部通过。

### 后续可选

- 当前天眼是有限未来目标预览，会利用可见刷新序列停止无目标消费，但还不是对完整未来商店树进行全局搜索的规划器；若继续增强，应在独立留出种子上比较均值、方差、最低/最高轮次和运行时，再决定是否替换默认策略。

## 2026-08-07 · 四类托管真实终局评级

- 以固定留出种子 `73020–73023` 对比四类配置：稳健/普通、均衡/普通、搏上限/普通、搏上限/天眼。初始统一跑到第 32 战，仍存活的五条配置/种子单独重放到第 64 战或真实死亡，因此最终数据没有 32 战截断。
- 稳健平均终止战 `32.25 ± 5.45`、平均 `29.25` 胜，最低/中位/最高为 `26/31/41`；前 12 战四局全胜，第 20 与第 24 战胜率均为 `100%`，平均每战利息 `11.88`。这是当前下限最稳的默认策略。
- 均衡同样平均终止战 `32.25`、平均 `29.25` 胜，但标准差为 `7.33`，最低/中位/最高为 `25/30.5/43`；前 12 战全胜率 `50%`，第 20 战胜率 `100%`、第 24 战胜率 `75%`，平均每战利息 `10.84`。它与稳健均值并列，最高上限更高但波动更大。
- 搏上限普通与搏上限/天眼均为平均终止战 `26.75 ± 8.61`、平均 `23.75` 胜，最低/中位/最高为 `18/24/41`；前 12 战全胜率 `25%`，第 20/24 战胜率为 `75%/50%`，平均每战利息 `8.67`。当前参数的“搏上限”既降低下限，也没有提高四局最高值。
- 天眼并非当前最强：四个种子中，搏上限/天眼与普通搏上限的逐回合状态、动作总计、终止战、胜场和净资产全部完全相同。现有天眼只用未来若干商店中“是否存在目标棋”作为继续刷新的布尔门控，不会选择最佳刷新停点或联合规划未来购买；本批路径始终没有触发与普通模式不同的决定。
- 配对终局：`73020` 稳健/均衡/搏上限为 `31/25/25` 战，`73021` 为 `41/43/41`，`73022` 为 `26/35/23`，`73023` 为 `31/26/18`；天眼与每个搏上限结果相同。四类非法移动和最长选中停滞均为 `0`。
- 完整报告保存在忽略提交的 `artifacts/autochess-ai-level-v35-*.json`。一次 `12×40` 大样本尝试在 30 分钟保护线仍未完成且不会增量落盘，已精确终止其四个遗留 Node 子进程；正式 `4×32` 加五条截断重放约耗时 26 分钟，后续应让基准按种子接入 worker 池并逐局写出结果。

## 2026-08-07 · “看穿”托管风格

- 托管设置改为稳健、均衡、搏上限、看穿四种互斥风格；移除单独的“天眼商店”开关，选择“看穿”时自动启用未来商店信息，切换到其他风格时自动恢复普通信息。
- “看穿”沿用稳健风格的运营参数和最差分支预演规则，未来商店信息只作为这一风格的固有能力；旧版已开启天眼的本地设置会自动迁移为“看穿”，新设置只持久化风格。

## 2026-08-07 · 终局截图战报

- 终局页改为可截图传播的完整战报：明确展示到达战线、本局积分、胜负战绩、最终阵容与星级、最终激活羁绊，并移除失败时恒为零的核心血量。
- 每战结束会按棋种累计伤害、治疗、护盾和承伤；终局按实际参战场次展示场均输出、场均治疗/护盾和场均承伤前三，避免早入队棋子仅凭上场更久占据累计榜。同一战多只同名棋合并贡献，但该棋种只增加一次参战场次。
- 战报底部通过 `window.location.origin + window.location.pathname` 读取并显示当前游戏网址，部署域名或路径变化后无需改常量。
- `render_game_to_text()` 同步公开整局总计、参战场次和各项场均值；重开会清空整局统计。
- 系统 Chrome 终局专项构造 9 人最终阵容、8 组激活羁绊与不同参战场次，正常走失败结算进入 `gameover`；桌面 `1440×900` 与手机 `390×844` 均在首屏完整容纳战报，三类榜各显示 3 人，当前网址正确，终局全画面不再出现核心血量。
- 两张终局截图及主流程 17 张截图全部通过纯色、透明和近黑拒绝检查并逐张打开确认；`render_game_to_text()`、Canvas 尺寸、DOM 溢出、console/page error 与失败请求交叉验证正常。
- 最终验证：完整 `pnpm autochess:test` `250/250`、TypeScript、目标 ESLint、发布元数据、主 `verify-autochess.cjs`、终局专项脚本语法与 `git diff --check` 全部通过。

## 2026-08-07 · 50 战快速训练环境

- 旧离线训练仍让每个 worker 使用正式进化规划器：整备期每次买卖、升本和站位决策都会复制局面并执行多场完整物理战斗；单把约 `58.6s`，只能用于最终验收，无法承担策略搜索。
- 引擎新增仅供离线实例使用的无遥测模式，关闭战斗事件、目标观察、短命视觉特效和桥接层操作/战斗快照；伤害、技能、投射物、天赋、敌方实际棋盘和经济流程继续走与网页相同的规则。正式网页默认行为不变。
- 训练规划改为单阵容静态战力估计，不再对每个整备候选暗中逐帧开战，也不执行 24 组合 × 3 阵型的穷举救场；晋级候选仍由原 60Hz 完整进化规划器在独立留出种子上复核。持久化战斗缓存保留给精确复核，快速估分本身无需战斗缓存。
- 尝试将训练战斗降为 30Hz：8 个相同种子只有 `3/8` 的终止轮和胜场与 60Hz 一致，虽然达到约 `210 把/分钟`，但误差不可接受，已拒绝作为默认。60Hz 快速训练仍达到 `148.92 把/分钟`，因此默认保持精确战斗步长。
- 训练/验证战数上限从 32 提高到 64，`pnpm autochess:benchmark:training` 固定以 50 战封顶；提前死亡按真实终止轮记录，不会伪造存活到 50。报告新增平均值、标准差、最低/最高终止轮、50 战完成率、worker 单把耗时和墙钟吞吐。
- 8 worker 的正式 50 战吞吐样本共 16 把，`6.45s` 完成，折算 `148.92 把/分钟`，worker 单把平均 `1.82s`。扩大到 8 策略 × 4 代 × 8 种子后，`256` 把在 `56.76s` 完成，折算 `270.60 把/分钟`。
- 快速训练候选从平均 `15.13`、最低 `8`、最高 `18` 提升到平均 `17.00`、最低 `11`、最高 `21`，仍远低于真人 54 战和 40 战目标；该候选尚未通过完整规划器留出复核，因此没有替换网页默认策略。当前结论是训练吞吐已解决，后续瓶颈是中后期策略与目标阵容质量。
- 最终验证：完整自走棋套件 `250/250`、AI 专项 `47/47`、TypeScript、目标源码 ESLint、两份训练脚本语法和 `git diff --check` 全部通过。浏览器标签由用户使用，本轮未导航或刷新，也未进行会干扰当前对局的页面自动化。

## 2026-08-07 · 全三星终局目标与候补席归属训练

- 终局单战基准改为无遥测/无特效模拟，按测试战次自动带入当时已经获得的战斗天赋，固定阵型汇总，并支持逐棋种星级；避免以“每场偷选最优站位”或无天赋的失真战局评估 40–50 战。
- 全三星 `40/45/50` 战搜索中，旧真人 9 人骨架为 `0/6`；新 10 人终局为病院坂灵、邪恶外星人、暴龙岁、梨安、七海、蛙梓、轴轴的宝、露蒂丝、北欧魔法师、雪绘，中央楔形达到 `5/6`。9 人版本移除雪绘，达到 `4/6`。
- 终局目标改为上述 10 张全部追三星；雪烛作为二星四理财过渡牌，终局完成后解除保护并允许退役。候补保护从“每种目标留一张”改为按目标复制数保留到 9 份，商店也会持续购买未完成目标。
- 托管的候补归属现在区分当前出战阵容、终局复制、四理财过渡和真正合成项目。板凳少于 3 个空位时主动清理无归属牌；不在当前最强阵容的非目标一星、二星、三星均可出售。单个离场二星不再自动成为长期追三项目，两个二星等高进度项目仍可保留。
- 快速训练报告新增终局拥有种数、目标三星数、复制完成率、候补数量和无关候补数量，并在同轮次/胜场下奖励终局建设。`8×4×8` 共 256 把在 `60.22s` 完成，约 `255.05 把/分钟`。
- 快速训练的稳定候选将平均终止战从 `15.63` 提到 `16.75`，终局拥有从 `4.5` 提到 `5.75` 种；另一高波动候选最高 `25` 但最低降至 `10`，未作为稳健参数晋级。
- 四个独立留出种子的 50 战完整演化复核中，旧默认平均终止战 `26.0`、最低/最高 `21/31`；晋级候选为 `31.75`、最低/最高 `27/36`，平均胜场 `23→28.75`，终局复制完成率 `34.7%→43.6%`，目标三星 `0.25→0.75`，早期平均败场同为 `0.75`。候选通过晋级条件并已成为稳健托管默认参数。
- 研究与验证产物保存在忽略提交的 `artifacts/autochess-lineup-terminal-all3-v1.json`、`autochess-lineup-terminal-star-order-v1.json`、`autochess-terminal-plan-training-v1.json` 和 `autochess-terminal-plan-exact-validation-v1.json`。
- 固定 `73020` 验收实际赢下第 20 战；截停于第 21 战时仍有 12 血，前 12 战全胜，非法移动和最长选中停滞均为 0。
- 最终验证：完整自走棋套件 `251/251`、AI 专项 `48/48`、TypeScript、目标源码 ESLint、三份训练/基准脚本语法、固定第 20 战验收与 `git diff --check` 全部通过。
- 浏览器标签由用户使用，本轮未导航、刷新或执行浏览器自动化。

## 2026-08-07 · 52 战真人轨迹、项目冲刺与终局牌表

- 完整恢复用户第 52 战、`195,554` 分真人对局：共 `616` 次操作、`52` 场战斗、`17,339` 条战斗事件，轨迹丢弃数为 `0`。原始 18.3 MB 轨迹保存在 `artifacts/autochess-human-runs/2026-08-07T16-06-40-050Z-round-52-score-195554.json`，增强逐回合报告保存在同名 `.analysis.json`。
- 本局 `49/52` 胜，败场仅为第 `12/51/52` 战；第 8 战凑齐四理财，第 11 战达到 20 利息，第 20 战十本十人口，第 28 战场上十张全三星。最终状态仍有 `1,933` 金，说明后期瓶颈是阵容与敌方强度，不是经济。
- 真人核心节奏被还原为：第 20–23 战用 80 金以上余额搜索并完成 3 张三星；第 24 战从 138 金集中搜到 3 金，把三星数从 3 提到 6；第 25 战停搜恢复到 102；第 26 战搜到 23 金；第 27 战再次停搜；第 28 战守住 80 金完成十张三星。AI 新增“至少两个终局项目达到 6/9 份时允许较低存款冲刺，下一回合因起始金币不足自动停搜”的分支。
- 第 28–50 战长期连胜阵容为邪恶外星人、梨安、小岁鸟、雪烛、蛙梓、雪绘、暴龙岁、七海、北欧、病院坂灵。第 51 战首败后临时换入塔神、露蒂丝，第 52 战仍败，因此没有把临时终局照抄为目标。
- 固定 `40/45/50/51/52` 战全三星对比：首轮 3 种子中真人长期阵容 `9/15`、旧 AI 目标 `8/15`、临时终局 `5/15`；独立 8 种子留出中真人长期阵容与旧 AI 均为 `21/40`，但平均余量 `-0.97` 优于 `-2.20`，最差余量也略优。终局目标据此用小岁鸟、雪烛替换轴轴的宝、露蒂丝，并全部追三星。
- 快速训练 `8×3×8` 共 208 把在 47.56 秒完成，约 `262.41 把/分钟`。完整 60Hz 留出种子 `98000–98003` 中，训练冠军相对新基线平均终止战 `26.25 → 33.25`、最低 `21 → 27`、最高 `35 → 39`、平均胜场 `23.25 → 30.25`，早期败场同为 `1.0`；终局三星 `1.5 → 2.5`、复制完成率 `45.3% → 57.8%`。四局分别终止于 `37/39/27/30`，通过晋级条件并成为稳健默认参数。
- 本地开发环境在 gameover 时会把完整轨迹同时写入 `autochess-human-recovered-latest.json` 和按时间/回合/分数命名的历史文件；刷新后也会从 IndexedDB 恢复并补归档。分析器新增开战前金币/利息、掉血、三星进度、集中搜牌窗口、首次十人口/十三星和最终阵容统计。
- 最终验证：完整 `pnpm autochess:test` `255/255`、TypeScript、目标源码 ESLint、三份训练/分析脚本语法与固定种子第 20 战验收全部通过；种子 `73020` 在截停的第 21 战仍有 13 血，实际赢下第 20 战，非法移动和最长选中停滞均为 0。
- 系统 Chrome 主流程覆盖标题、购买、拖放布阵、两轮战斗、结算统计、天赋、全屏与 `390×844` 移动布局；17 张新截图全部通过纯色、透明和近黑拒绝检查并逐张打开确认，`render_game_to_text()`、Canvas 尺寸、console/page error 与服务请求交叉检查正常。

## 2026-08-08 · 分本固定商店序列与“看穿”动态规划

- 商店序列改为 `f(对局种子, 玩家等级, 该等级第 N 次商店)`；3–10 本分别维护独立游标，低本刷新不会消耗高本未来商店。新增无副作用的 `previewFutureShopsAtLevels(levels)`，可以预览任意升本路线，同时不改变真实商店、玩家等级或战斗随机状态。
- 开局首店仍保持旧版首战战斗 RNG 起点，避免商店随机源改造让无关战斗平衡样本整体漂移。回归覆盖相同种子/等级/序号的稳定性、等级间互不消耗、交错预览与实际刷新一致、预览无副作用。
- 新增压缩状态规划器 `seerPlanner.ts`。状态包含回合、等级、金币、升级剩余费用、血量档、连胜档、各本商店游标和十张终局目标牌份数；动作包含目标等级与刷新 `0 / 零头 / 2 / 4 / 8 / 12 / 24` 次，并自动购买目标牌。
- 规划器对同一压缩状态保留金币更多的路径，正式模式逐层保留前 `96` 个状态并规划 `8` 回合，训练模式为 `64` 个状态和 `6` 回合。每个整备回合只执行最优路线的第一步，下一回合根据真实血量、金币、阵容与新商店滚动重规划；这是带支配剪枝和 beam 上限的近似动态规划，不是无上限穷举。
- 只有“看穿”风格会读取未来商店并创建规划；稳健、均衡、搏上限不访问未来信息。当前真实战斗预演低于 `10050` 安全线时，规划器无权要求升本或保钱，仍由原有稳血搜索接管，避免为了远期牌表直接掉血出局。
- 12 个种子 × 4 风格的 50 战快速样本保存在 `artifacts/autochess-four-styles-dp-safe-10050-fast-142000-r12-b50.json`：看穿平均终止于 `15.92` 战，略高于稳健的 `15.25`；前期平均败场 `1.83` 对 `2.08`，终局复制完成率 `18.5%` 对 `16.6%`，墙钟吞吐约 `60.48 把/分钟`。
- 同一种子 `152000` 的完整 60Hz 物理战斗复核中，稳健在第 `21` 战死亡、`18` 胜、净资产 `324`；看穿在第 `28` 战死亡、`25` 胜、净资产 `782`，前 `18` 战全胜，第 `8` 战凑齐四理财，第 `15` 战达到 20 利息。报告分别为 `artifacts/autochess-survival-dp-exact-152000-r1-b40.json` 与 `artifacts/autochess-seer-dp-exact-152000-r1-b40.json`。
- 一次四风格 × 两种子的完整物理批处理在 15 分钟保护线超时且没有落盘，相关进程已精确终止。因此当前只能确认看穿在快速多种子样本中排名第一、在一个精确同种子对照中明显优于稳健，不能据此宣称所有种子都“天下无敌”。后续优先让精确基准逐局增量落盘，并把单一终局目标扩展为少量可按敌方阵容切换的终局模板。
- 最终验证：`pnpm autochess:test` `258/258`、AI 专项 `55/55`、TypeScript、目标源码 ESLint、修改脚本 `node --check` 与 `git diff --check` 通过。系统 Chrome 主流程 17 张和 AI 专项 5 张截图均通过拒黑检查并逐张打开确认；桌面、全屏、手机、整备、战斗、结算、天赋和托管控制无新增黑屏、裁切或重叠。

## 2026-08-08 · 人口硬约束、候补席压力换阵与 CPU 预演分层

- 用户反馈均衡等 AI 会把大量棋子堆在候补席，甚至在战力不足时仍不开满人口；看穿还会保护过多终局项目，多个一星项目互相卡住无法升星。
- AI 新增人口硬约束：当总阵容已有足够棋子且目标阵容达到人口上限时，整备流程先把目标候补棋移入空场位，再升本、刷新或购买；只有总棋子不足人口上限时才继续买棋补人口。
- 候补席压力阈值按风格配置：默认剩余不超过 2 个空位就进入换阵/清理阶段。均衡在压力下会先用战斗预演判断“卖一张候补棋再买当前商店棋”是否提高战力，不再等候补席已经 8/8 才处理；没有直接替换方案时，会先清理明确闲置的候补。
- 非看穿风格在压力下允许出售“场上已有二星、候补仅一星、且不是最后一个合成项目”的终局重复件；看穿继续保留自己的未来项目归属。这样既释放候补席继续搜牌，也不随意拆掉唯一的合成素材。
- 新增回归覆盖：均衡候补席接近满时先卖暂存棋再买直接替换牌；有足够候补时连续补满人口后才升本；均衡残血且 `10/10 + 8/8` 时出售场上已有高星对应的低星终局重复件；看穿项目分流保护主项目。AI 专项由 `55/55` 增至 `59/59`。
- 预演效率分层：网页托管默认使用 30Hz CPU 预演；稳定性预演默认 2 个分支，换阵先以 30Hz 筛选最多 5 个候选，再对最优候选做 60Hz 复核。显式 `rolloutHz=60` 的审计仍保留完整 4 分支和 2 个最终候选。`benchmark-autochess-styles.mjs` 与 `benchmark-autochess-autopilot.mjs` 输出实际 `rolloutHz`，避免把快速筛选和精确审计混淆。
- 60Hz 不是等待现实时间，而是每个候选战斗最多执行 `26 × 60 = 1560` 次引擎更新；此前四风格各一局、24 战精确审计约 `93s / 2.59 局每分钟`。30Hz CPU 16 战筛选约 `8.87 局每分钟`，日常训练仍使用更快的静态训练路径。
- 均衡固定种子 `122000`：30Hz CPU 版由第 23 战失败改善为跑到第 24 战、22 胜；60Hz 审计版跑到第 24 战、24 胜，前 12 战全胜，非法移动为 0。两版只有第 1 战因总棋子不足为 `3/4`，该战实际获胜；后续人口均满。完整均衡报告见 `artifacts/autochess-balanced-pressure-run-v3.json` 与 `artifacts/autochess-balanced-pressure-run-exact60.json`。
- 最终验证：AI 专项 `59/59`、TypeScript、训练/基准脚本语法与 `git diff --check` 通过；待完成本轮系统 Chrome 主流程截图复核。

## 2026-08-08 · CPU 极限快进与精确审计分档

- 明确 60Hz 预演不等待现实时间：慢点来自每个候选战斗最多执行 `26 × 60 = 1560` 次 `engine.update(1/60)`，以及看穿的多候选、多分支重复预演。
- 训练 worker、四风格快速基准和单风格基准现在都能显式传入实际战斗快进频率；快速路径默认 30Hz，`--battle-hz 20` / `--training-hz 20` 可用于 CPU turbo 粗筛，60Hz 仍保留为精确留出审计。
- `benchmark-autochess-styles.mjs` 与 `benchmark-autochess-autopilot.mjs` 的报告新增 `battleStepHz`，和已有 `rolloutHz` 分开记录，避免把战斗结算频率与 AI 预演频率混淆。
- 同种子均衡 16 战实测：20/20Hz 约 `25.8s`、14 胜；30/30Hz 约 `17.3s`、15 胜；60/60Hz 约 `30.7s`、16 胜。四风格 16 战快速批处理默认 30Hz 约 `72.04 把/分钟`，精确 60Hz 约 `5.26 把/分钟`。
- 新增 CPU 快进回归，AI 专项 `60/60`、TypeScript、脚本语法和 `git diff --check` 通过；待完成小规模训练烟测及最终静态检查。

## 2026-08-08 · CPU 极限档默认化与当前真实基线

- 训练脚本和 `autochess:benchmark:training` 默认训练快进由 30Hz 调为 20Hz；四风格 `planning=fast` 的默认 `rolloutHz` / `battleStepHz` 同步为 20Hz。显式传入 `--training-hz 30`、`--battle-hz 30` 或精确模式 60Hz 仍可复现中间档和审计档。
- 同一种子均衡 16 战实测：20/20Hz 约 `13.3s`、16 胜；30/30Hz 约 `22.2s`、15 胜；60/60Hz 约 `45.4s`、15 胜。差异来自离散步长近似，不是现实时间等待；20Hz 适合 CPU 粗筛，60Hz 只用于精确留出。
- 20Hz 四风格快速筛选 `16` 把约 `11.2s`、吞吐 `85.93 把/分钟`；均衡均值 `22.5` 战、最高 `32`，稳健均值 `18.25`、最高 `25`，搏上限均值 `16`、最高 `27`，看穿均值 `17.25`、最高 `18`。该路径只用于训练/筛选，不能替代真实战斗预演。
- 30Hz 真实进化预演 `8` 把、50 战耗时约 `597.95s`，因此不再整批运行四风格精确审计。当前报告 `.tmp/autochess-current-evolution30-r2-b50.json`：稳健两局均跑满 50；均衡 `32/50`、`50/50`；搏上限 `36/50`、`50/50`；看穿 `36/50`、`50/50`。终局三星均值分别为 `8.5/3.5/5.5/5.0`。
- 20Hz 训练烟测、完整自走棋 `263/263`、TypeScript、四个脚本 `node --check` 和 `git diff --check` 通过。

## 2026-08-08 · Go级第五策略（进行中）

- 新目标是最大化长局存活回合，并与正在优化原四套策略的工作隔离；Go级作为独立第五种托管风格接入，默认拥有未来商店 oracle 信息。
- 对用户提供的第 52 战真人轨迹和第 51–56 战固定战局做离线搜索后，确认经济不是终局瓶颈：真人死亡时仍有约 1900 金。固定十人阵容无法同时适配后期波次，第 52 战由 `cog_scribe` 替换 `yukisyo` 更好，第 54 战由 `rutice` 替换 `yukisyo` 更好。
- Go级终局目标改为 12 张全三星候选池；保留旧十人并加入 `cog_scribe`、`rutice`。另外四种风格继续使用原十张终局表。
- 阵容搜索采用 sequential-halving 式预算分配：最多 6 个阵容先跑一个真实战斗分支，保留前 3，再跨 `human_recorded`、`human_midline`、`center_wedge`、`split_flanks` 四套站位做稳健复核。真人记录站位只对 Go级开放。
- 新增独立 `autochess-go-strategy.test.mjs`，避免改写另一套 AI 正在扩展的测试文件；当前专项 `3/3`、TypeScript、四份基准/训练脚本语法与 `git diff --check` 通过。
- 待完成：独立种子长局对照、完整回归、系统 Chrome 设置入口与实际托管截图验证，以及根据实测决定是否调整搜索预算和经济参数。

### Go级动态转追、计划复用与长期战斗缓存

- 新多步 oracle 规划与动作顺序基线在 30Hz 固定种子 `152010/152011` 分别到第 `49/57` 战，平均 `53`，首次在双种子调参样本上超过真人第 52 战均线；报告为 `artifacts/autochess-go-v9-tuning-152010-r1-b64-hz30.json` 与 `autochess-go-v9-tuning-152011-r1-b64-hz30.json`。该结果尚非独立留出结论。
- Go级项目选择从“按已有份数固定追三个”改为每轮按现有份数、候补占位、当前商店、未来固定商店中的最早三星刷新数动态选择；带两次刷新等价的轻微滞后，密集来牌仍可立即替换旧目标。
- 动态重点集合同时驱动规划、购买、升级项目保护与候补清理。非重点低进度项目可出售；当前阵容、差一张合成项目和任意位置的三星继续硬保护。
- 修复 Go 多步计划复用校验错误：此前三个动态目标被拿去与全部 12 个终局目标比较，导致计划几乎不能跨回合复用；现在只校验计划实际目标，并把其他终局牌作为过渡阵容核对。
- 持久化战斗缓存版本从“整个托管 AI 源码哈希”解耦为“核心数据/战斗引擎哈希 + 独立 rollout schema”。运营策略调整不再让整批战斗缓存换目录失效；战斗规则、分支或评分语义改变时需升级 `rolloutCacheSchema.ts`。
- 本机确认可用于后续学习型评估器：RTX 5080 16GB、PyTorch 2.11 CUDA 可用，i5-13600K 14 核 20 线程。建议固定训练种子池与战斗分支池、保留独立留出种子，用缓存标签训练候选排序网络，再只对 Top-K 做真实战斗复核。
- 当前验证：Go 专项 `7/7`、AI 控制专项 `67/67`、TypeScript、训练脚本语法及目标 `git diff --check` 通过；动态转追同种子 A/B 仍在运行。

### Go级 v12 冠军、基准持久缓存与学习型排序可行性

- 动态转追 v10 在 `152010/152011` 为 `54/48`，平均 `51`；增加项目承诺成本的 v11 为 `53/52`，平均 `52.5`。两者都没有达到预设的平均 `53` 晋级线。v12 在首次进入终局时以当前高进度项目作为初始承诺，再允许当前密集来牌替换，修复前达到 `58/53`。
- 逐回合复盘发现候补清理把唯一一张候补三星误当成“已存在另一个三星的重复件”：`completedDuplicates` 的同名三星搜索把棋子自己也算了进去。现在必须存在不同 UID 的同名三星才属于完成后重复件，并新增“唯一候补三星不可出售”回归。最终代码重跑 `152010/152011` 达到 `57/60`，平均 `58.5`、最低 `57`，成为当前 Go级冠军。
- 第一个完全未参与调参的种子 `152100` 使用 30Hz 预演、60Hz 实战：修复前第 `47` 战、修复后第 `46` 战。该结果未超过真人第 `52` 战，因此只能声称 Go级显著提高了当前上限和调参池下限，不能声称已经跨种子稳定超人。
- 精确基准脚本此前只有进程内缓存，v9-v12 每次结束都会丢失几千条标签。现在 Go级基准默认按核心数据/战斗引擎哈希、固定种子区间和预演频率读取并原子写回持久缓存，可用 `--no-rollout-cache` 关闭或用 `--rollout-cache` 指定路径。
- `152100` 首跑写入 `8,176` 条标签、耗时 `395.4s`；相同种子修正版重跑加载全部标签，命中 `7,538`、未命中 `77`，约 `99.0%` 命中，耗时降到 `67.4s`，加速约 `5.87x`。随后两个冠军种子又分别持久化 `6,780/7,940` 条标签。
- RTX 5080 CUDA 原型证明训练本身不是瓶颈。单种子 `4,658` 个独立场景的普通价值回归只需 `1.54s`，胜负准确率 `88.6%`，但 pairwise 排序仅 `60.8%`；三个种子 `13,173` 个场景改用 pairwise ranking 后训练 `11.2s`，排序提高到 `68.2%`，中位 Top-1 regret 为 `103`，平均仍为 `1,696`。当前模型不足以独立接管决策，后续应使用结构化单位/星级/站位 embedding 批量筛选，再由真实模拟复核 Top-K。
- 后续训练应采用 common-random-numbers：固定 256 个商店种子、每状态 4 个固定战斗分支，另留 64 个永不训练的种子。固定的是一个覆盖池而不是单一种子，避免缓存失效和单卷过拟合；策略/价值网络引导 beam 或 MCTS，再把搜索专家轨迹蒸馏到运营策略，不在三小时预算内从随机买卖动作直接 PPO 起步。
- 最终专项验证：Go `7/7`、AI 控制 `67/67`、TypeScript 和修改脚本语法通过，`git diff --check` 无空白错误。完整套件为 `276/277`；唯一失败是另一套并发工作正在修改战斗引擎后，旧断言“十名三星高费阵容在第 42 战必须失败”不再成立。本轮没有回退或覆盖该并发修改。此前系统 Chrome 主流程与 Go级桌面/移动入口共 25 张截图已逐张检查，未发现黑帧、裁切、重叠、console/page error 或失败请求。

## 2026-08-08 · 看穿2迁移与真正 Go级 GPU 管线（进行中）

- 原 Go级的 12 张终局池、动态转追、六进三 sequential-halving 和真人记录站位已迁移到独立 `seer2Strategy.ts`，托管标识与界面名称改为“看穿2”。
- 托管设置存储格式升级到版本 2；旧版本保存的 `go` 会迁移成 `seer2`，新版本明确选择的 `go` 保持为真正 Go级。
- `AutopilotStyle`、训练/基准脚本、EngineBridge 和桌面/移动验证入口已同时接入 `seer2` 与新的 `go` 标识。
- 第一阶段验证通过：TypeScript 无错误；看穿2专项 `7/7`。审计确认当前新 `go` 仍临时共享看穿2终局基础，下一阶段必须在学习模型接入时拆除，避免成为旧人工策略换皮。
- 正在实现：从持久战斗缓存解析结构化棋子/星级/站位/敌军/天赋上下文，使用 CUDA 训练 pairwise 排序模型，再由新 Go级用模型筛选扩大后的候选池并以真实战斗复核 Top-K。

### GPU 排序器、真实 Go 搜索与暂停点

- 已新增 `train-autochess-go-value.py`：读取持久战斗缓存，按整份种子文件隔离外部留出，使用玩家/敌方单位、星级、站位、开局协议、天赋和波次强度训练 DeepSets 排序器；模型额外读取每个单位 42 个静态机制特征（基础数值、回能、远近程、18 类羁绊、9 类施法触发）。
- 已新增 `generate-autochess-go-combat-dataset.mjs`：跳过完整运营流程，直接用 common-random-numbers 生成阵容/站位战斗标签。64 个上下文、每个 12 套阵容 × 4 站位 × 2 分支，共落盘 6,072 个去重标签；墙钟 271.38 秒，约 22.37 场/秒。
- RTX 5080 上首轮普通 pairwise 训练 6.60 秒，但完整未见轨迹 `152100` 只有 59.3% pairwise，证明此前随机切分的 68.2% 高估了跨种子泛化。增加随机战斗覆盖没有改善该轨迹，说明不能把“GPU 训练很快”等同于“模型已经会玩”。
- 改为分差加权 pairwise + listwise 后，训练集 Top-6 最佳候选保留率超过 96%，但未见完整轨迹 Top-24 仍只有 60%–75%，出现明显上下文记忆。加入 42 个单位机制特征后，未见 `152102` pairwise 约 60.1%，Top-24 最佳候选保留率约 76.0%；它仍不够独立决策，只能作为保守剪枝器。
- 浏览器推理模块 `goValueModel.ts` 与紧凑权重 `goCombatModel.json` 已接入。5 条 CUDA 留出样本在 Node 中逐项复算误差小于 `2e-4`，排除了词表错位、矩阵转置和池化顺序偏差。
- 真 Go级当前采用学习引导 beam：神经网络扫描已有候选和单换扩展的数百至上千个“阵容+站位”，强制保留当前阵容、启发式阵容和理财阵容，再由真实战斗首轮复核最多 24 个、稳健多分支复核最多 5 个。Go 不进入看穿2四条固定主阵容、六进三分支或 `human_recorded` 真人站位。
- 第一版错误地把看穿2已验证的动态商店规划也从 Go 切掉。未见种子 `152102` 因此停在 9 人口并于第 23 战死亡，20 胜、净资产 459；首跑 4,834 cache misses、最长单次 tick 10.46 秒。对照中，同种子看穿2与稳健都跑满第 50 战、49 胜、12 血，证明该失败主要是运营边界退化，不是可以接受的模型探索代价。
- 已修正边界：Go 保留用户要求的看穿2 oracle 经济规划、动态按来牌转追和持久战斗缓存，但阵容/站位选择仍由神经 beam + 真实复核负责。修正后专项与 TypeScript 通过，尚未完成长局验收。
- 当前专项：Go `3/3`、看穿2 `7/7`、TypeScript 通过。Go 专项覆盖 CUDA/Node 一致性、看穿2阵容搜索隔离、模型宽搜和 Top-K 真实复核预算。
- 用户要开始使用显卡后，已终止本任务的修正版 `152102` 长局基准 PID 52540；该进程没有正常结束，因此没有报告或缓存回写。所有 CUDA 训练进程此前已结束。另一个 AI 的四风格 Node CPU 基准 PID 65640 保留未动。

### 下一阶段：两套固定敌方种子与唯一规范站位

- 用户接受先针对固定敌方战役训练，不要求立刻跨任意随机种子泛化。建议明确两套 60 关敌方种子：一套训练/主战役，一套防止完全死记的交叉验证；不要让商店随机种子和敌方波次种子继续隐式绑在同一个 `state.seed` 上。
- Go级只使用一个确定性的规范站位函数：按单位 ID、星级、远近程和防御/射程排序后分配固定槽位；同一阵容、同一星级组合永远得到同一 placements 字符串。删除 Go 对三至四个站位模板的重复扫描，缓存键空间立即缩小约 3–4 倍。
- 敌方每套种子固定 60 关、每关固定少量公共战斗分支；训练、基准和网页托管共享完全相同的 `(enemySeed, round, lineup, canonicalFormation, branch)` 缓存键。缓存长期保留，只有战斗规则或评分 schema 改变才失效。
- 先对两套敌方种子的全部 60 关离线填充常见三星阵容及单换/双换邻域，再训练模型引导组合 beam；模型 Top-K 之后只补算缓存未命中的阵容。目标是热缓存时网页整备 tick 降至 100ms 量级，而不是继续容忍 6–10 秒卡顿。
- 下一次继续时先跑被中止的“保留动态经济”修正版，但应先实现规范站位和固定 enemy seed 解耦，避免再为多站位产生数千个无复用标签。验收至少包含两套 60 关种子的存活回合、胜场、最大 tick、cache hit/miss、最终三星阵容和净资产；没有同时改善存活与交互性能，不晋级为默认 Go级。

## 2026-08-08 · 60 战上限终局验证

- 确认引擎没有第 50 战结束条件：第 16 战胜利后解锁普通无限，第 32 战后进入地狱无限，只有 HP 归零才会进入 `gameover`。四风格基准上限已改为 60（脚本允许显式传到 64），因此“跑满 60”才代表 60 战内未输，不代表人为截断。
- 四风格真实进化横评使用种子 `84000–84001`、每类 2 局、`planning=exact`、规划/实战 `30Hz` CPU 快进、4 worker，报告为 `artifacts/autochess-four-styles-evolution30-r3-b60.json`。8 局耗时 `873.86s`，约 `0.55 局/分钟`；没有任何一局跑满 60，全部是 HP 归零后的真实终止。
- 逐局终止战：稳健 `43/27`（均值 `35`，最低/最高 `27/43`）；均衡 `38/38`（均值 `38`，最低/最高 `38/38`）；搏上限 `40/58`（均值 `49`，最低/最高 `40/58`）；看穿 `52/51`（均值 `51.5`，最低/最高 `51/52`）。当前四类中看穿的均值最高，搏上限的偶发最高值最高；看穿已经实际越过第 50 战。
- 看穿2独立留出种子 `152100` 使用 `rollout=30Hz`、`battle=60Hz`、上限 60，报告为 `artifacts/autochess-seer2-holdout-152100-r3-rollout30-battle60.json`：第 `47` 战真实失败、`44` 胜；比该种子之前第 46 战结束多存活 1 战，但仍不能据此宣称跨种子稳定超过真人第 52 战。
- 这次证明“看穿不如其他三种”的旧印象同时混入了 50 战封顶和旧版本策略；当前看穿不是被 50 截断，而是在 51/52 战的地狱无限中真实失败。四类横评长局后段耗时明显上升，后续效率优化重点是减少已毕业阵容阶段的重复候选预演，并保留真实战斗复核。
- 回归：AI 控制专项 `68/68`、看穿2/Go 专项 `10/10`、TypeScript、修改脚本 `node --check` 和 `git diff --check` 通过；完整 `pnpm autochess:test` 为 `280/281`，唯一失败是并行引擎数值改动导致“十名三星高费阵容在三十二战后必须失败”的旧断言不再成立，本轮没有回退该并发改动。

## 2026-08-08 · 看穿 60 战路线执行校准

- 看穿规划器继续以第 1–60 战的敌方波次、各等级未来商店和 60 步 beam 前缀为完整路线；每个步骤现在同时记录预期波次、战斗分差、胜负和运营状态，跨回合复用前会核对实际敌方单位/星级、商店、人口、候补归属和目标复制数。
- 修复看穿在第 32 战以后“精确预演已深度负分，但抽象路线仍继续执行”的执行漏洞：计划首步预计不会主动卖血，且当前精确分数低于 `-500` 时立即废弃旧宏计划；血量已经低于受伤阈值时，即使计划原本允许卖血也强制切换稳血逻辑。计划销售可从候补或场上执行，计划购买/刷新不再满足条件时会失效重算。
- 看穿 planner 的后半程败局模型不再把所有败局压成同一个 `-240`；第 32 战后保留当前精确败局的负分幅度，前期仍沿用已验证的经济路线，避免为了后期风险破坏第 16 战附近的利息和升本节奏。
- 固定种子 `152100`、20Hz 规划/战斗快进、60 战上限复核：前 32 战 `32/32` 全胜、20 血；第 52 战实际掉血后仍连续赢下第 53–58 战，真实终止在第 60 战，共 `57` 胜。第 52 战的候补三星替换逐项精确复核仍无直接胜解，说明该波需要更早的路线积累，不是遗漏一次布阵动作。
- A/B 拒绝把看穿终局池从 10 张直接扩大到 12 张：包含轴轴/露蒂丝的全目标版本在同一种子第 36 战死亡，目标分散超过固定主线；保留 10 张主线和临时过渡换阵。
- 新增回归覆盖“低血量不执行精确必败旧路线”和“轻微败局与深度败局分数保持区分”；AI 控制专项 `71/71`、TypeScript 与 `git diff --check` 通过。长局产物为 `artifacts/autochess-seer-oracle60-152100-r17-b60-hz20.json`。

## 2026-08-08 · 看穿救援换位与 60 战终局复核

- 修复救援搜索的执行状态：同一战斗局面按棋子归属、星级和战斗 RNG 去重，纯棋盘槽位重排不再重复枚举；买卖、升星或场上/候补归属变化仍会触发重新搜索。
- 救援搜索只有找到可执行胜局才锁定；锁定后整备动作优先完成实际换位，完成后直接开战，不再落回普通站位逻辑覆盖救援阵容。
- 后期满人口只接受合法的直接板位替换/两换方案；禁止把候补棋直接移动到空棋盘槽位造成“当前只能上阵 N 名单位”循环，并增加防御性开战兜底。
- 新增 AI 回归：同一局面不重复搜索、升星后允许重搜、救援锁期间不购买/刷新。AI 控制专项 `74/74`，TypeScript 通过。
- 固定种子 `152100`、看穿、60 战上限、20Hz 规划/战斗：冷启动真实跑到第 `60` 战、`58` 胜、最终 HP `4`；第 48 战 `1` 次换位获胜，第 51 战 `8` 次换位获胜，第 55 战 `2` 次换位获胜；非法移动 `0`、选中停滞 `0`。第 52 和 59 战为真实败局，但均未立即终局，后续继续作战。
- 同一持久缓存热跑结果完全一致（第 60 战、58 胜、HP 4），rollout 命中 `7103`、未命中 `0`；托管整备推演耗时由约 `519s` 降到约 `154s`。缓存文件位于忽略的 `.tmp/`，不作为提交产物。
- 完整自走棋套件 `286/287`；唯一失败是已有的“十名三星高费阵容会在三十二战后遇到终局压力”数值断言，因并行引擎数值改动使该阵容当前仍获胜，与本轮 AI 修复无调用关系。

## 2026-08-09 · Vercel AI lint/build 修复

- 修复 `AutoChessAutopilot.ts` 和 `seerPlanner.ts` 的 `prefer-destructuring`、`no-confusing-arrow`、`no-shadow`、`no-loop-func` 报错；循环内候选筛选改为等价显式迭代，避免捕获可变规划状态。
- 修复规划器 `Math.max` 产生的 `number` 到 `PlayerLevel` 类型错误。
- 验证：目标文件 ESLint、TypeScript、`git diff --check` 和干净 `pnpm run build` 均通过；Next.js 成功生成 39 个静态页面。
- 当前完整 `pnpm autochess:test` 为 `285/287`；两个失败为当前工作区已有的看穿首步等级断言与后段高费阵容压力断言，本轮未改变相关策略意图。

## 2026-08-09 · 真 Go 规范战役与公共缓存分支（进行中）

- `GameState` 新增独立 `enemySeed`：商店和玩家随机继续使用 `seed`，敌方波次只读取 `enemySeed`；非 Go 默认两者相同。
- Go 按商店种子奇偶固定映射到敌方战役 `152100/152102`，开局状态重建会保留映射；重开后会按新商店种子重新映射。
- Go 新增唯一 `go_canonical` 站位：按远近程、星级、生命/护甲、射程、攻击与单位 ID 稳定排序；同一单位与星级组合不受 UID、购买顺序或当前棋盘顺序影响。
- 修复旧 lineage 站位仍会混入 Go 搜索的旁路；Go 的模型宽搜、Top-K 真实复核和救援阵容现在都只走规范站位。
- Go 战斗缓存升级为 `combat-go-v2`，键包含敌方种子与回合，分支固定为 `rollout:0..N`，不再写入实际战斗 RNG；非 Go 的 `combat-v1` 行为保持不变。
- 新增专项回归覆盖规范站位稳定性、双敌方种子与商店解耦、跨实际 RNG 的缓存复用；当前 Go 专项 `6/6`、TypeScript 与 `git diff --check` 通过。
- 下一步：生成两套固定战役各 60 关的规范站位标签，先以 `152102` 作整份外部留出训练，再合并两套数据导出最终 CUDA 模型。

### 双战役标签与 CUDA v2 排序器

- 数据生成器已改为逐关覆盖固定敌方战役，只生成 `go_canonical`；默认每个 `(enemySeed, round)` 采样 24 套阵容、2 个公共战斗分支，并按回合逐步扩大人口与星级分布。
- 敌方 `152100/152102` 各完成 60 关、`2,880` 条标签，总计 `5,760` 条且解析拒绝数为 0；两进程并行墙钟约 2 分钟，后期整段平均约 24 场/秒。
- 严格外部留出训练只用 `152100` 更新权重、整份 `152102` 不参与训练：RTX 5080 CUDA 用时 `10.17s`，留出 pairwise `66.28%`、Top-12 最佳保留率 `85.83%`、胜负准确率 `90.42%`。旧未见轨迹约为 pairwise `60.1%`、Top-24 `76%`，新模型的剪枝可靠性已有实质提升。
- 最终 v2 模型合并两套标签训练，CUDA 用时 `49.78s`、最佳 epoch `425`；内部验证 pairwise `74.44%`、Top-6 最佳保留率 `88.46%`、胜负准确率 `92.63%`。严格泛化结论仍以上述整份 `152102` 外部留出为准。
- `goCombatModel.json` 与浏览器推理 schema 已升级为 `go-combat-ranker-v2`；5 条导出样本的浏览器/CUDA 复算继续保持 `<2e-4` 误差。Go 专项 `6/6` 与 TypeScript 通过。
- 下一步：以商店种子 `152100/152101` 分别映射两套敌方战役，跑 60 战冷缓存与热缓存长局，记录存活、胜场、最大 tick 和 hit/miss。

## 2026-08-09 · 北欧时停残局目标选择修复

- 根因：唯一存活目标在时停落地时若正处于冲刺、跳跃或击退等位移状态，目标选择器会把它排除；时停又会暂停该位移，导致圈外远程单位没有目标、坐标和攻击都停到领域结束。
- 修复：仅当位移中的目标确实被时停冻结时允许战斗目标选择；沐霂拉援这个明确可以穿出时停的特殊位移仍保持不可选规则。
- 保留原设计：单位按当前位置动态进入时停范围后仍会被冻结；新增回归同时覆盖圈外远程接近、位移目标冻结和后来进圈单位继续被冻结。

## 2026-08-09 · 线上第 46 战日志导出与看穿统一

- 已将线上日志复制到 `artifacts/autochess-human-runs/2026-08-08T18-47-53Z-round-46-score-157793.json`，并生成结构化分析 `2026-08-08T18-47-53Z-round-46-score-157793.analysis.json`。
- 该记录包含 `1,237` 个动作、`46` 场战斗、`20,195` 条战斗事件，丢失事件为 `0`。第 46 战结束时等级 10、金币 1,360，开战前仍有 1,275 金、20 利息和 8/8 候补；最终血量归零。
- 日志确认第 46 战前仍有可追的 `cog_scribe` 来牌，但候补空间已被占满，最终只保留一星场上棋和二星候补，且大量资金没有转化为升星/战力。看穿终局投资逻辑现在会在有目标来牌时先出售非目标闲棋，再买牌或刷新；出售排序优先保留已规划目标、三星和未来变阵关键牌。
- 新增全局 Console 函数 `exportAutoChessLastRun()`：优先导出当前内存记录，页面重载后自动回退读取 IndexedDB，下载 `autochess-run-round-<N>-score-<S>.json`；同时保留 `window.autoChessLastRun`、sessionStorage 和开发环境归档。
- 看穿2已正式替换为对外的“看穿”：看穿现在使用看穿2的终局目标、六进三复筛、真人记录站位和终局投资逻辑；历史设置/脚本中的 `seer2` 仅兼容迁移为 `seer`，不再出现在设置面板或默认横评。历史版本的 `go` 设置会迁移到看穿，新 Go 级保留为独立实验入口。
- 验证：看穿专项 `8/8`、AI 控制专项 `77/77`、TypeScript、脚本语法和 `git diff --check` 通过；系统 Chrome `verify-autochess.cjs` 与设置/导出专项均通过，截图人工检查无黑屏/透明画布，页面错误为 `0`。

## 2026-08-09 · 真 Go 评估与实战站位一致性（进行中）

- 第 36 战诊断确认 `plannedLineupScore=10055.78`，但开战前按实际棋盘复算仅 `-1010.50`；实际战斗失败与后者一致，说明战斗模拟本身没有漂移，陈旧的是计划对应的物理站位。
- 根因是 `commitGenome()` 保存冠军阵容和分数后立即清空 `plannedBoardSlots`，导致模型与 60Hz 终选评估的 `go_canonical` 槽位没有随计划进入实际布阵执行。
- Go 现在提交冠军时同时冻结被评估的 UID→槽位映射；普通 Go 与救援 Go 都由 `formationAction()` 逐步执行明确槽位，不影响看穿及其他三种策略。
- 新增逐格回归：先记录冠军评估 placements，再执行完整换位链，要求开战前实际 board placements 字符串完全相等且有限步收敛。Go 专项 `10/10`、TypeScript 与差异检查通过。
- 下一步正在使用 `.tmp/autochess-go-fixed-v2-seed-152100-152101-hz20.json` 热缓存复跑商店种子 `152101` / 敌方种子 `152102`，核对第 36 战计划分数、实际棋盘分数和真实胜负。
- 第一轮热跑在第 `30` 战死亡、`27` 胜；缓存由 `25,037` 增至 `29,389` 条，最大单 tick `4.87s`。第 29 战计划/实际复算均判胜，但第 12、30 战仍为计划胜、实际棋盘复算败；第 18、29 战还在未完成规范换位时进入战斗。
- 继续定位出两个独立旁路：整备 `96` 动作或同状态访问 3 次的硬保险会直接开战；救援搜索只用 20Hz 单分支筛选并在锁定时再次用 20Hz 覆盖计划分数。前者解释未完成的槽位映射，后者解释规范棋盘仍出现计划/60Hz 分数不一致。
- Go 的硬保险现在先完成规范换位；救援搜索改为 20Hz 首筛、Top-4 在 60Hz 下用四个固定公共分支复核，且锁定时保留复核分数。新增“超过 96 动作仍完成站位”和“20Hz 伪冠军被 60Hz 淘汰”回归；Go 专项 `11/11`、TypeScript、目标 ESLint与差异检查通过。
- 收尾复核：`pnpm autochess:test` 为 `297/298`；唯一失败是更早终局数值调整留下的旧断言“十名三星高费阵容会在三十二战后遇到终局压力”，当前第 32 战实际为超时未清场，本轮导出函数、看穿迁移和时停修复相关测试均通过。TypeScript、验证脚本语法和 `git diff --check` 继续通过。

### 继续训练：精确分数闭环

- 最新 `152101 → enemy 152102` 复跑仍在第 `30` 战死亡、`27` 胜，但计划分数与实际 60Hz 棋盘分数的方向完全一致；第 `18/22/30` 战均在开战前正确判负，非法移动和选中停滞均为 `0`。
- 全 30 战中仅第 2、4 战存在 `13.88/23.12` 的同为胜局分差，根因是持有棋子未超过人口时以及稳胜裁员时仍用探索频率提交最终分数。
- Go 的这两条提交路径已统一为 60Hz 公共分支复核，不影响看穿和另外三种策略；新增小阵容回归。当前 Go 专项 `12/12`、TypeScript、目标 ESLint（0 error）、脚本语法和 `git diff --check` 通过。
- 下一步在第 30 战冻结完整状态，对实际持有棋子的组合空间做模型宽搜和 60Hz 真战复核，区分“候选搜索漏解”与“更早运营没有积累出胜解”。

### 第 30 战全组合诊断与 60Hz 标签缺口

- 新增 `scripts/diagnose-autochess-go-loss.mjs`：可在指定 Go 败局开战前冻结完整状态，枚举全部合法阵容，用神经模型与启发式宽筛，再用 20Hz 和 60Hz 公共分支分层复核；新增标签会原子写回长期缓存。
- 商店种子 `152101` / 敌方 `152102` 的第 30 战共有 `43,758` 个 UID 组合、约 `13,058` 个不同棋种/星级组合；当前搜索会让同名同星 UID 等价阵容重复占据预算。
- 首轮 60Hz 终审 33 个候选无胜解，但扩大到 256 个后找到 2 个四公共分支全胜解，最佳分数 `10062.78`。胜解只需用二星小岁花替换实战阵容的一星余烬冲阵者。
- 胜解在全组合中的模型排名为 `176`、启发式排名 `48`，但 20Hz 首筛把它误判为 `-302.74`；当前只复核 20Hz Top-4，因此必然漏掉。当前实战阵容 60Hz 为 `-1112.34`。
- 结论：现有 20Hz 训练标签和真实 60Hz 目标存在实质域偏移，且 UID 等价组合浪费搜索预算。下一步先生成两套固定战役的 60Hz 规范标签并重新做整战役留出训练，再基于留出 Top-K 保留率决定搜索预算。
