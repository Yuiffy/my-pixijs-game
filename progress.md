Original prompt: /goal 我们仓库里自走棋游戏demo，非常简陋，基本玩不了（能操作，但是没啥玩头，兵种、技能、平衡、游戏进程，都不太精心调整过，纯demo）。你来检查、完善这个游戏，让它变得是一个不用太完善但能让人总想开几把挑战一下的游戏。

## 目标

- 做成一局约 8—12 分钟、阵容选择清楚、战斗反馈明确、失败后愿意马上重开的轻量自走棋。
- 保留短局节奏，同时提供自由布阵、羁绊、升星、经济、随机强化和逐步加压的敌人波次。

## 已完成

- 2026-07-17：完成旧版审计与基线试玩，定位到固定阵型、内容重复、经济逻辑分散、战斗缺少反馈和本地统计接口报错等问题。
- 2026-07-17：重构为 8 战短局，加入 9 个可购买兵种、首领、7 类双人羁绊、三种开局协议、自动升星、精英/首领波次、8 种三选一战术烙印、利息与连胜经济。
- 2026-07-17：根据用户反馈，把 4×2 固定阵型替换成 6×4 交错战术网格（24 个落点）；备战坐标会映射进战斗，并保留 3→6 的上阵人数成长。
- 2026-07-17：网格同时支持点选换位与鼠标拖放；真实拖放测试已将单位从 (0,0) 移至 (5,3)，状态和画面一致。
- 2026-07-17：重做单画布界面，加入购买、移动、出售、刷新、开战、快捷键、全屏、技能特效、伤害/治疗/护盾反馈、结算和快速重开。
- 2026-07-17：加入可复现随机种子、文本状态接口和确定性时间推进，便于自动化验证多回合流程。
- 2026-07-17：处理护盾无限叠加和首战节奏偏慢问题；限制护盾、调整回能，把战斗上限收紧到 24 秒，并建立前三战教学坡度。
- 2026-07-17：完成两次烙印选择、精英波、失败结算、最高分保存与重开状态清理的长流程验证；控制台无游戏错误。
- 2026-07-17：验证 R 刷新、Space 开战、F 进入全屏、Esc 退出全屏；本地环境不再加载生产广告和统计脚本。
- 2026-07-17：删除已失去引用的旧 Phaser 场景、对象、UI、配置和系统原型；这些删除可通过 Git 历史恢复。
- 2026-07-17：`npm run build`、类型检查、静态生成全部通过。构建只保留仓库其他页面原有的 lint 警告，自走棋页面无新增警告。

## 验证清单

- [x] 完整试玩旧版并记录主要摩擦点。
- [x] 完成兵种、技能、羁绊、经济、强化和局内成长设计。
- [x] 分批实现并用 Playwright 覆盖开局、购买、自由布阵、战斗、胜负、强化、游戏结束与重开。
- [x] 验证真实鼠标拖放、键盘快捷键、全屏进出和本地持久化。
- [x] 通过生产构建和 TypeScript 校验。

## 后续可选

- 继续根据真人试玩数据微调单位数值、商店概率和后四战难度。
- 增加音效、音乐和更丰富的单位美术；当前版本优先保证玩法循环与可读性。

## 2026-07-25 · 满员时保留商店羁绊高亮

- 用户反馈：场上棋子达到人口上限后，商店不应隐藏“换上该棋子可激活羁绊”的高亮提示。
- 已将羁绊价值提示与当前上阵空位解耦；场上满员但仍可购买时，Phaser 商店和 DOM/移动端商店都会继续显示待激活羁绊高亮。
- 金币和棋子储存空间仍独立控制购买可用性，不改变购买规则。
- 回归覆盖两套商店判定，禁止羁绊预测再次依赖 `boardCount` / `boardCap`，并保留场上同名单位不重复计羁绊。
- 系统 Chrome 专项验证：种子 1、火热整活、上阵 3/3 时，老弥仍高亮 `27期` 与 `粤帮`，`成熟` 保持暗态；桌面与 390×844 移动商店截图均已打开检查，文本状态、单画布尺寸、DOM 与控制台错误交叉检查通过。
- 验证：`pnpm exec tsc --noEmit` 与商店/Phaser 专项回归 `23/23` 通过；完整自走棋回归在并行的地面棒棒糖战斗改动落盘后为 `95/98`，失败项为已有的“满能量远程单位会先进入攻击距离再施法”，以及与本轮无关的北欧时停、礼小虎眩晕用例。

## 2026-07-17 · 兵种池扩充目标

- 用户反馈：9 个兵种无法合理支撑五档费用与 7 个羁绊，要求大幅扩充并自行提交、推送。
- 审计确认旧版只有 9 个 1—4 费商店单位，唯一“5 费”其实是不可购买 Boss；商店 4 格、备战席 5 格也偏小。
- 已完成新数据设计：30 个可购买兵种，1—5 费各 6 个；5 个阵营与 5 个职业各覆盖 6 个兵种，全部采用 2/4/6 三档羁绊。
- 新池保留原有 9 个单位并新增 21 个单位；每个费用档同时包含前排、输出、法术或切后等不同战术职责。

### 本轮 TODO

- [x] 设计 30 兵种、五档费用、均衡羁绊分布与后期商店概率。
- [x] 接入引擎中的三档羁绊、30 个技能和新敌方波次。
- [x] 将商店扩为 5 格、备战席扩为 8 格，并展示当前费用概率。
- [x] Playwright 验证新商店、羁绊、战斗、强化、结算和重开长流程。
- [x] 生产构建通过后，仅提交本轮相关文件并推送。

- 2026-07-17：TypeScript 校验通过；首轮浏览器状态确认 `purchasableUnits: 30`、每档 `[6,6,6,6,6]`、商店 5 张、备战席 8 格。
- 2026-07-17：实战截图确认新商店与网格无重叠，余烬Ⅰ、先锋Ⅰ能正确激活并作用于单位属性；首批新增单位技能可正常释放，控制台无错误。
- 2026-07-17：第 6 战状态曾由两只同名熔火刃把余烬推到Ⅱ阶，暴露出旧版“同名重复计羁绊”的捷径；现已改为同名只计一次，重复单位仍可上场和追三星，但不能虚增羁绊。
- 2026-07-17：羁绊徽章新增悬停说明，能展示阵营/职业、当前人数及三档完整数值；截图确认不会遮挡关键操作区。
- 2026-07-17：在第 6 战 3% 五费概率下实际刷出并购买虚空终猎者/时序泰坦；五费单位可进入备战席、换上战场并激活裂隙与夜袭，控制台无错误。
- 2026-07-17：长流程走完两次契印、后期敌方技能、失败结算与重新开局；最高分保留，局内状态清空。
- 2026-07-17：同名去重回归通过：两只烬角冲阵者只贡献 1 个余烬；换成四个不同余烬单位后，状态正确显示 `count: 4, level: 2` 和 65% 灼烧。
- 2026-07-17：生产构建通过；自走棋文件无 lint 或类型错误。构建输出中只剩仓库其他页面原有警告。
- 2026-07-17：数据不变量审计通过：30 个单位 ID 全部唯一、五档各 6 个、10 个羁绊各覆盖 6 个、每回合商店概率总和均为 100%。
- 2026-07-17：功能提交 `06c1ca0` 已推送到 `origin/master`。

## 2026-07-20 · 怕死受击后跳优化

- 用户反馈：怕死羁绊的受击后跳出现闪现感，且被远程单位攻击时可能越跳越远、无法重新接敌。
- 已完成：怕死单位受击后接入现有抛物线跳跃状态，使用 0.46 秒持续过程；后撤落点优先保持在自身攻击距离内，越界时沿攻击者周围的侧向弧线跳跃。
- 已完成：增加 0.72 秒后跳冷却，避免远程攻击连续触发后跳造成锁死。
- 已完成：更新怕死羁绊说明，并新增“非瞬移跳跃”和“越界改为侧跳”的回归测试。
- 验证：`pnpm autochess:test` 57/57 通过。
- 2026-07-20：TypeScript 校验通过；完整浏览器流程和专门的怕死 + 远程敌人场景均通过，控制台无运行时错误；截图确认跳跃中间态可见，远程场景落点为侧向弧线。
- 备注：生产构建代码编译通过，但仓库既有 ESLint 的 CRLF/格式错误仍会阻止 `next build` 在 lint 阶段退出成功；本轮未改动这些无关格式问题。

## 2026-07-20 · 合并自走棋图片与羁绊分支

- 已合并 `origin/feature/add-image-auto-chess`，保留裂隙首领头像、主播图片池、弹幕/舞台梦等新内容，并解决核心数据、引擎和类型定义冲突。
- 合并后移除旧的瞬时坐标推送，保留怕死的 0.46 秒真实跳跃；后撤会越出自身攻击距离时改走攻击者侧向弧线，并使用 0.72 秒冷却。
- 回归验证：`pnpm autochess:test` 59/59、`pnpm exec tsc --noEmit` 通过；`verify-autochess.cjs` 浏览器流程无错误，专门场景捕获到 `jumping: true` 且 `jumpFrom`/`jumpTo` 不同。

## 2026-07-22 · 战斗状态与备战区视觉反馈

- 已完成：美·鱿鱼点名减甲时，目标头顶显示持续 🦑 状态，并额外播放一次上浮鱿鱼 emoji；通用头顶标记补齐灼烧、减速、眩晕、跳跃和技能增益状态。
- 已完成：商店中已有同名棋子显示「已有 ×N」徽标、头像脉冲圈和强调边框，宽屏与紧凑布局均覆盖。
- 已完成：移除备战区左上角冗余「已激活」总结；羁绊横条按已激活优先排序；修正横条与「后方 · 远程与辅助 / 6 × 4 / 前线」说明行的间距。
- 已完成：提示层初始化增加空保护，兼容自动化虚拟时间早于 Phaser 场景创建的边界。
- 验证：`pnpm autochess:test` 69/69、`pnpm exec tsc --noEmit` 通过；`verify-autochess.cjs` 完整 Chrome 流程通过且控制台无错误；通用 action client headed 流程可结束，旧版 headless shell 截图仍受环境 WebGL 限制。

- 2026-07-22 追加：商店已有棋子提示改为按 1/2/3 星分桶；纯一星继续显示「已有 ×N」，出现二星或三星时显示对应星级数量，混合星级同时列出各档数量。
- 追加验证：`pnpm exec tsc --noEmit` 通过；通用 action client 可结束且无错误文件，截图仍受本机旧版 headless WebGL 环境限制。
- 2026-07-22 追加：战斗棋子星级移到动态名字右侧；头顶状态/debuff 标记字号降至 13，并固定在头像顶部附近，避免与星级叠加。
- 追加验证：TypeScript 检查与 diff 空白检查通过；临时自动化运行目录已清理。

## 2026-07-23 · AutoChess UI 指挥舱重构

### 视觉与内容方向

- 视觉 thesis：深夜指挥舱——冷墨蓝战场、青蓝系统反馈、金色只强调经济决策，整体克制但保留裂隙与远征的紧张感。
- 内容 plan：开局页负责建立远征目标并选择协议；备战页由原生画布承载战场内简报/羁绊/部署，DOM HUD 承载顶栏与战术商店；移动端用本回合提示 + 底部操作栏 + 抽屉承接商店、备战席、羁绊。
- interaction thesis：商店卡片 hover 横移确认可购买性；购买区持续展示已有棋子数、费用概率、利息/连胜；战斗态只保留一个 DOM 统计入口，移动端抽屉负责深层操作。

### 已完成

- 全量重做 `RiftHud.tsx` 与 `RiftHud.css`：新版远征顶栏、协议选择页、经济/战斗指标、费用概率、商店卡、已有棋子提示、锁店/刷新/升本/开战决策区、战斗状态条、结束页。
- 移动端新增商店/备战席/羁绊抽屉与“本回合快捷提示”，保留安全区和 48px 以上主要触控目标。
- 修正移动端 Phaser 备战视图：竖屏使用 480×1000 移动逻辑组合与 mobile board/bench slot，不再把 1120×720 桌面场景缩成中间小块；移动端由 DOM 抽屉负责商店与备战操作。
- 移除备战阶段画布 toast 与 DOM 的重复层级，保留工具栏状态提示；移除战斗阶段重复的原生统计按钮，统一到 DOM 战斗入口。

### 验证

- `pnpm exec tsc --noEmit` 通过。
- `node --test scripts/tests/autochess-sprite-rendering.test.mjs` 20/20 通过。
- Playwright action client 标题/备战流程无 console error；桌面端实测协议→购买→锁店/刷新→开战→确定性推进→战斗统计，状态接口与画面一致。
- 1280×720、390×844 截图已检查：桌面标题/备战/战斗、移动标题/备战、商店/备战席/羁绊抽屉均可读且无遮挡。
- `pnpm autochess:test` 当前 69/71：本轮未改动的引擎测试“满能量远程单位会先进入攻击距离再施法”仍失败；Phaser 静态检查已恢复 20/20。失败与本轮 HUD/布局变更无调用关系。
- `pnpm build` 已编译成功，但在仓库既有 ESLint 错误（`gameEngine.ts`、`layout.ts`、`RiftLineScene.ts`）阶段退出；新版 HUD 无新增 lint error，仅保留 `<img>` 优化提示。

### 后续可选

- 如果继续打磨移动端，可将战斗态也重排为竖屏专用战场；当前优先保证备战阶段和 PC 浏览器体验。
- 可另行清理仓库既有 lint 错误与远程单位施法回归失败，不应与本轮 UI 变更混在一起。

## 2026-07-23 · 修复重复商店渲染

- 用户反馈宽屏出现两个商店：确认是 React `RiftHud` 新商店与 Phaser `drawWideShop()` 旧商店同时渲染。
- 已关闭桌面 Phaser 旧商店及旧整备操作条，保留 React 商店作为唯一桌面决策区；移动端继续使用 DOM 抽屉。
- 新增 1500px 以上宽屏工作区扩展规则，让新版商店与 2048px 宽屏战场右侧对齐，不再留下旧商店位置的空白错位。
- 验证：2048×1080 截图只剩 1 个商店、1 个可见“开始战斗”按钮，Playwright 无 console error；`pnpm exec tsc --noEmit` 通过。

## 2026-07-23 · 商店说明与羁绊反馈

- 商店棋子支持桌面端 hover / 键盘 focus 说明面板：展示棋子定位、属性、技能描述、回能方式与完整羁绊。
- 商店羁绊 tag 放大，并复用原有“购买后达到下一档阈值”的判定；可激活项显示金色边框、光晕与上箭头，已激活项保留羁绊色强调。
- 移动端商店抽屉同步放大 tag，并验证 390×844 无横向溢出。
- 验证：`pnpm exec tsc --noEmit` 通过；HUD 文件 ESLint 无新增错误（仅原有 `<img>` 优化提示）；Playwright 桌面 hover 说明面板 1 个、待激活 tag 3 个、商店 5 张，移动端商店 5 张、无溢出、无 console error。

## 2026-07-24 · 羁绊明暗、阵型交互与出售区修复

- 羁绊标签改为同色明暗体系：普通羁绊压暗，待激活/已激活使用原色高亮、加粗和同色光晕，不再使用金色框或 ↑ 符号。
- 移除商店头像右下角小圆数字；费用只保留卡片右侧大数字。
- 恢复“已有”分星显示：纯一星显示 `已有 ×N`，混合或出现二星/三星时显示 `已有 3星×N 2星×N 1星×N`；有拥有单位的卡片增加绿色拥有态边缘和明显徽标。
- 修复空的 React 舞台层拦截 Phaser 棋盘事件的问题，阵型棋子 hover 说明与拖拽恢复。
- 桌面备战席新增暗红“拖到这里出售”区域，拖拽进入后亮红并显示返还金币，松开直接出售；右键出售和移动端出售入口保留。
- Phaser 棋子/羁绊 tooltip 在桌面备战阶段自动避开 React 商店，完整显示在商店左侧。
- 验证：`pnpm exec tsc --noEmit` 通过；HUD/Phaser ESLint 无新增错误（仅 `<img>` 与既有 `_tree` 警告）；Playwright 验证移动、出售、hover 说明与商店拥有态，无 console error；390×844 商店 5 张、已有徽标可见且无横向溢出。

## 2026-07-24 · 拖拽 hover 与出售金额反馈

- 拖拽棋子时屏蔽所有棋盘/备战席 slot 的 `pointerover`，经过其他棋子不再弹出说明；拖拽结束后 hover 说明恢复。
- 拖拽开始后出售区即时显示 `出售 +N 金币`，进入出售区显示 `松开出售 +N 金币`；拖拽幽灵移到按钮上方偏右，避免遮住返还金额。
- 验证：两个备战席棋子拖拽经过目标后成功交换且无 tooltip；拖拽备战席棋子出售成功，出售区金额可见，Playwright 无 console error，TypeScript 通过。

## 2026-07-24 · 默认 125% 视觉密度与 Phaser 清晰度

- 宽屏默认 UI 比例改为连续 `1.00–1.25`：`1280×720` 及更小保持原默认大小，空间增加时逐步放大，`1600×900` 以上达到类似浏览器 125% 的视觉密度。
- DOM HUD、工具栏、图鉴和 Phaser 宿主统一放在同一个缩放视口中；移动端仍保持 1.00 并继续使用竖屏专用布局。
- Phaser 改为由 React 宿主显式区分 CSS 显示尺寸与高 DPI backing buffer；渲染密度同时计入设备 DPR 和 UI 放大比例，并继续受 800 万像素预算约束。
- 浏览器验证：`1680×1050 → 1.250`、`1440×810 → 1.125`、`1280×720 → 1.000`、`390×844 → 1.000`，四种尺寸都无页面横向溢出。
- 在 1.25 比例下实际完成协议选择、商店购买、Phaser 棋子选中及棋盘到备战席移动，点击坐标与画面一致；浏览器无运行时 error。
- `pnpm exec tsc --noEmit` 与 Phaser 静态回归 `20/20` 通过；完整套件 `70/71`，唯一失败仍是既有“满能量远程单位会先进入攻击距离再施法”回归。
- 浏览器日志仍有 Phaser 4 对既有 geometry mask `setMask` 的 WebGL warning，本轮未改变该遮罩实现。

## 2026-07-24 · 通用技能视觉反馈

- 所有主动技能统一补上按棋子强调色绘制的施法能量圈；以后新增技能即使没有定制特效，也不会只剩技能名文字。
- 通用 `ring` 效果升级为范围染色、外圈冲击波和内圈扩散反馈，AOE 实际作用区域更容易辨认；`line` 效果增加沿路径飞行的亮核、尾迹和命中扩散圈，可作为即时子弹/光线的通用表现。
- 已有胡萝卜、鲨鱼、松针、钢镚等实体弹幕保持原样；爆发效果增加混合发光轮廓。Phaser 主场景与旧 Canvas 兼容渲染器同步实现。
- `render_game_to_text` 新增当前帧 `battle.visualEffects`，记录非文字效果和实体弹幕，便于自动化精确捕获施法帧；同时修复场景图层尚未初始化时 `refresh()` 提前重建的竞态。
- Chrome 实战分别捕获并检查施法圈 + AOE、飞行连线、松针实体弹幕和 390×844 竖屏 AOE；四类反馈都出现，控制台无 error。竖屏战斗棋盘偏小仍是既有后续项，本轮未改布局。
- `pnpm exec tsc --noEmit` 与 Phaser 静态回归通过；完整自走棋套件仍只有既有的“满能量远程单位会先进入攻击距离再施法”失败。

## 2026-07-24 · 合并胆小与怕死羁绊

- 用户要求移除与“怕死”定位重复的“胆小”羁绊，并将其成员统一归入“怕死”。
- 已完成：删除 `timid` 羁绊 ID、定义及闪避/移速战斗加成；小红帽、小岁鸟、小猫拳、能能弄你、恬豆、又一和狍子偶像改为拥有 `vanguard`（怕死）。
- 已更新数据与战斗回归测试，验证旧成员会计入怕死人数并获得怕死的攻击距离加成。
- 验证：`pnpm exec tsc --noEmit`、9 项数据测试和合并专项战斗测试通过；通用 Playwright 客户端成功进入备战且无 console error。
- 整页 Chrome 截图确认羁绊横条与商店标签只显示“怕死”、不再显示“胆小”，布局无错位；通用客户端的 canvas 截图仍受既有 WebGL 黑图问题影响。
- 完整套件当前 `70/72`：既有“满能量远程单位会先进入攻击距离再施法”仍失败；并行进入工作区的技能位移动画改动导致“小猫拳会闪现到最远敌人身后并推进击晕”新增失败，均与本次羁绊合并断言无关。

## 2026-07-24 · 技能位移过程与滑跪碰撞

- 新增由战斗引擎持有的 `dash / jump / push` 运动状态；位移坐标、碰撞和落地结算均按固定步长推进，Phaser 只读取同一状态渲染弧线、压缩与倾斜。
- 中单光一“滑跪”改为严格直线冲刺；逐帧扫掠路径上的敌人，每个目标只命中一次，并用短促的前向 + 侧向 `push` 过程撞开。
- 小岁鸟、饼干岁、又一、秋凛子、狍子偶像、沐霂的飞行/突进技能均取消瞬时改坐标，伤害、治疗、眩晕与护盾延后到抵达时结算。
- 小猫拳保留技能说明明确要求的起手闪现，随后施法者与目标同步推进，三连击和眩晕在推进结束时结算；冷笑话继续保留真正的瞬移。
- `render_game_to_text` 新增运动种类、技能、进度和起终点；专项回归覆盖滑跪中间坐标、沿途多目标一次命中、击退、跃击空中延迟结算和猫拳同步推进。
- Chrome 实测捕获滑跪起步、中段碰撞、完整击退落点，以及跃击空中和落地状态；截图与文本进度一致，控制台无 error。通用 Playwright 客户端仍受本机既有 WebGL 黑图限制。
- 最终验证：`pnpm exec tsc --noEmit`、Phaser 静态回归 `22/22`、位移专项 `3/3`、`git diff --check` 通过；完整套件 `74/75`，唯一失败仍是既有“满能量远程单位会先进入攻击距离再施法”断言。

## 2026-07-24 · 偷袭羁绊按己方交战提前起跳

- 旧逻辑只检查双方是否进入近身区域，远程队友已经开火时仍可能让偷袭成员继续等待约 3.4 秒。
- 战斗状态新增双方独立的持久交战标记；己方任一单位首次普攻、启动进攻技能或实际造成/承受伤害时，对应队伍立即记为已交战。
- 仍在等待的偷袭成员会在下一模拟帧响应己方交战标记并起跳；原倒计时继续作为双方长期不接触时的兜底。
- 固定步长回归构造了“双方距离超过旧近身阈值、远程队友已经出手”的场景，验证两名偷袭成员在等待时间仍大于 3 秒时提前跳向后排。
- Chrome 连续截图与文本状态确认：首次出手帧 `engagedTeams.player = true`，下一帧两名偷袭成员均进入真实抛物线跳跃；控制台无 error。
- 最终验证：`pnpm exec tsc --noEmit`、偷袭专项 `2/2`、`git diff --check` 通过；完整套件 `75/76`，唯一失败仍是既有“满能量远程单位会先进入攻击距离再施法”断言。

## 2026-07-24 · 邪恶外星人贯穿光线瞄准修复

- 根因：外星贯穿光线的起点使用施法者坐标，但终点固定为战场左右边缘和目标的 y 坐标；目标在斜上方或斜下方时，光束不会经过目标，视觉上总是偏向水平方向。
- 已完成：光束改为从施法者穿过当前目标并延伸至战场边界；贯穿伤害范围同步改为同一条前向射线，避免视觉轨迹与伤害判定不一致。
- 已新增斜上方目标回归，覆盖当前目标、目标后方同线敌人、射线外敌人与光束三点共线断言；专项测试和 TypeScript 检查通过。
- 浏览器使用种子 7 将邪恶外星人布置在左下、敌人置于右上，逐帧捕获确认光束穿过当前目标并向同方向延伸；控制台无 error。
- 完整测试仍只有既有的“满能量远程单位会先进入攻击距离再施法”失败；项目专用长流程脚本另在第一战等待结算时超时，外星人专项、类型检查和真实施法画面均通过。

## 2026-07-24 · 备战棋子属性显示口径修复

- 用户反馈：三星兔子射手在战斗中最大生命为 474，但备战悬浮说明仍显示一星裸值 150。
- 根因：备战棋盘 tooltip 直接读取 `UNIT_DEFS` 基础值，没有应用星级倍率、羁绊、开局协议和契印；战斗实体则使用了完整计算。
- 已完成：抽取并复用玩家部署战斗属性计算；棋盘与备战席预览会应用星级、当前羁绊、开局协议和契印，战斗生成也调用同一函数。
- 回归验证构造三星兔子射手 + 坚韧淬炼 + 第二阵风，备战与战斗均为生命 474、攻击 75、护甲 33、射程 230；初始能量也使用同一预览口径。
- Chrome 整页截图确认 tooltip 在商店左侧完整显示且无控制台错误；TypeScript、专项回归与 Phaser 静态回归通过。完整套件 `76/77`，唯一失败仍是既有“满能量远程单位会先进入攻击距离再施法”。

## 2026-07-24 · 泽音、贪吃岁与椰子栞技能调整

- 已完成：泽音美乐蒂改为 180 血近战首命，首次死亡触发“涅槃重生”切换为 72% 血量的远程形态，攻击 +36%、攻击间隔缩短 30%；贪吃岁改为“吃！”的自动 + 攻击双回能下一击强化吸血；椰子鸡栞改名椰子栞并改为“大声”范围伤害眩晕。
- 验证：TypeScript、差异检查与泽音/贪吃岁/椰子栞专项战斗测试通过。Chrome 页面编译并进入战斗，截图和日志无运行时错误；仓库整局脚本仍在既有结算等待处超时，完整测试仍保留既有“满能量远程单位会先进入攻击距离再施法”失败。

## 2026-07-24 · 技能按星级成长框架

- 用户要求技能能够随星级使用不同参数，并先为以技能为核心的北欧魔法师提升持续时间和范围。
- 已新增单位级三档技能配置与统一的星级参数、说明、成长摘要解析接口；未配置技能继续兼容原固定逻辑。
- 北欧时停的 1/2/3 星参数设为半径 `108/132/162`、持续 `1.8/2.5/3.4 秒`；战斗区域、停滞判定和范围特效共用解析后的参数。
- 商店说明展示完整三档成长，棋盘和战斗悬浮说明展示当前星级效果及成长摘要。
- 验证：`pnpm exec tsc --noEmit` 通过；数据测试 `10/10`，北欧时停三档战斗专项 `3/3` 通过，确认区域半径、持续时间与范围特效一致。
- `render_game_to_text` 已补充活动时停区中心、半径、剩余时间和总持续时间，便于浏览器验证持续控制区域而不只捕获施法瞬间。
- 最终完整套件 `80/81`：北欧时停的数据、战斗和文本状态测试全部通过；唯一失败仍是既有“满能量远程单位会先进入攻击距离再施法”断言。
- 浏览器使用种子 22 验证商店说明，完整显示 `1星 108/1.8秒 · 2星 132/2.5秒 · 3星 162/3.4秒`，面板无截断或遮挡。
- 购买北欧魔法师并进入第一战后捕获真实施法帧：文本状态为半径 `108`、总时长 `1.8`、剩余 `1.77` 秒，画面圆环与作用区域一致，控制台无 error。
- 截图：`.tmp/autochess/spark-skill-growth-shop.png`、`.tmp/autochess/spark-skill-level-1-cast.png`。

## 2026-07-24 · 七海、恬豆与三理理技能调整

- 已完成：七海大鲨鱼改为 5 秒“鲨鱼变身”，持续期间攻击 +85%、吸血 45%；恬豆改为“棒棒糖刘海”，投掷 5 颗可被双方碰撞消耗的 🍭，友军回复 14% 最大生命并加速，敌人受伤并减速；三理理改为“站我后面”，获得 22% 最大生命护盾并嘲讽 155 范围内敌人 3.2 秒。
- 技能状态和投射物同步进入 Phaser、Canvas 兼容渲染与 `render_game_to_text`；嘲讽会覆盖原先锁定目标，目标失效后自动恢复普通选敌。
- 验证：TypeScript、数据测试 10/10、三角色战斗专项 3/3 通过；完整自走棋套件 80/81，唯一失败仍是既有“满能量远程单位会先进入攻击距离再施法”。

## 2026-07-24 · 狍子偶像与雪绘技能调整

- 已完成：狍子偶像技能改为“捏捏摸摸”，持续 3.4 秒锁定最近敌人；双方无法行动，目标持续受伤，狍子偶像按伤害的 90% 治疗自身，并以连线和状态标记反馈引导。
- 已完成：雪绘改为近战，并将“同步视听”改为贴身范围挥斩和灼烧；优势时的骄兵、劣势时的哀兵仍会线性影响攻速和射程，但不再降低或提高移速。
- 已完成：战斗头像加入青蓝“哀”与橙红“骄”的强度光环，持续显示当前同步状态。
- 验证：TypeScript、数据测试和 Phaser 渲染静态测试通过；战斗专项 49/50 通过，唯一失败仍是既有“满能量远程单位会先进入攻击距离再施法”。

## 2026-07-24 · 蛙梓终场歌唱调整

- 已完成：蛙梓保留基础单体远程激光；满能量后进入 5.5 秒的“终场歌唱”，持续耗能并每 0.6 秒治疗全体友军 3.2% 最大生命。
- 歌唱期间普攻切换为射程 235 的火焰弹，主目标受到 90% 攻击力伤害，附近目标受 70% 溅射伤害，均附带范围灼烧；头像显示“歌”状态。
- 验证：蛙梓专项覆盖治疗、火焰弹、范围伤害、范围灼烧和能量耗尽复原；完整战斗测试仅保留既有的远程施法时机失败。

## 2026-07-24 · 理财与流量羁绊

- 新增关系羁绊“理财”（2/4）：低档每场结束额外 +2 金币；高档保留 +2 金币，并将利息改为每 5 金币 +1、无上限。
- 贪吃岁、暴龙岁、饼干岁、椰子栞、七海、梨安、三理理加入理财；七海同时移除“杂谈”。
- 流量补入贪吃岁、能能弄你、沐霂、病院坂灵；每个备战回合按流量等级重置 1/2/3 次免费刷新。
- HUD 已显示实际利息与剩余免费刷新次数。`pnpm exec tsc --noEmit` 通过；新增数据与引擎回归通过。
- Chrome 实测完成购买两名流量成员、首战、结算和进入第二回合；流量 I 保持激活，刷新按钮显示“免费 1”，理财标签也在商店卡正确可见，控制台无错误。
- 完整 `pnpm autochess:test` 为 85/86，唯一失败仍是既有“满能量远程单位会先进入攻击距离再施法”断言，与本轮经济羁绊无关。

## 2026-07-24 · 星汐、塔神与礼墨加入自走棋

- 已下载三人的公开 Bilibili 头像到 `public/images/livers` 并由游戏本地加载：星汐 `51030552`、帅比笙歌超可爱 `15641218`、礼墨 Sumi `435243735`；来源 URL 均返回 `200`。此前生成的本地头像已删除。
- 已移除“星塔墨契”及其全部属性加成和 ID，不再把三人绑定为专属羁绊。
- 星汐保留“山猪冲阵”；笙歌的“塔神”梗取自“脑袋尖尖、背后长了座塔”，技能调整为“尖塔压顶”范围伤害与眩晕；礼墨的东方龙、瓜咪与礼小虎称呼用于“礼小虎出击”范围伤害、眩晕和护甲削弱。
- 验证：`pnpm exec tsc --noEmit`、三人专项数据/战斗回归与 `git diff --check` 通过。两文件战斗套件 `65/66`，唯一失败仍是已有的“满能量远程单位会先进入攻击距离再施法”断言。

## 2026-07-24 · 击败赏金与十本人口

- 用户确认取消基础工资，改为只按实际击败的敌人结算：每颗星提供 1 金，1/2/3 星敌人分别价值 1/2/3 金；败局也保留已经取得的赏金。
- 已移除胜利基础 5 金、失败整备工资和精英固定奖励；利息、连胜、理财、契印与开局协议仍作为额外经济来源。
- 结算状态新增赏金总额、击败人数与分星计数；备战商店和移动端简报会预告全歼本战的总赏金。
- 等级改为标准 3 本起步，补齐 9 本、10 本；最高 10 人口，并新增对应升本费用与高费商店概率。
- 验证：赏金、连续升至十本、零赏金还款专项 `4/4`，数据与 Phaser 静态回归 `34/34`，`pnpm exec tsc --noEmit` 通过。
- 通用 Playwright 客户端确认文本状态含首战 `potentialBounty: 2`、`bookLevel: 3`；其 headless WebGL 截图仍为仓库已有黑图，因此另用应用内浏览器完成视觉验证。
- 浏览器实战确认备战赏金预告、败局按已击败的一星敌人只结算 1 金，以及 9/10 本概率表均清晰显示；控制台无 error。
- 完整 `pnpm autochess:test` 为 `91/92`，唯一失败仍是已有的“满能量远程单位会先进入攻击距离再施法”断言。

## 2026-07-25 · 利息节奏调整

- 普通利息改为每 5 金币提供 1 利息，最多计算 20 金币，即最高 4 利息。
- 理财Ⅱ改为每 4 金币提供 1 利息且不设上限；理财Ⅰ仍只保留每场额外 2 金币。
- 顶栏、商店、Phaser 规则提示、图鉴与文本状态统一读取实际利息，避免显示旧公式。
- 验证：利息专项 `2/2`、数据与 Phaser 静态回归 `34/34`、`pnpm exec tsc --noEmit` 通过。
- 通用 Playwright 文本状态确认 8 金对应 1 利息；应用内浏览器确认 9 金时顶栏和商店均显示 1 利息，玩法说明文案完整且控制台无 error。
- 完整 `pnpm autochess:test` 为 `92/93`，唯一失败仍是已有的“满能量远程单位会先进入攻击距离再施法”断言。

## 2026-07-25 · 流量羁绊成员调整

- 流量移除能能弄你、沐霂·领舞开场、病院坂灵。
- 流量加入毛神、泽音美乐蒂、恬豆·甜点转圈、三理理、雪绘、塔神。
- 更新数据测试与免费刷新专项测试；相关 71 项测试通过 70 项，唯一失败仍是既有远程施法断言。
- `pnpm exec tsc --noEmit` 与 `git diff --check` 通过。浏览器验证因本机已有 Next 进程占用端口且请求超时，未获得可用截图证据。

## 2026-07-25 · 跨区域升星位置保留

- 确认自动升星已优先保留场上同名同星棋子的原格；当其余材料位于备战席时，合成后的二星或三星仍留在场上。
- 新增引擎回归测试，覆盖一枚场上与两枚备战席同名一星棋子合成为二星的场景。
- 验证：新增回归通过；战斗专项仍仅有既有的“满能量远程单位会先进入攻击距离再施法”断言失败。

## 2026-07-25 · 主持成员与帕可 Pako

- 已按用户指定将主持成员收敛为轴轴的宝、北欧魔法师、中单光一、狍子偶像、帕可 Pako、沐霂；其余棋子移除主持。
- 沐霂移除怕死，改为主持 + 跳舞。
- Bilibili 公开账号确认帕可 Pako 为 UID `1978590132`、个人认证“bilibili 直播高能主播”；公开投稿包含全配音剧情实况、吐槽向长流程游戏与翻唱。
- 已下载该账号公开头像到 `public/images/livers/pako.jpg`；帕可作为 5 费主持 + 杂谈 + 流量后排控场加入商店。
- 新技能“全配音实况”攻击敌人最密集区域并短暂打断，随后给主持成员补充能量。
- 验证：数据测试 `13/13`、TypeScript 与 `git diff --check` 通过；完整自走棋套件 `95/96`，唯一失败仍是已有的“满能量远程单位会先进入攻击距离再施法”断言。
- 技能要求的共享客户端已运行；文本状态正常进入第 1 战且无错误文件，纯黑 WebGL 画布截图按仓库约定判为捕获路径失败并拒绝作为视觉证据。
- 系统 Chrome 图鉴确认公开头像、5 费定位、主持/杂谈/流量与技能说明完整显示；主持羁绊仅列出指定六人，沐霂不再出现在怕死名单。
- 系统 Chrome 专项战斗捕获到“全配音实况”范围圈与爆发、两名敌人受伤、帕可和轴轴的宝各获得 18 能量，控制台无 error；整页截图为 1440×900，抽样 501 种颜色且非纯色/近黑。
- 仓库长流程 `verify-autochess.cjs` 能生成正常的标题、备战和战斗整页截图，但其固定 30 秒等待在第 1 战结算处超时；帕可专项短流程已独立通过。

## 2026-07-25 · 自走棋视觉验证入口

- 仓库所有者明确要求：自走棋视觉验证不得先运行通用
  `web_game_playwright_client.js`。它强制使用 headless ANGLE/SwiftShader，
  已知会浪费时间生成纯黑 WebGL 截图。
- 第一次浏览器尝试就必须使用项目内 `verify-autochess.cjs` 的系统 Chrome
  与整页 `page.screenshot()` 流程；专项交互应扩展项目内 Chrome 脚本或使用
  Browser/Chrome 工具，不得回退到通用 SwiftShader 客户端。
- 截图仍需做纯黑、透明和近黑有效性检查，并与 `render_game_to_text`、
  DOM、画布尺寸及控制台错误交叉验证。

## 2026-07-25 · 技能圆圈视觉收敛

- 删除所有主动技能无条件生成的通用 `cast` 施法圈；技能名文字继续保留。
- 继续审计定制 `ring`：移除突进/跳跃起点、召唤起手、自强化/变身、投射物起手、单体连线、群体治疗中心等 20 处无范围含义的圆圈。
- 当前剩余圆圈仅用于护盾，以及实际 AOE 伤害、眩晕、嘲讽或全场冲击范围；投射物、位移、连线和召唤物继续使用自身已有视觉。
- 新增苹果派回归，确认技能起手保留爆发和实体弹幕但不再在施法者脚下生成圆圈；Phaser/Canvas 静态回归同步禁止恢复通用 `cast` 类型。
- 系统 Chrome 定点验证普通单体治疗、护盾和 AOE：`端茶倒水` 触发帧只有技能名与连线，`绿冻护甲` 保留护盾圈，`打翻火锅` 保留 `hotpot + ring` 作用范围；三张整页截图均已打开检查且无浏览器错误。
- 验证：`pnpm exec tsc --noEmit`、Phaser 静态回归 `22/22` 通过；完整 `pnpm autochess:test` 为 `95/96`，唯一失败仍是已有的“满能量远程单位会先进入攻击距离再施法”断言。

## 2026-07-25 · 流量吸血与贪吃岁增强

- 策划调整：流量成员全能吸血由 8%/15%/24% 提高到 12%/20%/32%；4/6 人档由仅远程友军 5%/10% 改为全体友军 8%/15%，并与成员加成叠加。
- 明确底层吸血原本已由统一伤害结算触发，覆盖普攻与技能；规则文案现统一称为“全能吸血”，并新增近战非流量友军用技能伤害回血的回归测试。
- 贪吃岁“吃！”的下一击攻击力倍率由 1.9 倍提高到 2.25 倍，保留该次 45% 额外吸血，避免伤害与续航同时过度膨胀。
- 验证：新增流量专项回归通过，确认 4 流量时成员叠加为 28%、近战和远程非成员均为 8%，且近战非成员的主动技能伤害会回血；数据测试 `13/13`、`pnpm exec tsc --noEmit` 与 `git diff --check` 通过。
- 完整 `pnpm autochess:test` 为 `96/97`，唯一失败仍是已有的“满能量远程单位会先进入攻击距离再施法”断言。
- 仓库长流程 `verify-autochess.cjs` 的系统 Chrome 截图均为有效非黑图，但仍在已有的首战结算等待处超时；专项系统 Chrome 验证确认流量和贪吃岁图鉴文案完整、无截断或遮挡，`render_game_to_text`、画布尺寸与 DOM 正常，控制台无 error。

## 2026-07-25 · 恬豆棒棒糖改为地面陷阱

- 用户要求：恬豆的棒棒糖不再作为远距离普通弹幕命中，改为扔在自身周围的地面，长时间保留并由单位踩踏触发。
- 已完成：5 颗糖果的落点限制在恬豆周围 72–108 像素的扇形范围；飞行阶段不碰撞，落地后保留 10 秒，友军踩到治疗并加速，敌人踩到造成伤害并减速，触发后消失。
- 已完成：Phaser 与兼容 Canvas 为落地糖果增加地面范围圈；`render_game_to_text` 暴露 `grounded` 与剩余时间。
- 验证：恬豆地面糖果专项战斗回归通过；`pnpm exec tsc --noEmit` 与 `git diff --check` 通过。完整 `autochess-battle-spawn` 仍只有既有的远程单位施法失败。

## 2026-07-25 · 绿冻护甲回能与低级三打一平衡

- 用户反馈：低级战斗中三名单位围攻果冻风纪时，纯受击回能会反复刷新绿冻护甲，难以打破。
- 果冻风纪改用独立的“稳态回能”：初始 `25/100`，每秒自动 `+5`，每次受击仅 `+1`，攻击不回能；自动部分在三人围攻时仍高于受击部分。
- 绿冻护甲由 `42%` 收到 `30%` 最大生命护盾；图鉴和技能说明同步展示新回能与护盾数值。
- 新增三项专项回归：护盾数值与破盾视觉状态、1 秒自动回能对比三次受击回能、单独面对第二波三名一星敌人时 24 秒内只释放一次护盾且护盾被打穿、生命降至 10% 以下或阵亡。
- 新增系统 Chrome 专项脚本 `scripts/verify-sun-guard-energy.cjs`，内置纯色、透明与近黑截图拒绝检查。
- Chrome 种子 2 实测：开战能量 `25`，精确推进 1 秒后为 `30`；第一波在 `11.9` 秒触发一次护盾，持久抗压加成后护盾为 `119`，控制台无错误。图鉴、自动回能和护盾触发三张截图均已打开检查。
- 验证：数据测试 `13/13`、绿冻护甲专项 `3/3`、`pnpm exec tsc --noEmit` 与专项 Chrome 流程通过；完整 `pnpm autochess:test` 为 `104/105`，唯一失败仍是已有的“满能量远程单位会先进入攻击距离再施法”断言。

## 2026-07-25 · 护盾贴身显示与额外圆圈清理

- 绿冻护甲不再生成施法 `ring`，只保留统一的技能名浮字；所有护盾都由角色身上的贴身盾图形表达。
- 战斗实体新增当前护盾池峰值；贴身盾的填充与描边透明度按 `shield / shieldPeak` 下降，护盾耗尽、死亡或重生时同步清零。
- `render_game_to_text` 暴露 `shieldPeak`，便于把视觉强度与战斗状态交叉核对。
- 再次审计全部 17 个残余 `ring` 生成点：均对应真实 AOE 伤害、眩晕、嘲讽或全场冲击范围；纯护盾、自强化、治疗、位移起手、召唤起手和投射物起手没有额外圆圈。
- 回归覆盖绿冻护甲无中心 `ring`、只生成一次技能名、护盾比例衰减和耗尽重置；Phaser 静态回归锁定透明度公式。
- 系统 Chrome 使用种子 2 捕获满盾 `167/167` 与受击后 `62/167` 两帧：技能名只显示一次，角色贴身盾明显变淡且没有额外大圈；火锅 AOE 仍保留实际命中范围。
- 截图：`.tmp/autochess-skill-vfx/shield-high.png`、`.tmp/autochess-skill-vfx/shield-low.png`、`.tmp/autochess-skill-vfx/aoe-cast.png`。三张均为 1440×900 有效非黑图，页面单画布、逻辑尺寸 1120×720，DOM、文本状态与控制台错误检查通过。
- 验证：`pnpm exec tsc --noEmit`、绿冻护甲专项 `1/1`、Phaser 静态回归 `25/25` 通过；完整 `pnpm autochess:test` 为 `101/102`，唯一失败仍是既有的“满能量远程单位会先进入攻击距离再施法”断言。
- 追加验证：本轮完整 `pnpm autochess:test` 为 `101/103`；新增失败的能量回收断言也不涉及本轮改动，恬豆专项 `1/1`、Phaser 静态回归 `25/25` 通过。
- 系统 Chrome 专项脚本 `scripts/verify-tiandou-lollipop.cjs` 以 seed 20 实际购买并开战，捕获 5 颗落地糖果，剩余时间约 9.5 秒，截图为 `.tmp/autochess/tiandou-lollipop-grounded.png`，控制台无 error。

## 2026-07-25 · 非自身中心 AOE 投送动画

- 将非自身中心 AOE 分为两类：椰子栞“大声”和帕可“全配音实况”为即时声束型；北欧时停、火锅云、礼小虎出击、尖塔压顶、烟头烫屁股、幽灵终演、终场谢幕为弹幕型。
- 弹幕型会从施法者放出带技能主题符号的缩小范围球，飞到施法时锁定的固定圆心后才同步生成 AOE、结算伤害与控制；飞行期间不会提前伤害或提前显示范围。
- 即时型会在施法者与 AOE 圆心之间生成亮线，并在同一模拟帧触发伤害、控制和范围视觉；自身中心 AOE 与位移落地 AOE 保持原逻辑。
- Phaser 与 Canvas 兼容渲染器均新增 `aoe_orb` 表现；`render_game_to_text` 的投射物状态包含对应技能 ID，便于识别归属和自动化捕获。
- 专项回归覆盖全部 7 个弹幕型技能的延迟结算，以及 2 个声束型技能的同步触发；`pnpm exec tsc --noEmit` 和 `git diff --check` 通过。完整 `pnpm autochess:test` 为 `102/103`，唯一失败仍是既有“满能量远程单位会先进入攻击距离再施法”断言。
- 系统 Chrome 专项脚本 `scripts/verify-remote-aoe.cjs` 使用 seed 22 捕获北欧时停飞行/落地，使用 seed 15 捕获大声声束/范围同帧；画布、文本状态与效果坐标一致，控制台无 error。
- 截图：`.tmp/autochess/remote-aoe-projectile-flight.png`、`.tmp/autochess/remote-aoe-projectile-impact.png`、`.tmp/autochess/remote-aoe-beam-impact.png`。三张均为 1440×900 有效非黑图，透明率和近黑率均为 0，已逐张打开检查。

## 2026-07-25 · 商店经济信息与备战价值层级

- 设计评审确认顶栏金币与商店经济栏重复；已移除顶栏金币，仅在战术商店保留金币、赏金、利息和连胜决策信息。
- 利息数字新增可聚焦、可点击的 info 说明：普通规则为每 5 金提供 1 利息、20 金封顶；理财Ⅱ为每 4 金提供 1 利息且无上限，并显示当前金币对应的实际收益。移动端说明在商店抽屉内就地展开，不遮挡概率与商店卡。
- 备战席为每个棋子常驻显示实际回收价值；二/三星使用与出售逻辑相同的 1/3/9 份价值，高于 5 金时使用明亮金色。单位 tooltip 将费用拆成右上角独立金色标签，长名字会自动截断避让。
- 抽取 `getUnitSellValue()` 统一备战显示、移动端出售按钮与实际返还金额，避免星级棋子显示基础价但返还总价。
- 新增 `scripts/verify-autochess-economy-ui.cjs`，使用系统 Chrome 覆盖桌面利息 hover、备战 tooltip、移动端利息点击展开与备战价值；DOM、`render_game_to_text`、画布尺寸和控制台错误交叉检查通过。
- 截图：`.tmp/autochess/economy-interest-desktop.png`、`.tmp/autochess/economy-bench-tooltip-desktop.png`、`.tmp/autochess/economy-interest-mobile.png`、`.tmp/autochess/economy-bench-mobile.png`。四张均已逐张检查，抽样颜色数 570–3495、近黑率最高 0.01%、透明率 0。
- 验证：`pnpm exec tsc --noEmit`、Phaser/HUD 静态回归 `26/26`、专项 Chrome 流程和 `git diff --check` 通过；完整 `pnpm autochess:test` 为 `104/105`，唯一失败仍是已有的“满能量远程单位会先进入攻击距离再施法”断言。

## 2026-07-25 · 帕可与轴伊费用及治疗技能调整

- 帕可 Pako 从 5 费下调为 1 费，并将生命、攻击、护甲、射程、攻速与移速收回到 1 费远程辅助档位。
- 帕可技能改为“天使摸鱼”：向受伤友军最密集处投出鱼形 AOE 弹，抵达后治疗 128 半径内的所有友军，不再造成伤害、眩晕或主持回能。
- 轴伊从 1 费上调为 4 费，基础属性提升到 4 费远程辅助档位。
- 轴伊“扔橘子”改为连续投出 5 颗实体橘子；每次发射重新选择生命比例最低的友军，治疗倍率依次为 `100% / 82% / 66% / 54% / 44%`。
- 新增系统 Chrome 专项脚本 `scripts/verify-pako-joi-heals.cjs`，交叉验证投射物文本状态、治疗统计、DOM、画布尺寸、控制台错误和截图像素。
- 专项数据测试 `13/13`，新增帕可与轴伊战斗回归均通过，`pnpm exec tsc --noEmit` 与源码 ESLint 通过；完整 `pnpm autochess:test` 为 `106/107`，唯一失败仍是既有的“满能量远程单位会先进入攻击距离再施法”断言。
- 系统 Chrome 捕获帕可鱼形弹飞行/落地和轴伊双橘子飞行/治疗完成四帧；帕可累计治疗 `111` 且敌方生命不变，轴伊五段累计治疗 `476`。四张截图均为 1440×900、抽样颜色至少 4096、近黑率与透明率均为 0，已逐张打开检查，控制台无 error。
- 截图：`.tmp/autochess/pako-joi-heals/pako-angel-fish-flight.png`、`.tmp/autochess/pako-joi-heals/pako-angel-fish-impact.png`、`.tmp/autochess/pako-joi-heals/joi-orange-volley.png`、`.tmp/autochess/pako-joi-heals/joi-orange-impact.png`。
## 2026-07-25 · 中单光一滑跪减速与短眩晕

- 滑跪保留原有直线扫掠、逐目标一次命中和击退逻辑，位移改为专属三次缓出曲线，前段冲刺、末段逐渐减速到停下。
- 被滑跪撞到的敌人新增 `0.45` 秒眩晕，并同步更新技能说明。
- 回归增加前后段位移增量比较和命中时眩晕时长断言。
- `render_game_to_text` 的战斗单位摘要新增 `stun`，并新增 `scripts/verify-guangyi-slide.cjs`，用系统 Chrome 捕获滑跪中段与碰撞帧。
- 种子 27 的 Chrome 验证：滑跪进度 `0.21–0.31` 时已完成约 `51%–67%` 路程，碰撞时敌人剩余眩晕 `0.33–0.39` 秒；两张 1440×900 截图均非黑、非透明，画布逻辑尺寸 1120×720，控制台无错误。
- 验证：滑跪专项 `1/1`、TypeScript 与差异检查通过；完整自走棋套件 `113/114`，唯一失败仍是既有“满能量远程单位会先进入攻击距离再施法”断言。

## 2026-07-25 · 升本费用降至零并跨本结转

- 修复自然减费被 `Math.max(1, ...)` 锁在 1 金的问题；当前升本费用现在可以正常降到 0。
- 新增未消费减费结转：费用已经为 0 后继续完成回合，溢出的每一点减费都会保留；免费升本时先抵扣下一本，仍有剩余就继续跨本级抵扣，玩家不会因为暂时不升本而损失进度。
- `render_game_to_text` 暴露 `upgradeDiscountCarry`；图鉴规则同步说明零费与结转，桌面商店和移动商店会直接显示结转数量。
- 新增引擎回归，覆盖 `1 → 0`、零费后结转、免费升本后 `9 → 8` 以及大额结转连续跨两本抵扣。
- 新增系统 Chrome 专项脚本 `scripts/verify-autochess-upgrade-discount.cjs`；实测 3 本费用降至 0、再等一回合结转 1，免费升到 4 本后金币不变且下一本费用为 8。桌面与移动 DOM、文本状态、画布尺寸、控制台错误和截图像素交叉检查通过。
- 截图：`.tmp/autochess/upgrade-discount-zero.png`、`.tmp/autochess/upgrade-discount-carried.png`、`.tmp/autochess/upgrade-discount-carried-mobile.png`、`.tmp/autochess/upgrade-discount-next-level.png`，四张均已逐张打开检查。
- 验证：专项引擎与 Phaser/HUD 静态回归 `2/2`、`pnpm exec tsc --noEmit`、`git diff --check` 和专项 Chrome 流程通过。升本核心改动完成时完整套件为 `106/107`，仅有既有远程施法断言失败；并行的滑跪/数值改动随后继续进入工作区，最终当前套件为 `101/107`，新增失败落在部署属性、成熟护盾、两条绿冻护甲和开局文案，均不经过本轮升本状态链。仓库长流程 `verify-autochess.cjs` 仍在已有的首战结算等待处超时。

## 2026-07-25 · 泽音涅槃特效与短时攻击后坐力

- 策划结论：泽音第二形态的生存改善应来自短时撤离窗口，而不是永久拉扯或额外提高生命；后坐力必须以当前攻击目标为参照，并把“落点仍在自身攻击距离内”作为硬约束，避免改善生存却造成停火。
- 重生新增专属 `rebirth` 特效：核心闪光、收放双环与旋转放射线共同表现形态切换，不复用有伤害语义的 AOE 圆圈；Phaser 与兼容 Canvas 渲染器同步实现。
- 重生后 4 秒内，每次普攻最多向目标反方向后退 34 像素，使用 0.16 秒 `push` 运动状态逐帧缓动；提交落点前会校验确实远离目标、没有占位冲突且保留 4 像素射程余量，任一条件不满足就取消该次后坐力。
- `render_game_to_text` 暴露 `rebirthRecoilTime` 与既有运动详情；战斗状态标记在撤离窗口显示“退”，技能说明同步写明持续时间和射程保护。
- 新增 `scripts/verify-zeyin-rebirth.cjs`，使用系统 Chrome 构造确定性重生场景，交叉验证涅槃特效、后坐力中间态、射程边缘不后退、4 秒后停止、画布尺寸、控制台/资源错误和截图像素。
- Chrome 实测：泽音从 `x=280` 向后缓动至 `x=246`，中间帧位于 `x=258`；与目标距离从 120 增至 154，仍小于 245 攻击距离。截图 `.tmp/autochess/zeyin-rebirth-effect.png` 与 `.tmp/autochess/zeyin-rebirth-recoil.png` 均为 1440×900、非纯色/透明/近黑图，并已逐张打开检查。
- 验证：泽音专项 `1/1`、Phaser 静态回归 `26/26`、`pnpm exec tsc --noEmit`、`git diff --check` 与专项 Chrome 流程通过；完整 `pnpm autochess:test` 为 `113/114`，唯一失败仍是既有“满能量远程单位会先进入攻击距离再施法”断言。仓库长流程 `verify-autochess.cjs` 仍在已有的首战结算等待处超时。

## 2026-07-25 · 天赋三档与同轮平衡

- 将局内成长明确拆为开局天赋、局中小天赋、局中大天赋：第 2 战后只出现小天赋，第 5 战后只出现大天赋；无限模式从第 14 战开始每 6 战交替两档。
- 两个局中档位各扩充到 6 项。小天赋统一收敛为约 8%–15% 常驻战力或 4 金净经济；大天赋以“德川家康”为锚，加入并校准抢先施法、持续治疗、斩杀、风险输出和开战护航等同档选择。
- 开局协议按起始棋子的 1/2 费价值重新补偿：成熟稳重与舞台梦增加金币，火热整活和持久抗压收回过强附加值，热点追踪与稳定输出保留方向性优势。
- 同档天赋在全部拿完前严格不重复；只有极后期耗尽该档 6 项后才回补旧项。回补项会标为“再次强化”，所有数值按层数生效。
- 图鉴、选择页、结算按钮、提示、历史记录和 `render_game_to_text` 均显示小/大档位、选择 ID、层数与历史；图鉴按三类分组展示。
- 新增 `scripts/tests/autochess-talents.test.mjs` 和系统 Chrome 专项 `scripts/verify-autochess-talents.cjs`，覆盖轮次分档、去重、耗尽回补、开局资产、关键数值、真实点击、图鉴滚动和截图有效性。
- 系统 Chrome 使用 seed 4 验证：第 2 战为花呗生活/弹幕加速/体能储备，第 5 战为德川家康/收割/出道推流；实际点击后历史档位正确，耗尽小天赋池后才出现“再次强化”。七张 1440×900 截图均非黑、非透明，逐张打开检查，控制台无 error。
- 截图：`.tmp/autochess/talents/opening-talents.png`、`talent-codex.png`、`talent-codex-major.png`、`talent-codex-major-bottom.png`、`minor-talents-round-2.png`、`major-talents-round-5.png`、`minor-talents-exhausted-refill.png`。
- 验证：TypeScript、专项数据/战斗/Phaser 回归 `46/46`、`git diff --check` 与系统 Chrome 专项通过；完整自走棋套件 `113/114`，唯一失败仍是既有“满能量远程单位会先进入攻击距离再施法”断言。仓库长流程 `verify-autochess.cjs` 仍在已有的首战结算等待处超时。

## 2026-07-25 · 连锁三星场上位置优先

- 修复连锁合成的保留位优先级：此前备战席一星先合出的二星会覆盖场上已有二星的优先级，导致最终三星错误留在备战席。
- 现在每一轮合成均先选择场上材料；仅在该轮没有场上材料时，才保留连锁合成刚生成的位置。
- 回归覆盖一枚场上一星的跨区二星，以及“场上一枚二星 + 备战席一枚二星与三枚一星”的完整连锁三星场景。
- 验证：两项升星回归 `2/2`、`pnpm exec tsc --noEmit` 与 `git diff --check` 通过。
- 系统 Chrome 已确认备战画布截图有效、非黑且布局正常；仓库长流程仍在已有的首局结算等待处超时，与此次合成保留位无调用关系。

## 2026-07-25 · 25 战主线与完整收入难度曲线

- 将主线从 8 战扩展为 25 战；第 5/10/15/20 战固定为精英关，第 25 战为终局首领。旧第 4/7 战精英标记与第 8 战提前首领峰值已收平。
- 第 26—40 战为普通无限，敌方预算按“5 利息 + 2 连胜 + 上一战实际星级赏金”逐战复投；第 41 战起为地狱无限，改按“20 利息 + 理财固定 2 金 + 2 连胜 + 上一战实际星级赏金”逐战复投。
- 敌方预算优先转换为人口和升星；10 人三星满编后，剩余预算才通过平方根属性倍率兑现，避免同一份经济增长被属性重复指数放大。删除原先每回合无上限 `+2` 护甲的隐藏成长，改为随星级和预算倍率小幅增加。
- 关键采样：第 26 战预算 96、预计结算收入 20；第 40 战预算 504，10 个三星敌人提供 30 赏金，叠加 20 利息、2 理财、2 连胜后，第 41 战预算增加 54 至 558。
- 理财Ⅱ改为每 4 金 1 利息、80 金封顶，即最高 20 利息；普通利息仍为最高 4。
- 主线天赋扩为第 2/5/10/15/20/25 战后交替小/大天赋；无限从第 31 战起每 6 战继续交替。
- `scripts/verify-autochess-progression.cjs` 使用系统 Chrome 验证第 5 战精英预警、第 26 战普通无限、第 41 战地狱无限、100 金理财仍为 20 利息、玩法说明和 390×844 移动端预警布局；五张截图均通过纯色、透明和近黑拒绝检查，已逐张打开确认无截断或遮挡，控制台与资源请求无错误。
- 截图：`.tmp/autochess/progression/round-05-elite-warning.png`、`round-26-normal-endless.png`、`round-41-hell-finance-cap.png`、`progression-rules.png`、`round-05-elite-warning-mobile.png`。
- 最终验证：`pnpm exec tsc --noEmit` 与 `git diff --check` 通过；完整 `pnpm autochess:test` 为 `116/117`，唯一失败仍是已有“满能量远程单位会先进入攻击距离再施法”断言。仓库长流程 `verify-autochess.cjs` 仍在既有首战 `result` 等待处超时。

## 2026-07-25 · 桌面羁绊多行收纳

- 修复桌面备战阶段羁绊数量多时横向越出主面板的问题；宽屏不再依赖 Phaser WebGL 几何遮罩和横向拖动，改为面板内最多三行的紧凑居中排列。
- 多行按数量均衡分配，18 种羁绊会稳定显示为 `6 / 6 / 6`，避免末行只剩一个标签；棋盘整体小幅下移并压缩行距，羁绊、方向说明、24 个部署格和备战席均不重叠。
- 同步更新仓库 Chrome 长流程与经济 UI 专项脚本中的棋盘坐标，并新增 `scripts/verify-autochess-trait-layout.cjs`，构造覆盖全部 18 种羁绊的九人阵容验证边界。
- 系统 Chrome 在 `2048×1104` 下实测 18 个标签逻辑边界为 `x=161.5..634.5`、`y=184..260`，全部位于 `x=48..748` 的主面板羁绊区内；截图 `.tmp/autochess/trait-layout/all-traits-desktop.png` 已打开检查，非黑、非透明，画布、文本状态与控制台错误交叉检查通过。
- 验证：`pnpm exec tsc --noEmit`、Phaser 静态回归 `26/26`、专项 Chrome 流程与 `git diff --check` 通过。完整 `pnpm autochess:test` 为 `114/115`，唯一失败仍是已有的“满能量远程单位会先进入攻击距离再施法”断言；源码 ESLint 无新增错误，仅保留已有 `_tree` 未使用警告。

## 2026-07-25 · 结算阵容固定行高与滚动视窗

- 用户反馈：战斗结算会随阵容数量增加持续缩小行高、头像和字号，希望先显示固定数量，剩余内容通过滚动条查看，同时保留棋子 hover 详情。
- 设计决策：继续使用现有 Phaser 结算层，避免只为单一面板引入第二套 DOM 状态与交互；两侧阵容改为独立的 6 行固定视窗，使用 48px 行高、固定 10/8px 字号，并显示当前可见名次范围。
- 已实现：我方与敌方各自支持鼠标滚轮、滚动槽点击和滚动块拖拽；只实例化当前可见行，因此被裁掉的行不会产生越界命中区域，可见行继续保留 hover / 点击详情。
- 验证：新增 `scripts/verify-autochess-result-scroll.cjs`，用系统 Chrome 构造 10 对 8 的结算数据，覆盖首屏 6 行、两侧独立滚动、滚动槽点击、滚动块拖拽、指标切换保持位置、滚动前后 tooltip、紧凑布局与完整文本排名；五张整页截图均通过纯色、透明和近黑拒绝检查，控制台无错误。
- 最终检查：`pnpm exec tsc --noEmit`、结算/Phaser 静态回归、`git diff --check` 和目标源码 ESLint 均通过（仅保留已有 `_tree` 未使用 warning）；完整 `pnpm autochess:test` 为 `116/117`，唯一失败仍是已有“满能量远程单位会先进入攻击距离再施法”断言。

## 2026-07-25 · 帕可治疗区增强与目标生命解耦

- “天使摸鱼”半径由 `128` 提高到 `145`；落地治疗后留下持续 `3.2` 秒的区域，每 `0.7` 秒重新治疗当前位于范围内的友军，共触发 4 次脉冲。
- 治疗量完全按帕可自身属性计算，不读取目标最大生命：落地为 `1.6 × 帕可攻击 + 8% 帕可最大生命`，每次脉冲为 `0.7 × 帕可攻击 + 2.5% 帕可最大生命`。
- 一星帕可落地约治疗 `40`、每次脉冲约 `16`、总计约 `105`；二星与三星随帕可自身属性成长。三星五费大核不会反向放大一星帕可的奶量。
- 治疗区是固定战场区域：走出后停止回血，后续走入的友军可以从下一次脉冲开始获得治疗；`render_game_to_text` 暴露范围、剩余时间和下次脉冲时间。
- 回归覆盖普通友军与 `10000` 最大生命模拟大核获得相同治疗、进出范围、四次脉冲和敌方不受影响。帕可/轴伊专项 `2/2`、帕可数据专项 `1/1`、`pnpm exec tsc --noEmit` 通过；完整自走棋套件 `116/117`，唯一失败仍是既有远程施法距离断言。
- 用户明确表示由其负责视觉验证，本轮停止后续浏览器截图流程。

## 2026-07-25 · 修复线上构建 ESLint errors

- 修复自走棋构建阻断项：图鉴天赋分组的 `tier` 变量遮蔽、波次计算的 `unit` 变量遮蔽、条件箭头表达式、循环内预算闭包和 `Math.pow` 限制。
- 目标源码 ESLint 已达到 `0 errors`，`pnpm exec tsc --noEmit` 通过；清理不一致的本地 `.next` 生成产物后，`pnpm run build` 完整通过，线上构建也已通过。
- 构建仍保留仓库既有非阻断 warning；`pnpm autochess:test` 当前为 `116/117`，唯一失败仍是已有“满能量远程单位会先进入攻击距离再施法”断言，与本轮 lint 修复无关。
